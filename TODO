We moved TODO list to our Wiki. You may find it at the following link:

  http://wiki.scummvm.org/index.php/TODO

Own ToDo for http://wiki.scummvm.org/index.php/TODO#Iterator_handling:
  ✓ Make reverse in list_intern
      - add reverseIterator and ReverseConstIterator (like SGI documentation)
        ✓ self
        ✓ reverseIterator()
        - reverseIterator(const reverseIterator& x)
        ✓ reverseIterator& operator=(const reverseIterator& x)
        ✓ reverseIterator(RandomAccessIterator x)
        - RandomAccessIterator base()
        ✓ Reference operator*() const
        ✓ reverseIterator& operator++()
        ✓ reverseIterator operator++(int)
        ✓ reverseIterator& operator--()
        ✓ reverseIterator operator--(int)
        - reverseIterator operator+(Distance)
        - reverseIterator& operator+=(Distance)
        - reverseIterator operator-(Distance)
        - reverseIterator& operator-=(Distance)
        ✓ T* value_type(const reverseIterator&)
        - Reference operator[](Distance)
        - reverseIterator operator+(Distance, reverseIterator)
        - Distance operator-(const reverseIterator&, const reverseIterator&)
        ✓ bool operator==(const reverseIterator&, const reverseIterator&)
        ✓ bool operator<(const reverseIterator&, const reverseIterator&)
        - random_access_iterator_tag iterator_category(const reverseIterator&)

      - refactor list_intern (less copi paste code)

  - Refactor to mantain code estable:
      ✓ reverseErase (overloading)
      ✓ reverseBegin & reverseBegin const (renamed as lastElement)

  - Make test for reverse
