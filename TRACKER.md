# TRACKER

## Progress

- Program: `HARVEST.LE`
- Total functions: `871`
- Named/documented: `819`
- Remaining `FUN_*`: `52`
- Remaining undocumented total: `52`

## Next Suggested Action

- Immediate: run an in-engine Harvester validation pass on the Sergeant remains/lodge handoff with `Show CD change prompts` enabled. Recreate the `GOT_REMAINS_FOR_LODGE` branch so `0x4333 -> C150.FST -> 0x433f..0x4385 -> C076.FST` fires, confirm line `0x434f` now uses the PC portrait, and verify the native black-screen + `CD3.BM` prompt appears after `C076.FST` before returning control. Then retry the lodge approach to determine whether the still-unopened lodge entry is a separate room/map-script issue rather than a remaining `handle_talk_to_sergeant` parity bug.
- Immediate: run an in-engine Harvester validation pass focused on the `WOLFIE` crypt kill after the newly confirmed native parity check. Keep `combat` logging enabled, kill `WOLFIE`, and confirm the death bank still plays through to completion before the monster disappears, that `WOLFIE_DEATHCOM` only opens the crypt hotspots without leaving a clickable or visible monster residue, and that no stale monster entity survives in the live room after the `CRYPT` state refresh.
- Immediate: run an in-engine Harvester combat validation pass with one low-band melee loadout and one high-band weapon while `combat` logging is enabled. Recheck `FIREAXE` in `CRYPT` to confirm the new logs show the native `loadout=17 weapon='FIREAXE' damage=3`, then switch to a higher-damage weapon such as `SHOTGUN` (`loadout=3 damage=7`) or `CHAINSAW` (`loadout=14 damage=8`) and confirm the contact log changes accordingly. During the same pass, verify `WOLFIE` continues to report its seeded per-hit monster damage in the spawn and attack logs so we can tell whether the remaining difficulty complaint is native data/cadence rather than duplicate hit application.
- Immediate: rerun the in-engine `MBLM` glue validation pass after the stale-reference crash fix. With Moynahan alive (`MOYNAHAN_IS_DEAD=0`), click the room `GLUE` hotspot and confirm `MBLMGLUE_COM -> START_DIALOG MOYNAHAN_MBLM` still lands in the restored Moynahan handler, the native refusal lines `0x3e4d/0x3e52/0x3e56 -> 0x3e76/0x3e7a` play, the denied pickup still rolls back out of carry/inventory state, and dismissing the dialogue no longer trips the main-thread assert in `Common::String::compareToIgnoreCase` from the post-dialogue rollback check. Then validate both confirmed native acquisition paths still work: present `CASKET_PHOTO` / `CASKET_PHOTOCOPY` to Moynahan and take the glue-granting response at `dialog.rsp[0x18d]`, and on day 5 follow the `0x1bf..0x1c2` glue topic through response menu `0x1c3` to confirm `GLUE` plus `JUST_GOT_GLUE`. Finally, recheck the direct room-click pickup with `MOYNAHAN_IS_DEAD=1` to confirm the script-side `ADD_GLUE` branch still bypasses dialogue and awards the object cleanly.
- Immediate: run an in-engine Harvester validation pass around the day-5 overnight CD handoff and the hidden warp-to-CD3 path. With launcher option `Show CD change prompts` enabled, trigger `CHANGE_TO_CD2_A -> CHANGE_TO_CD2_B -> CHANGE_TO_CD2_C` and confirm the native `CD2.BM` prompt appears after `OVERNITE.FST`, waits for a fresh left-click press/release cycle, and then continues into `PC_ROOM_HOUSE_START`; also trigger `WARP_TO_LODGE_A -> WARP_TO_LODGE_B` and confirm the `CD3.BM` prompt appears after `C076.FST`. Repeat the same paths with the option disabled and confirm the prompt is skipped while the room handoff still completes.
- Immediate: run an in-engine Harvester validation pass that covers both the startup title menu and the post-death `GAMEOVER.BM` overlay. Confirm the menu now preserves the native six-row layout with `main_menu_2` (`SAVE GAME`) blanked when no live room/session state exists, `main_menu_3` (`LOAD GAME`) still visible and functional from both entry points, and the `OPTIONS` / `HELP` / `QUIT GAME` rows retaining their original vertical spacing instead of collapsing upward.
- Immediate: run an in-engine Harvester validation pass in `PCHOUSE` against Jimmy's `MONSTERFY` combat branch. Confirm the monster still enters combat at the same native locomotion rate, keeping the room loop redraw-only behavior during chase/attack; confirm an open inventory no longer reopens every item bitmap from disk when HP changes or status items refresh; and confirm a lethal Jimmy hit now plays the recovered player death bank before the room loop exits through the native `GAMEOVER.BM` / `GAMEOVER.PAL` + `ANXIETY.CMP` backdrop path instead of jumping directly out on `HP=0`.
- Stay in live Ghidra on `call_hmi_driver_callback_op0` and its caller frame inside `initialize_sound_bank_resources`. The upstream source record is now labeled as `g_sound_bank_setup_source_record`, the callback-capture block at `0xca2d8` is typed as `g_sound_selected_hardware_config`, and the first three fields there are confirmed as `device_port`, `device_irq`, and `device_dma`. The remaining useful confirmation is whether the mixed far-pointer / stack-cleanup oddity in `call_hmi_driver_callback_op0` can be modeled well enough to expose what, if anything, the unresolved source dwords at `g_sound_bank_setup_source_record + 0x14` and `+ 0x18` contribute to the callback.
- Before trusting `ClearTalkHandlerDialogueOverrides.java` as a full replacement for `InspectCallTermination.java`, keep validating it against single known-dirty handlers and immediately follow with `InspectCallTermination.java verify`. `CLOAK_ATND` showed the hardened bulk-clear script can safely remove a stale `CALL_RETURN`, but it also showed that already-cleared fallthroughs with `nextInst=null` may still need the explicit `InspectCallTermination.java repair` logic afterward. The next useful confirmation is another one-gap handler of the same shape, or a script update that directly repairs `missing_next_instruction` fallthroughs after the override is already clear.
- Before using the bulk dialogue cleanup path in the live database again, validate the now-hardened `/Users/alex/ghidra_scripts/ClearTalkHandlerDialogueOverrides.java` on a disposable copy or a single known-dirty talk handler via the new NPC filter. The script still clears each stale `CALL_RETURN` override and hands the fallthrough address to `ClearFlowAndRepairCmd`, but it now aborts on the first repair failure and rolls the whole transaction back on cancel instead of committing a partial sweep. The next useful confirmation is whether a single-handler run such as `MOM` or `MR_POTTS` repairs cleanly and durably with the broader Ghidra repair path.

## Last Confirmed Action

- On March 26, 2026, rechecked `handle_talk_to_sergeant` against live `HARVEST.LE` after the user-reported lodge-entry regression. `InspectCallTermination.java verify SERGEANT` showed the saved handler had regressed to a truncated `0x30340..0x30fca` body with only `12` reachable dialogue calls and `12` stale `CALL_RETURN` gaps; a filtered `/Users/alex/ghidra_scripts/ClearTalkHandlerDialogueOverrides.java SERGEANT` pass cleared all `12` stale overrides, `InspectCallTermination.java repair SERGEANT` finished the remaining three fallthrough repairs, and a saved re-verify restored the full clean body `0x30340..0x30fd2` with `70` dialogue calls and `gapCount=0`. The cleaned native body confirmed there is no extra lodge-opening flag/object mutation in the Sergeant handler; the concrete engine drifts were narrower: ScummVM had misattributed line `0x434f` to `SERGEANT` instead of `PC`, and the remains branch was missing the native post-`C076.FST` black-screen plus `prompt_for_cdrom_disc_change(3, 0)` tail. Patched [`engines/harvester/dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/dialogue.cpp), [`engines/harvester/npc/dialogue_runtime.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/dialogue_runtime.h), and [`engines/harvester/npc/sergeant_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/sergeant_dialogue.cpp) to mirror that native behavior and rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, rechecked Harvester's native `WOLFIE`-style room-monster death completion in live `HARVEST.LE` to answer whether post-animation disappearance is original behavior. Ghidra confirmed that native class-6 monsters do not vanish on the lethal hit itself: `update_actor_runtime_state` first pushes them through the facing-specific death families `0x28..0x33`, then waits for the last death frame before entering terminal state `0x38` and dispatching the monster record's `on_death_action_tag`. The same live pass also reconfirmed that `room_setup` only respawns monsters whose records still satisfy `(active != 0 || visible != 0) && runtime_spawned == 0`, so a monster that has completed its death/remove path is not rebuilt into the room on the next same-room setup pass. That makes the current ScummVM behavior of letting `WOLFIE` finish the death bank and then disappear from the crypt a native-correct result rather than a `debug_combat`-only bug. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, rechecked Harvester's native combat damage seeding in live `HARVEST.LE` before changing the ScummVM formulas. Ghidra confirmed that `spawn_monster_entity_from_record` copies the monster record's `damage_amount` straight into live actor field `+0x1180`, and that `update_actor_runtime_state` subtracts that field directly on a confirmed hit with no extra post-hit multiplier. The same pass over `set_player_combat_loadout` also exposed a real player-weapon tuning bug in ScummVM: the native table uses `CHAINSAW -> loadout 14 -> damage 8` and `HARVEST_BLADE -> loadout 15 -> damage 3`, but the engine had those two loadouts swapped. Patched [`engines/harvester/player.h`](/Users/alex/Workspace/scummvm/engines/harvester/player.h), [`engines/harvester/player.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/player.cpp), [`engines/harvester/inventory.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/inventory.cpp), [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp), and [`ARCHITECTURE.md`](/Users/alex/Workspace/scummvm/ARCHITECTURE.md) so the loadout table now matches native, and combat logs now print the resolved weapon/loadout label plus the native per-hit damage values during inventory toggles and player/monster hit resolution. Rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully with `make -j4 scummvm`. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, traced the `MBLM` glue assert to a stale `StartupObjectRecord` reference in the room pickup loop, not to `Common::String` itself. The post-dialogue carry rollback added in [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) was still reading `object.objectName` and `object.actionTag` after `handleInteractionResult()`, but that interaction can rebuild the room scene and invalidate the clicked-object reference. Patched both room pickup branches to use the pre-saved object copy for all post-interaction logging and rollback checks, including the `isObjectInInventory()` probe that had been asserting in `Common::String::compareToIgnoreCase`. Rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully with `make -j4 scummvm`. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, traced the remaining `MBLM` glue regression to the room-item carry path rather than the Moynahan dialogue handler. The alias fix was already working: live logs showed `MBLMGLUE_COM` reaching `MOYNAHAN_MBLM` and playing the native refusal lines `0x3e4d` / `0x3e52` / `0x3e56` / `0x3e76` / `0x3e7a`, but [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) was still leaving the denied `GLUE` as a carried room item because the carry-start rollback only handled modal-text denials. Patched the carried-pickup branch so, after a pickup action opens dialogue, it restores the original room object and clears the transient carry state unless that dialogue path explicitly moved the object into inventory; rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully with `make -j4 scummvm`. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, traced the `MBLM` glue regression to an alias mismatch between the room script and the restored Moynahan dialogue handler. The native room action `MBLMGLUE_COM` already dispatches `START_DIALOG MOYNAHAN_MBLM` whenever `MOYNAHAN_IS_DEAD` is clear, but [`engines/harvester/npc/moynahan_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/moynahan_dialogue.cpp) was still matching only `MOYNAHAN`, so live room clicks fell through to `unsupported room NPC dialogue handler 'MOYNAHAN_MBLM'` instead of entering the confirmed `IF_TRY_TO_TAKE_THE_GLUE` interruption path. Patched the handler to accept both speaker ids and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully with `make -j4 scummvm`. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, rechecked Harvester's native CD-swap script path in live `HARVEST.LE` before patching the ScummVM port. Ghidra confirmed that `parse_command_record` maps `CHANGE_CD` to opcode `0x23`, that `dispatch_room_event_actions` keeps executing through `CHANGE_ROOM` and only then calls `prompt_for_cdrom_disc_change`, and that `prompt_for_cdrom_disc_change` formats `1:\graphic\other\CD%i.BM`, loads `1:\graphic\pal\cd1.pal`, and waits for a fresh left-click press/release cycle before continuing. The decoded `HARVEST.SCR` chain around the day-5 handoff also matched that native control flow exactly: `CHANGE_TO_CD2_A` plays `GRAPHIC\FST\OVERNITE.FST`, `CHANGE_TO_CD2_B` queues `CHANGE_ROOM PC_ROOM_HOUSE_START`, and `CHANGE_TO_CD2_C` issues `CHANGE_CD 2`; the only other transition continuations in the shipped script are the `WARP_TO_CD2_*`, `STLEXIT2_*`, and `TO_CRYPT_TOP` tails. Patched [`engines/harvester/metaengine.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/metaengine.cpp), [`engines/harvester/detection.h`](/Users/alex/Workspace/scummvm/engines/harvester/detection.h), [`engines/harvester/detection_tables.h`](/Users/alex/Workspace/scummvm/engines/harvester/detection_tables.h), [`engines/harvester/harvester.h`](/Users/alex/Workspace/scummvm/engines/harvester/harvester.h), [`engines/harvester/harvester.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/harvester.cpp), [`engines/harvester/script.h`](/Users/alex/Workspace/scummvm/engines/harvester/script.h), [`engines/harvester/script.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/script.cpp), [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp), and [`engines/harvester/dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/dialogue.cpp) so Harvester now exposes a launcher `Show CD change prompts` option, preserves `CHANGE_CD` after `GOFLIC` / `CHANGE_ROOM` continuations, and renders the native CD prompt image gated behind a fresh left click when the option is enabled. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, rechecked the native room-combat death path in live `HARVEST.LE` and corrected the ScummVM player defeat flow to match it more closely. Ghidra confirmed that `update_player_combat_avatar_state` does not hand control to `run_game_over_screen` on raw `HP <= 0`; it only returns true once the player combat avatar reaches terminal state `0x38`, after `update_actor_runtime_state` has already selected and played a concrete death-animation bank from the live combat state. The recovered death-bank selection block at `0x4e5a3..0x4e7ea` plus the state handlers at `0x5045a..0x50991` bounded the exact player ranges (`0xc4..0xff` for vertical deaths, `0xb0..0xc3` and `0x100..0x127` for horizontal deaths) and showed the death animation inherits the native hit-reaction cadence rather than the normal walk rate. Patched [`engines/harvester/player.h`](/Users/alex/Workspace/scummvm/engines/harvester/player.h), [`engines/harvester/player.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/player.cpp), [`engines/harvester/room_support.h`](/Users/alex/Workspace/scummvm/engines/harvester/room_support.h), and [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) so lethal room-combat hits now start the recovered player death bank, suppress further region activation while that animation runs, and only queue the existing game-over return once the death sequence completes. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, rechecked the native `run_main_menu` / `run_game_over_screen` path in live `HARVEST.LE` before touching the ScummVM menu layout. Ghidra confirmed that `run_game_over_screen` clears `g_game_session_active`, leaves `run_main_menu` responsible for the overlay, and that `run_main_menu` preserves all six `main_menu_*` slots while blanking `main_menu_2` (`SAVE GAME`) when no live room/session state exists instead of collapsing the list. Patched [`engines/harvester/menu.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/menu.cpp) so the ScummVM startup/game-over menu now mirrors that native policy: `SAVE GAME` is blanked in place when unavailable, `LOAD GAME` remains visible when loading is allowed, and the downstream rows keep the original spacing. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, verified in live `HARVEST.LE` that native actor animation timing still uses `animation_tick_interval_ms = 100 / animation_rate`, with the room-combat locomotion path seeding `animation_rate = 0x11` inside `update_actor_runtime_state` (`0x4d750`), so the ScummVM combat-speed complaint was not evidence for changing the raw room-monster animation constant. Patched [`engines/harvester/inventory.h`](/Users/alex/Workspace/scummvm/engines/harvester/inventory.h), [`engines/harvester/inventory.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/inventory.cpp), and [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) so inventory overlay refreshes now cache decoded bitmap resources by path instead of reopening every icon file on HP-driven refresh, and lethal room-combat monster hits now request the existing native game-over return path instead of leaving the player at `HP=0` inside the live room loop. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, confirmed the native Harvester `GODEATHFLIC` path in live `HARVEST.LE` and patched the ScummVM port to match it instead of dropping straight into the generic main-menu stub. Ghidra showed `dispatch_room_event_actions` case `0x1c` calling `play_fst_sequence(arg1, 0)` and then `run_game_over_screen` (`0x7c540`), and `run_game_over_screen` in turn loading `1:\GRAPHIC\OTHER\GAMEOVER.BM` plus `1:\GRAPHIC\PAL\GAMEOVER.PAL`, starting `SOUND\MUSIC\ANXIETY.CMP`, waiting for dismissal input, and only then entering `run_main_menu` over that backdrop. Patched [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp), [`engines/harvester/flow.h`](/Users/alex/Workspace/scummvm/engines/harvester/flow.h), [`engines/harvester/flow.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/flow.cpp), [`engines/harvester/menu.h`](/Users/alex/Workspace/scummvm/engines/harvester/menu.h), and [`engines/harvester/menu.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/menu.cpp) so death FSTs now request a dedicated game-over return, the menu system stages the native `GAMEOVER` bitmap/palette plus `ANXIETY.CMP`, and the post-death menu overlays onto that backdrop instead of the generic wait-screen panel. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, completed the upstream HMI source-record follow-up on `g_sound_bank_setup_source_record` (`0xc2d2c`) and the callback-capture block at `0xca2d8`, reran `/Users/alex/ghidra_scripts/RefineSoundBankDataLayouts.java`, and saved the updated type information into `HARVEST.LE`. The pass improved the live disassembly without overclaiming new semantics: it relabeled the static source block as `g_sound_bank_setup_source_record`, typed `0xca2d8` as `g_sound_selected_hardware_config`, and confirmed that the shared low fields are `device_port`, `device_irq`, and `device_dma` because both the manual `SOUND.CFG` path and the HMIDET auto-detect path populate those exact slots before bank setup. The fourth staging dword remains only `callback_op1_dx_word`, because it is copied from the detect callback's `DX` result and no higher-level meaning is confirmed. The source-side dwords at `+0x14` and `+0x18` remain unresolved: in the shipped image they are zero-initialized, `+0x18` is still only observed as the archived `reserved_setup_dword`, and no independent producer or consumer was confirmed beyond the opaque `call_hmi_driver_callback_op0` path. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, completed the consumer-side HMI bank follow-up on `g_sound_bank_config_snapshots` and `g_sound_bank_hmi_callback_records`, reran `/Users/alex/ghidra_scripts/RefineSoundBankDataLayouts.java`, and saved the updated type information into `HARVEST.LE`. The pass replaced `field_24_code` with `temp_buffer_fill_mode_code`, because the only observed consumer is the setup path that checks for mode `8` before filling the transient DOS buffer with either `0x80` bytes or zeroes. It replaced `field_11` with `requested_mix_rate_hz`, because the caller seeds the source record with `0x5622` (`22050`) immediately before bank setup and the bank setup path forwards that dword into `call_hmi_driver_callback_op2` as the only payload register. The former `field_19` remains `reserved_setup_dword`: it is copied into the snapshot, but the only observed callback wrapper on that path (`call_hmi_driver_callback_op0`) ignores the stack slot where it is passed, and no other direct consumers were found. The same pass also closes the slot-index question conservatively: the live flag-`0x80`, flag-`0x40`, and temp-buffer slabs are packed as five 6-byte entries each, and the only normal producer of a bank index is `initialize_sound_bank_resources`, which scans `idx = 0..4`; the looser `< 6` checks in lower helpers therefore do not justify a reserved sixth bank slot. Updated [`ARCHITECTURE.md`](/Users/alex/Workspace/scummvm/ARCHITECTURE.md) with the refined field names and the slot-geometry note. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, ran `InspectCallTermination.java repair authority` in the live `HARVEST.LE` database and confirmed the saved result. The pre-check showed `handle_talk_to_authority @ 0x2b730` in a short dirty state (`0x2b730..0x2b7b2`, `95` bytes) with `dialogueCalls=2`, `gapCount=2`, and stale `CALL_RETURN` gaps at `0x2b75a` and `0x2b7a9`, including one missing fallthrough instruction at `0x2b75f`. The repair completed in one pass, expanded the handler body to `131` bytes while preserving the same entry/end range, recovered one additional reachable dialogue call, and finished with `gapCount=0`, `status=clean`, and `repairActions=4`. An initial parallel save attempt hit the expected active-transaction lock while the script was still unwinding, but the immediate retry saved `HARVEST.LE` successfully, and a fresh `verify authority` still reports `dialogueCalls=3`, `gapCount=0`, and `status=clean`. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, ran `InspectCallTermination.java repair CLOAK_ATND` in the live `HARVEST.LE` database after the filtered `ClearTalkHandlerDialogueOverrides.java` pass had removed the stale `CALL_RETURN` but left the fallthrough byte at `0x2dbca` undefined. The repair completed in a single pass on `handle_talk_to_cloak_atnd @ 0x2db80`: the pre-pass state was a 79-byte body with `dialogueCalls=1`, `gapCount=1`, and one remaining gap at `0x2dbc5`; the repair applied `2` actions, preserved the handler body bounds `0x2db80..0x2dbce`, and finished with `gapCount=0`, `status=clean`. Saved `HARVEST.LE` successfully and confirmed the clean state with a fresh `verify CLOAK_ATND`, which still reports `gapCount=0` and both dialogue entry points returning. This closes the residual `CLOAK_ATND` cleanup noted by the earlier filtered bulk-clear experiment and confirms that `InspectCallTermination.java` remains the reliable fallback for already-cleared fallthroughs whose next instruction is still undefined. Documentation counts remain `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, completed the sound-bank data-layout pass behind `initialize_sound_bank_resources` in live Ghidra and saved it as the rerunnable helper script `/Users/alex/ghidra_scripts/RefineSoundBankDataLayouts.java`. The pass proved that the allocator loop scans five bank slots (`idx < 5`) even though several helper routines still accept indices up to `5`, promoted `FUN_00084a8b` to `allocate_sound_bank_temp_dos_buffer`, and replaced the key raw slabs with typed names: `g_sound_bank_flag80_primary_far_ptrs`, `g_sound_bank_flag80_secondary_far_ptrs`, `g_sound_bank_flag40_primary_far_ptrs`, `g_sound_bank_flag40_secondary_far_ptrs`, `g_sound_bank_temp_dos_buffer_far_ptrs`, `g_sound_bank_hmi_callback_records`, and `g_sound_bank_config_snapshots`. The new types `FarPtr48`, `FarPtrSlot48`, `HmiDriverCallbackRecord`, and `SoundBankConfigSnapshot` now surface the confirmed field geometry directly in decompilation: the callback record exposes `field_24_code`, `callback_flags`, and the fallback `flag40_record_id`, while the config snapshot preserves the copied temp-buffer size, the control-entry far pointer, the live flag-`0x80` / flag-`0x40` far-pointer slots, and the explicit flag-`0x40` override dword. Updated [`ARCHITECTURE.md`](/Users/alex/Workspace/scummvm/ARCHITECTURE.md) with the corrected slot count and named slabs. Live documentation counts are now `871` total functions, `819` named/documented, and `52` remaining `FUN_*`.
- On March 26, 2026, ran the hardened `/Users/alex/ghidra_scripts/ClearTalkHandlerDialogueOverrides.java` against the single handler filter `handle_talk_to_cloak_atnd` in the live `HARVEST.LE` database. The pre-check via `InspectCallTermination.java verify CLOAK_ATND` showed a 79-byte body at `0x2db80..0x2dbce` with one stale dialogue gap at `0x2dbc5` (`flowType=CALL_TERMINATOR`, `override=CALL_RETURN`, `fallthrough=null`, `next=0x2dbca`, `nextInst=XOR EAX,EAX`). The filtered cleanup run reported `CLEARED=1`, `REPAIRED=1`, and saved successfully after an initial parallel save attempt hit the expected active-transaction lock. A fresh post-save verify confirmed that the stale override is gone and the call is now `UNCONDITIONAL_CALL` with `override=NONE` and `fallthrough=0x2dbca`, but `nextInst` at `0x2dbca` is still undefined, so the handler remains `status=needs_repair` with `gapCount=1`. This confirms the hardened script is safe enough to clear and save a single handler, but it does not yet fully replace the explicit fallthrough repair logic in `InspectCallTermination.java`. Documentation counts remain `871` total functions, `818` named/documented, and `53` remaining `FUN_*`.
- On March 26, 2026, hardened the Ghidra helper script `/Users/alex/ghidra_scripts/ClearTalkHandlerDialogueOverrides.java` after the first `ClearFlowAndRepairCmd` upgrade so it is safer to test against the talk-handler cleanup problem. The script now accepts an optional exact NPC filter (`MOM` or `handle_talk_to_mom`), still clears each instruction-level `CALL_RETURN` override before invoking `ClearFlowAndRepairCmd` at the fallthrough address, aborts immediately on the first repair failure instead of logging-and-continuing, and uses `monitor.checkCancelled()` so canceling the run rolls the entire transaction back instead of committing a partial batch. Also retained the corrected `ghidra.program.model.listing.FlowOverride` import and re-verified the edited script with a compile-only `javac` pass against the local Ghidra 12.0.3 jars. No live `HARVEST.LE` database changes were made, so documentation counts remain `871` total functions, `818` named/documented, and `53` remaining `FUN_*`.
- On March 26, 2026, finished the parent-layer HMI pass around the newly named callback wrappers and promoted the two remaining orchestrators. `FUN_0008aa3a` is now `refresh_hmidrv386_flag80_record_from_callback`, because it is only called from the bank-setup path after the flag-`0x80` selector pair is loaded or restored and its only work is to validate that selector pair and refresh the live `0x6a`-byte callback record through `copy_hmi_driver_record_from_callback_op8`. `FUN_00083fb3` is now `initialize_sound_bank_resources`, because its only caller is `initialize_sound_driver_from_sound_cfg_or_autodetect`, its teardown pair is `release_sound_bank_resources`, and the function itself chooses a free bank slot, loads/reuses the HMIDRV386 flag-`0x80` and optional flag-`0x40` records, allocates the transient DOS buffer when requested, captures callback control-entry pointers, runs the callback init sequence, and copies the selected driver config into the per-slot runtime tables. Added matching plate comments in Ghidra, updated [`ARCHITECTURE.md`](/Users/alex/Workspace/scummvm/ARCHITECTURE.md) with the confirmed per-bank setup note, and saved `HARVEST.LE`. Live documentation counts remain `871` total functions, `818` named/documented, and `53` remaining `FUN_*`.

- On March 26, 2026, completed a conservative naming pass over the real-RAM HMI callback helper cluster at `0x87005..0x87439` and documented the resulting subsystem facts. The new Ghidra script `/Users/alex/ghidra_scripts/RefineHmiCallbackHelpers.java` renamed 14 wrappers and attached plate comments that preserve the exact observed opcode/register behavior instead of guessing higher-level semantics: the `0x6a`-byte record copiers are now `copy_hmi_driver_record_from_callback_op8[_raw]`; the callback op wrappers are now `call_hmi_driver_callback_op0`, `call_hmi_driver_callback_op1[_capture_result]`, `call_hmi_driver_callback_op2`, `call_hmi_driver_callback_op3_*`, `call_hmi_driver_callback_op4`, `call_hmi_driver_callback_op5`, `call_hmi_driver_callback_op10_capture_ptr_triplet`, and `chain_hmi_driver_callback_op11_into_op12`; and the direct interrupt helpers are now `query_windows_enhanced_mode_signature_raw` and `query_dpmi_service_0500_global`. The evidence came from the surrounding HMIDET/HMIDRV call graph: `initialize_hmidet386_context`, `find_first_detected_hmidet_driver_record`, `find_next_detected_hmidet_driver_record`, `load_hmidet_driver_record_by_device_id`, `initialize_hmidet_driver_from_selected_record`, `release_sound_bank_resources`, `FUN_00083fb3`, the `hmidet.386` / `hmidrv.386` strings, the `int 2F AX=1600` Windows-enhanced-mode probes, and the DPMI / `int 4B` setup-teardown path. Also updated [`ARCHITECTURE.md`](/Users/alex/Workspace/scummvm/ARCHITECTURE.md) with the confirmed HMI bootstrap notes. Live documentation counts remain `871` total functions, `816` named/documented, and `55` remaining `FUN_*`.

- On March 26, 2026, created and ran the new Ghidra helper script `CleanupImagePseudoFunctions.java` in `/Users/alex/ghidra_scripts`, then applied it to `HARVEST.LE` and saved the program. The script maps `.image::` pseudo-function entries into normal RAM with the confirmed delta `loaded = .image - 0x1c9b0`, classifies each target as a real function entry or a mid-function offset, and supports dry-run/report versus apply/delete cleanup. The dry run resolved all 43 candidates cleanly: 42 were exact duplicates of real RAM functions and 1 (`.image::00079b9d`) was a false function start inside `update_render_entity_screen_position` at `0x5d1ed`. The apply pass deleted all 43 `.image` pseudo-functions, and a follow-up symbol search confirmed there are no remaining `FUN_.image__*` functions in the database. Live documentation counts are now `871` total functions, `802` named/documented, and `69` remaining `FUN_*`.

- On March 26, 2026, performed a focused live-Ghidra investigation of the stray `FUN_.image_*` symbols in `HARVEST.LE` and confirmed they are analysis artifacts in the separate `.image` address space, not a coherent subsystem and not trustworthy standalone functions. Every sampled `.image` symbol had a one-byte body, zero decoded instructions, no callers, and decompiled only as `halt_baddata()`, even though raw bytes at those addresses are valid x86 code. Cross-checks showed the bytes map back into normal RAM-space code at a stable delta of `loaded = .image - 0x1c9b0`: `.image::000a3789` and `.image::000a37c6` map to the real DPMI wrappers `lock_linear_region_via_dpmi` (`0x86dd9`) and `unlock_linear_region_via_dpmi` (`0x86e16`); `.image::000a7b5d` maps to the IRQ/PIT helper cluster around `program_irq0_pit_divisor` (`0x8b1ad`); `.image::0004c1ad` maps to the dialogue handler `FUN_0002f7fd`; `.image::0004ad64` maps to the dialogue handler `FUN_0002e3b4`; and `.image::00079b9d` maps into the middle of `update_render_entity_screen_position` (`0x5d1ed`), proving that some `.image` labels are false function starts. No rename or architecture update was justified from this pass. Live documentation counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.

- On March 25, 2026, synced the Harvester engine source to the latest high-confidence live-Ghidra naming recovered from `update_actor_runtime_state` (`0x4d750`) and related actor-runtime helpers. Patched [`engines/harvester/monster.h`](/Users/alex/Workspace/scummvm/engines/harvester/monster.h), [`engines/harvester/room_support.h`](/Users/alex/Workspace/scummvm/engines/harvester/room_support.h), [`engines/harvester/player.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/player.cpp), [`engines/harvester/script.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/script.cpp), and [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) so the engine now uses the same confirmed terms as the native database: monster spawn bounds are `screenMinXBound` / `screenMaxXBound` to match `g_entity_screen_min_x_bound` / `g_entity_screen_max_x_bound`; the hidden `"blood.abm"` combat helper is consistently described as a hit effect instead of a generic damage effect, matching `hit_effect_entity`; and Jimmy's melee audio timing field is `attackSoundPlaybackFrame`, reflecting that the native slot is a concrete playback/trigger frame rather than a loose state flag. Verified with a clean source sweep for stale identifiers and rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4`. Live Ghidra documentation counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, performed a focused live-Ghidra naming pass on `update_actor_runtime_state` (`0x4d750`) in `HARVEST.LE` and repaired `RenderEntityRuntime` so the actor-runtime offsets used by the decompile now carry stable names instead of raw `field_0x...` accesses. The high-confidence evidence came from cross-checking `update_actor_runtime_state`, `advance_entity_animation_frame` (`0x4c8a0`), `apply_pending_entity_knockback` (`0x53770`), `spawn_npc_entity_from_record` (`0x4cd00`), and `spawn_monster_entity_from_record` (`0x53a10`): `+0x109c` is the live blocker pointer (`current_blocker_entity`); `+0x10a0/+0x10a4/+0x10a8` are the per-frame movement deltas (`frame_x_step`, `frame_y_step`, `frame_z_step`); `+0x1088` is the one-frame shove slot (`frame_knockback_x_step`); `+0x1198` is the frame-left screen bound (`frame_left_x`); `+0x11a0/+0x11a1` are the active/desired actor state bytes; `+0x11a8` is the hidden hit-effect runtime (`hit_effect_entity`); `+0x11ac/+0x11b0` are the remaining knockback amount and decay step; and `+0x1134..+0x1178` now expose the monster attack/hit/footstep/death sound slots plus their trigger frames. Also renamed globals `0x0c3c08` / `0x0c3c0c` to `g_entity_screen_min_x_bound` / `g_entity_screen_max_x_bound`, because `advance_entity_animation_frame` clamps actor screen movement against those values and monster spawn seeds them from `MonsterRecord.min_x_bound` / `max_x_bound`. Saved the Ghidra program after rebuilding `RenderEntityRuntime` as a fixed-size `replaceAtOffset` layout so the added names stayed at the correct offsets. Live documentation counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, rechecked Harvester's live combat cadence in Ghidra before touching source. `tick_monster_entity_runtime` (`0x54140`) and `update_player_combat_avatar_state` (`0x553a0`) both gate their handoff into `update_actor_runtime_state` (`0x4d750`) on the actor's animation-advanced flag, falling back to `tick_entity_visual_state` when no new frame has advanced; the same player wrapper also short-circuits the live combat path once `current_hit_points < 1`. Patched [`engines/harvester/runtime_entity.h`](/Users/alex/Workspace/scummvm/engines/harvester/runtime_entity.h), [`engines/harvester/runtime_entity.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/runtime_entity.cpp), [`engines/harvester/player.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/player.cpp), and [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) so room movement now keys off the runtime entity's last animation advance once walk animations are active, monster chase updates stop over-driving between frames, and the room loop clears live movement/attack input when Jimmy's HP reaches `0`. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Live Ghidra documentation counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, deep-dived the native combat runtime around `spawn_npc_entity_from_record` (`0x4cd00`), `spawn_monster_entity_base` (`0x4d140`), and `update_actor_runtime_state` (`0x4d750`) in live `HARVEST.LE`. The new disassembly comments at `0x4cd45`, `0x4d2e7`, and `0x52cba` record the now-confirmed hit-feedback path: both NPC and monster constructors allocate a hidden `"blood.abm"` helper into live actor field `+0x11a8` with animation rate `10`, and the close-range hit-resolution block later forces that helper visible at frame `0` over the struck actor. Patched [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) so player hits on monsterfying NPCs now spawn a transient `blood.abm` effect, same-room runtime mutation can patch current-room NPC and monster entities in place instead of always falling back to `refreshCurrentScene(true)`, and the spawned replacement monster keeps the live room loop so the existing native-style chase/attack logic can take over immediately. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Live Ghidra documentation counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, rechecked Harvester's inventory handoff path directly in live Ghidra before touching source. `run_inventory_screen` at `0x7df10` still contains the native drag-close test at `0x7e364..0x7e38e`, using the recovered bounds `x=0x45..0x238` and `y=0x4b..0x1b3`, and its teardown returns a non-null carried `RenderEntityRuntime *` to `run_harvester_main_loop` when the inventory loop exits with an active item (`lines 1221..1267` in the current decompile). ScummVM's room loop was stricter: [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) only hid the overlay on `EVENT_MOUSEMOVE` while a selected inventory item was active if the primary button was still down, which forced an extra out-of-bounds click after the initial selection. Patched that branch so the latched inventory selection now closes the overlay on mouse move whenever the cursor leaves the recovered native drag-close region, removed the now-unused `isPrimaryMouseDown()` helper, and rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Live Ghidra documentation counts are now `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, traced Stephanie's looping `wav=0x4a05` regression to an ambiguity between two different `BYE` entries in the native `DIALOG.RSP`: zero-based `dialog.rsp[0x2e4]` is the generic keyword-menu exit, while zero-based `dialog.rsp[0x2fd]` is the hidden typed-topic alias inside Stephanie's spyhole continuation cluster (`0x2f9..0x2fd`). The shipped resource file confirms `dialog.rsp[0x2cf] = Those People/Wedding/Grounded/Amnesia`, `dialog.rsp[0x2f8] = Lodge/Amnesia`, and the hidden follow-up strings `0x2f9 = Peephole`, `0x2fa = Hole`, `0x2fb = Spy`, `0x2fc = Spyhole`, `0x2fd = BYE`; live Ghidra notes for `handle_talk_to_stephanie @ 0x36710` already bounded that orphan tail as reachable only after the visible buffer advances to `dialog.rsp[0x2f8]`, with the generic `BYE` still exiting through `0x46a4`. ScummVM was losing that distinction because [`engines/harvester/dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/dialogue.cpp) only returned the selected text, so a clicked generic `BYE` could text-match the hidden `0x2fd` branch in [`engines/harvester/npc/stephanie_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/stephanie_dialogue.cpp). Patched [`engines/harvester/npc/dialogue_runtime.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/dialogue_runtime.h) and [`engines/harvester/dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/dialogue.cpp) so keyword selection metadata now tracks typed `Other` input versus the hardcoded generic `BYE`, and tightened Stephanie's hidden spyhole branch to require both the native `dialog.rsp[0x2f8]` state and typed input before `0x2f9..0x2fd` are considered. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Progress counts remain `912` total functions, `800` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, traced the ESC -> `Options` label drift to native `MENU.INI` padding rather than a special-case slider anchor. Ghidra shows `run_main_menu` (`0x67390`) copying `options_menu_1..7` from `MENU.INI` into raw buffers and centering them by measured width; the shipped file keeps trailing spaces in `options_menu_1=SOUND FX           `, `options_menu_2=MUSIC         `, and `options_menu_3=GAMMA         ` so the visible glyphs sit left of center before `VOLUME.BM` / `INDICATR.BM` are drawn. ScummVM now preserves that raw spacing in [`engines/harvester/menu.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/menu.cpp) when loading the option labels, and [`ARCHITECTURE.md`](/Users/alex/Workspace/scummvm/ARCHITECTURE.md) records the confirmed native layout detail. Progress counts remain `912` total functions, `800` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, removed the temporary Harvester investigation probes from [`engines/harvester/runtime_entity.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/runtime_entity.cpp), [`engines/harvester/runtime_entity.h`](/Users/alex/Workspace/scummvm/engines/harvester/runtime_entity.h), [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp), and [`engines/harvester/flow.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/flow.cpp) after confirming the centered room-anim drift fix. Removed the door geometry/frame/draw traces, room click selection probes, actor-placement/render-order dumps, and the now-unused helper they depended on, while keeping the behavioral fixes for same-room live mutations, draw-order-based hotspot resolution, `SET_ANIM` hidden→visible runtime-state reset, and the cached centered screen base for room anims. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Progress counts remain `912` total functions, `800` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, fixed a centered room-anim drift bug in [`engines/harvester/runtime_entity.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/runtime_entity.cpp) after reconciling the latest sheriff screenshot with native `show_entity_visual` (`0x4c290`). The decisive native detail is that the centered branch calls `set_entity_screen_position` only on the initial show path (`field_0x4f == 0`); later frame advances in `advance_entity_animation_frame` (`0x4c8a0`) update `frame_x_offset/frame_y_offset` and call `show_entity_visual`, but do not recenter from the new frame width each draw. ScummVM now mirrors that by caching a centered anim's initial top-left screen base in `RuntimeEntity` and reusing it across frame advances instead of recomputing `_x - frame.width / 2` every draw. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Progress counts remain `912` total functions, `800` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, closed the sheriff-office first-draw timing branch. The new `door anim draw trace` probe in [`engines/harvester/runtime_entity.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/runtime_entity.cpp) showed that ScummVM does not draw `SHER_EVIDDOOR` frame `0`; the first actual draw is already frame `1`, then frame `2`. Ghidra confirms native uses the same ordering in `run_harvester_main_loop` (`0x6dc70`): it dispatches `handle_target_interaction()` at `LAB_0006f996`, then immediately walks the live render list at `LAB_0006fa76`, where class-1 room anims call `tick_entity_visual_state()` before the frame is flushed. Combined with the already-correct post-interaction selection probe and doorway geometry logs, this means the sheriff-office mismatch is no longer supported by click ownership, anim anchor, or first-draw timing evidence on the current build. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Progress counts remain `912` total functions, `800` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, added a narrow room-anim geometry probe for the affected door visuals in [`engines/harvester/runtime_entity.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/runtime_entity.cpp) and [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) after the previous native placement patch produced no obvious visual change. The new logs emit the live `pos`, `frame`, `rect`, and frame-header `offsets` for `SHER_EVIDDOOR`, `SERV_EVIDDOOR`, `SCHDOOR`, and `WAYLHERL` at spawn and in-place update time so the next repro can answer a concrete question: whether the patched centered-anim geometry is actually reaching the live entity. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Progress counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, confirmed in Ghidra that native room ANIM placement was still mismatched in ScummVM even after the same-room live-mutation work. `spawn_anim_entity_from_record` (`0x4bbe0`) seeds `z_extent = 3`, and `show_entity_visual` (`0x4c290`) recenters the current decoded bitmap on the anim record's `x/y` pivot while lowering the render-list depth anchor by half `z_extent`; the disassembly around `0x4c548..0x4c595` shows it subtracting half the current bitmap width and height from the stored screen anchor before `set_entity_screen_position`, without adding the frame header `x_offset/y_offset` to that centered anchor. ScummVM now matches that in [`engines/harvester/runtime_entity.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/runtime_entity.cpp) by making centered room anims ignore ABM frame offsets in `getDrawOrigin()` and by lowering scene-animation Z by half `z_extent` when spawning, while the same-room live mutation path in [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) now reapplies that native render Z when updating existing room anim entities in place. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Progress counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, widened ScummVM's live-mutation path in [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) so every same-room `mutatedRuntimeState` interaction now tries to patch the current room in place before falling back to `refreshCurrentScene(true)`. This closes the gap the sheriff-office `EVKEY -> EVD_DOORHS` log exposed: that path had no `GOFLIC`, so the earlier cutscene-only fix still rebuilt the whole `SHRFOFC` room after `SET_ANIM SHER_EVIDDOOR,T,T`. The helper now reuses the same native-style live update rules for room objects, regions, timers, and room anims across all same-room mutations, while still bailing out conservatively when NPC or monster records are involved because those live semantics are not yet audited. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Progress counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, removed the temporary broom-door probes from [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) and replaced the same-room cutscene pre-refresh with a narrower live mutation path. Native `dispatch_room_event_actions` case `SET_ANIM` at `0x61f55..0x62056` preserves already-visible anim entities, resets saved frame state only on hidden→visible transitions, and spawns or destroys the current-room entity in place without rebuilding the room; ScummVM now mirrors that same-room `GOFLIC` boundary more closely by patching current-room objects, regions, and anims in place before the FST instead of materializing and respawning the whole scene. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Progress counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, reran `BROOMKEY -> SCHL_DOOR_HS` in `SCHLHL` after the same-room `GOFLIC` timing patch and confirmed the new control flow now matches the native boundary we audited earlier. The fresh probe sequence shows `interaction-pre-cutscene-refresh`, a single pre-FST `refreshCurrentScene()` that materializes and spawns both `SCHDOOR` and `WAYLHERL`, then `before-cutscene` and `after-cutscene` with those same two live anims still present and no second room rebuild after `C132.FST`. The post-cutscene frame trace still shows `SCHDOOR` and `WAYLHERL` advancing together from frames `1..4`, but that now happens on the same live entities that were already present before the movie rather than on freshly respawned post-cutscene anims. This closes the refresh-timing mismatch: ScummVM now keeps same-room anim mutations live across the FST boundary instead of replaying them after the cutscene. Progress counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, updated ScummVM's same-room cutscene handling in [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) so `handleInteractionResult()` now applies `mutatedRuntimeState` to the live room before playing a same-room `GOFLIC`, instead of deferring a synthetic `refreshCurrentScene()` until after the movie returns. The new control flow refreshes immediately for same-room runtime mutations, keeps room-transition behavior unchanged, and leaves the targeted broom-door probes active long enough to verify that `WAYLHERL` is already live before `C132.FST` and that no second post-cutscene room rebuild occurs. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Progress counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, compared the native same-room `GOFLIC` path against the ScummVM broom-door traces in live `HARVEST.LE` disassembly. Three native facts now close the loop. First, `dispatch_room_event_actions` case `SET_ANIM` (`0x60ee0`) updates the `AnimRecord` and, if the anim belongs to the current room, immediately destroys or spawns the live render entity; for `USE_BROOMKEY`, that means native spawns `WAYLHERL` as soon as step `7` runs, before `GOFLIC`. Second, `play_fst_sequence` (`0x72550`) only pauses music, swaps to the `320x200` movie surface, runs `run_fst_sequence_player`, then restores the gameplay surface and palette; it does not call `room_setup()` or rebuild the room. Third, `run_harvester_main_loop` only transitions through `room_setup()` when `g_pending_room_name` is queued, while same-room object/use actions just return to the live loop after `dispatch_room_event_actions`. Combined with `room_setup()`'s existing frame-snapshot logic for live anims, that proves the current ScummVM behavior is wrong at the boundary: deferring the broom-door `SET_ANIM` mutations until after `C132.FST` suppresses native pre-cutscene `WAYLHERL` startup and then incorrectly restarts both `SCHDOOR` and `WAYLHERL` from frame `0` in a synthetic room rebuild. Progress counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, added targeted ScummVM broom-door refresh and frame-trace probes in [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) and replayed `BROOMKEY -> SCHL_DOOR_HS` in `SCHLHL`. The new logs confirmed three facts. First, deferred refresh timing is real but not the visual bug: before and after `GOFLIC GRAPHIC/FST/C132.FST`, the only live door anim is `SCHDOOR`; `SCHDOOR_R` is already gone, so there is no stale reverse-door overlap. Second, `materializeRoomState()` rebuilds the room with `SCHDOOR` and `WAYLHERL` both active and visible, and `refreshCurrentScene()` respawns both live entities. Third, the new post-refresh frame trace shows `SCHDOOR` and `WAYLHERL` both starting at frame `0` and advancing in lockstep over the next `20` centisecond samples (`SCHDOOR` frames `1..4`, `WAYLHERL` frames `1..4`), which means ScummVM is replaying two distinct post-cutscene room anims together rather than accidentally drawing mirrored forward/reverse door variants. The remaining unknown is native intent after the FST: whether both anims should restart from frame `0` on room rebuild or whether one should preserve a later frame / disabled state. Progress counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 25, 2026, audited the school hallway broom-closet door in both the decoded shipping [`HARVEST.SCR`](/Users/alex/Downloads/Harvester_1996/HARVEST/iso/Harvester/HARVEST.SCR) data and live `HARVEST.LE` disassembly. Confirmed the room defines two `ANIM` records over the same `1:\GRAPHIC\ROOMANIM\HALLDOOR.ABM` asset and pivot `(261,276,71)`: `SCHDOOR` is the forward/open variant, while `SCHDOOR_R` differs only by `backward = T`. Native `dispatch_room_event_actions` case `SET_ANIM` updates the anim record, destroys the current-room render entity when visibility goes false, resets the saved frame when a hidden anim becomes visible, and `spawn_anim_entity_from_record` starts reverse anims from `last_frame_index`; together that means the `USE_BROOMKEY` chain is intentionally clearing `SCHDOOR_R` before activating `SCHDOOR`, so the original game should never keep both door variants live at once. No rename or engine patch was justified from this pass, and progress counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, used the new cemetery debug logs to isolate the remaining Karin layering bug to stale render-list ordering rather than bad `z_extent` math. The captured CEM10 repro showed `spawnSceneActorEntity` inserting `KARIN_CEM10` into the render list at the pre-placement record Z (`16`) while `actor placement` later lowered her final Z to `15.00`; because ScummVM never re-sorted the entity after placement, the final `scene render order` still kept Karin ahead of `CEM10_OBJ5`, which is why the grave mask continued to draw over her even though the placement log already showed the corrected render Z. [`engines/harvester/runtime_entity.h`](/Users/alex/Workspace/scummvm/engines/harvester/runtime_entity.h) and [`engines/harvester/runtime_entity.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/runtime_entity.cpp) now expose `reinsertSceneEntity`, and [`engines/harvester/flow.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/flow.cpp) now re-sorts room NPCs, monsters, and newly spawned player actors immediately after placement. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Progress counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, added targeted room-scene debuggability for the cemetery Karin regression in [`engines/harvester/flow.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/flow.cpp), [`engines/harvester/runtime_entity.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/runtime_entity.cpp), and [`engines/harvester/runtime_entity.h`](/Users/alex/Workspace/scummvm/engines/harvester/runtime_entity.h). The latest CEM10 logs prove the current code is applying the actor-depth anchor correction (`KARIN_CEM10` materializes at record `z=16` but the spawned scene NPC lands at `z=15.00`), but they still do not reveal whether the remaining bug is wrong `z_extent`, wrong render ordering, or the wrong ambient frame/rect. The new logs therefore dump per-actor placement inputs and outputs (`source z`, computed `render_z`, `z_extent`, depth scale, current frame, frame metrics, final rect) and the final room render-list order after scene construction so the next repro can distinguish depth from placement/frame issues directly. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Progress counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, corrected ScummVM's room-actor render-depth anchor in [`engines/harvester/flow.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/flow.cpp) to match the native `spawn_npc_entity_from_record` and `spawn_monster_entity_from_record` paths. Native disassembly at `0x4ce00..0x4ce43` and `0x540b0..0x540f3` shows both actor spawners subtracting half `z_extent` from the record Z before `set_entity_screen_position`; ScummVM was leaving actors at the raw record Z instead, which put Wednesday cemetery Karin (`KARIN_CEM10`, `posZ=16`) one layer behind `CEM10_OBJ5` (`z=15`) in CEM10. The shared room-actor placement helper now applies that native `z_extent / 2` offset before depth scaling and insertion for room NPCs, monsters, and the player. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Progress counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, patched the ScummVM Karin cemetery dialogue routing so [`engines/harvester/npc/karin_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/karin_dialogue.cpp) now accepts both `KARIN` and `KARIN_CEM10`. This fixes the confirmed Wednesday grave-rescue regression where clicking Karin after `PLACE_KARIN_TIMER` fell through to `unsupported room NPC dialogue handler 'KARIN_CEM10'` instead of reusing the native Karin talk logic already implemented for `handle_talk_to_karin`. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Progress counts remain `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Johnson dialogue handler directly against the repaired live `HARVEST.LE` body at `handle_talk_to_johnson` (`0x27dd0..0x28802`) as a fresh pass rather than trusting the older port. The fresh native audit found one remaining source drift in [`engines/harvester/npc/johnson_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/johnson_dialogue.cpp): Johnson's Whaley-photo, casket-photo, dual-ledger, note/checkbook, and TV-deed item branches call the shared `get_set_discussed_whaley_herrill_photo`, `get_set_discussed_casket_photo_evidence`, `get_set_discussed_ledger_evidence`, `get_set_discussed_note_checkbook_evidence`, and `get_set_discussed_tv_deed_evidence` accessors in the live handler, so the port now mirrors that by writing `sharedState.discussedWhaleyHerrillPhoto`, `sharedState.discussedCasketPhotoEvidence`, `sharedState.discussedLedgerEvidence`, `sharedState.discussedNoteCheckbookEvidence`, and `sharedState.discussedTvDeedEvidence` alongside the existing startup-script evidence flags. Re-auditing the repaired intro/revisit flow, the `OPENING_MANHOLE_DAYTIME` clear, the `SCRATCHED_TUCKER` plus `BOLT_OF_CLOTH_TAKEN` re-entry, the `KARIN_KIDNAPED` shared-state write, and the recovered keyword loop confirmed the current C++ control flow already matches the saved native handler there, so no further source drift was found. The direct live callee set is already fully named (`get_set_shared_karin_kidnaped_dialogue_state`, `get_set_shared_dialogue_state_d2ebc`, the shared evidence accessors, and the standard dialogue helpers), so no additional `FUN_*` rename was justified. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Live progress remains `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, treated `JOHNSON` as a fresh `InspectCallTermination.java` repair in the live `HARVEST.LE` session. A fresh `verify JOHNSON` reported `handle_talk_to_johnson @ 0x27dd0` in the stale short-body state (`0x27dd0..0x287f9`, `657` address-count) with only `12` reachable dialogue calls and `12` stale `CALL_RETURN` gaps while both dialogue entry points initially still remained returning. The first `repair JOHNSON` rebuilt the handler cleanly in memory to `0x27dd0..0x28802` (`2539` address-count, `51` reachable dialogue calls, `gapCount=0`, `38` repair actions), but the first saved `verify JOHNSON` snapped straight back to the short body and flipped both dialogue entry points back to non-returning. An immediate second full `repair -> wait -> save_program -> verify` cycle fixed it: the retry rebuilt Johnson to the stable saved shape `0x27dd0..0x28802` (`2539` address-count) with `51` reachable dialogue calls, `gapCount=0`, `play_dialogue_line` and `thunk_play_dialogue_line` both returning, and `status=clean`. This confirms Johnson remains in the same post-save retry bucket as Loomis, Edna, Stephanie, Mr. Potts, Dad, and similar handlers. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change: `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Loomis dialogue handler directly against the repaired live `HARVEST.LE` body at `handle_talk_to_loomis` (`0x34f80..0x35826`) as a fresh pass rather than trusting the older port. The fresh native audit found one remaining source drift in [`engines/harvester/npc/loomis_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/loomis_dialogue.cpp): Loomis's note/checkbook-plus-TV-deed branch and the both-ledgers branch call the shared `get_set_discussed_note_checkbook_evidence` and `get_set_discussed_ledger_evidence` accessors in the live handler, so the port now mirrors that by writing `sharedState.discussedNoteCheckbookEvidence` and `sharedState.discussedLedgerEvidence` alongside the existing startup-script evidence flags. Re-auditing the repaired intro/revisit flow, keyword loop aliases, `INV_MAG` diner and non-diner branches, and the Whaley photo fallback confirmed the current C++ control flow already matches the saved native handler there, so no further source drift was found. The direct live callee set is already fully named (`play_c048_fst_sequence`, `get_current_story_day_index`, `get_set_shared_dialogue_state_d2f04`, `dispatch_action_tag_if_set`, and the standard dialogue helpers), so no additional `FUN_*` rename was justified. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Live progress remains `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, treated `LOOMIS` as a fresh `InspectCallTermination.java` repair in the live `HARVEST.LE` session. A fresh `verify LOOMIS` reported `handle_talk_to_loomis @ 0x34f80` in the stale short-body state (`0x34f80..0x35406`, `639` address-count) with only `9` reachable dialogue calls and `9` stale `CALL_RETURN` gaps while both dialogue entry points initially still remained returning. The first `repair LOOMIS` rebuilt the handler cleanly in memory to `0x34f80..0x35826` (`2071` address-count, `33` reachable dialogue calls, `gapCount=0`, `22` repair actions), but the first saved `verify LOOMIS` snapped straight back to the short body and flipped both dialogue entry points back to non-returning. An immediate second full `repair -> wait -> save_program -> verify` cycle fixed it: the retry rebuilt Loomis to the stable saved shape `0x34f80..0x35826` (`2071` address-count) with `33` reachable dialogue calls, `gapCount=0`, `play_dialogue_line` and `thunk_play_dialogue_line` both returning, and `status=clean`. This confirms Loomis remains in the same post-save retry bucket as Edna, Stephanie, Mr. Potts, Johnson, Dad, and similar handlers. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change: `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, re-audited the ScummVM Karin dialogue handler directly against the repaired live `HARVEST.LE` body at `handle_talk_to_karin` (`0x2ea90..0x2ed7f`) as a fresh pass rather than trusting the older port. The native body and the current source in [`engines/harvester/npc/karin_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/karin_dialogue.cpp) and [`engines/harvester/npc/karin_dialogue.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/karin_dialogue.h) already match: the `KARIN_FOUND_ALIVE + IN_CEM10` one-shot exchange, the pre-rescue no-item opener and `0x10dc` fallback, the two-stage photo-present branch with the shared `discussedWhaleyHerrillPhoto` / `discussedCasketPhotoEvidence` writes plus Karin portrait reset, the `INV_MAG` police-dispatch branch, and the three persisted room-state bits all align with the saved native handler. The direct live callee set is already fully named (`dispatch_action_tag_if_set`, `get_set_discussed_whaley_herrill_photo`, `get_set_discussed_casket_photo_evidence`, `set_dialogue_head_left_portrait`, and the standard dialogue helpers), so no additional `FUN_*` rename was justified. No engine source patch was needed, no build was run, and live progress is unchanged at `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, treated `KARIN` as a fresh `InspectCallTermination.java` repair in the live `HARVEST.LE` session. A fresh `verify KARIN` reported `handle_talk_to_karin @ 0x2ea90` in the stale short-body state (`0x2ea90..0x2ed77`, `378` address-count) with only `7` reachable dialogue calls and `7` stale `CALL_RETURN` gaps while both dialogue entry points already remained returning. Running `repair KARIN` rebuilt the handler in a single pass to `0x2ea90..0x2ed7f` (`752` address-count) with `21` reachable dialogue calls, `gapCount=0`, and `14` repair actions. After the normal settle and explicit `save_program`, a fresh `verify KARIN` still reported `status=clean`, so Karin is durably clean again in the saved database on the first fresh cycle. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change: `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Edna dialogue handler directly against the repaired live `HARVEST.LE` body at `handle_talk_to_edna` (`0x358a0..0x3667b`) as a fresh pass rather than trusting the older port. The fresh native audit found two remaining source drifts in [`engines/harvester/npc/edna_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/edna_dialogue.cpp): Edna's casket-photo, both-ledger, Whaley/Herrill-photo, note/checkbook, and TV-deed item branches call the shared `get_set_discussed_casket_photo_evidence`, `get_set_discussed_ledger_evidence`, `get_set_discussed_whaley_herrill_photo`, `get_set_discussed_note_checkbook_evidence`, and `get_set_discussed_tv_deed_evidence` accessors, so the port now mirrors that by writing `sharedState.discussedCasketPhotoEvidence`, `sharedState.discussedLedgerEvidence`, `sharedState.discussedWhaleyHerrillPhoto`, `sharedState.discussedNoteCheckbookEvidence`, and `sharedState.discussedTvDeedEvidence` alongside the existing startup-script flags; and the native `dialog.rsp[0xa6]` branch only gates on `KARIN_KIDNAPED` / `KARIN_FOUND_DEAD`, so `KARIN_FOUND_ALIVE` still follows `0x3ad9 -> play_c091_fst_sequence` instead of the `0x3ae3/0x3ae9` ladder. Re-auditing the repaired intro, day-5, purse, lodge-topic, `0xc0`, and goodbye paths confirmed the current C++ control flow already matches the saved handler there, so no further source drift was found. During the same Ghidra pass I renamed the two directly evidenced `play_fst_sequence` wrappers `FUN_00038990` and `FUN_00038ba0` to `play_c091_fst_sequence` and `play_c070_fst_sequence`, saved `HARVEST.LE`, and rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Live progress in the current database is `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, treated `EDNA` as a fresh `InspectCallTermination.java` repair in the live `HARVEST.LE` session. A fresh `verify EDNA` reported `handle_talk_to_edna @ 0x358a0` in the stale short-body state (`0x358a0..0x35d1f`, `712` address-count) with only `11` reachable dialogue calls and `11` stale `CALL_RETURN` gaps while both dialogue entry points initially still remained returning. The first `repair EDNA` rebuilt the handler cleanly in memory to `0x358a0..0x3667b` (`3510` address-count, `65` reachable dialogue calls, `gapCount=0`, `57` repair actions), but the first saved `verify EDNA` snapped straight back to the short body and flipped both dialogue entry points back to non-returning. An immediate second full `repair -> wait -> save_program -> verify` cycle fixed it: the retry rebuilt Edna to the stable saved shape `0x358a0..0x3667b` (`3505` address-count) with `64` reachable dialogue calls, `gapCount=0`, `play_dialogue_line` and `thunk_play_dialogue_line` both returning, and `status=clean`. This confirms Edna remains in the same post-save retry bucket as Stephanie, Mr. Potts, Johnson, Loomis, and similar handlers. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change: `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Mrs. Potts dialogue handler directly against the repaired live `HARVEST.LE` body at `handle_talk_to_mrs_potts` (`0x2ee70..0x2f7be`) as a fresh pass rather than trusting the older port. The fresh native audit found one remaining source drift in [`engines/harvester/npc/mrs_potts_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/mrs_potts_dialogue.cpp): Mrs. Potts' Whaley/Herrill-photo, casket-photo, both-ledger, note/checkbook, and TV-deed item branches call the shared `get_set_discussed_whaley_herrill_photo`, `get_set_discussed_casket_photo_evidence`, `get_set_discussed_ledger_evidence`, `get_set_discussed_note_checkbook_evidence`, and `get_set_discussed_tv_deed_evidence` accessors, so the port now mirrors that by writing `sharedState.discussedWhaleyHerrillPhoto`, `sharedState.discussedCasketPhotoEvidence`, `sharedState.discussedLedgerEvidence`, `sharedState.discussedNoteCheckbookEvidence`, and `sharedState.discussedTvDeedEvidence` alongside the existing startup-script flags. Re-auditing the repaired native return-visit flow, `GOT_REMAINS_FOR_LODGE`, `TAKEN_INVITE_TO_SERGEANT`, `KARIN_KIDNAPED`, `DINER_BURNED`, and the late keyword loop confirmed the current C++ control flow already matches the saved handler there, so no further source drift was found. The only direct unnamed callee surfaced during this pass, `FUN_0002fc79 @ 0x2fc79`, is just a tiny zero-return stub used as a dead tail after the `0x294d` item bark and did not meet the rename bar; the meaningful direct helpers (`play_c040_fst`, `get_set_discussed_mrs_potts_tuesday_night_alibi`, `get_set_shared_dialogue_state_d2eb8`, and the other shared-state accessors) were already stably named. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Live progress remains `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, treated `MRS_POTTS` as a fresh `InspectCallTermination.java` repair in the live `HARVEST.LE` session. A fresh `verify MRS_POTTS` reported `handle_talk_to_mrs_potts @ 0x2ee70` in the stale short-body state (`0x2ee70..0x2f7be`, `1156` address-count) with only `19` reachable dialogue calls and `19` stale `CALL_RETURN` gaps while both dialogue entry points already remained returning. Running `repair MRS_POTTS` rebuilt the handler in two passes to a clean in-memory `0x2ee70..0x2fc81` body (`2392` address-count) with `54` reachable dialogue calls, `gapCount=0`, and `40` repair actions. After the normal settle and explicit `save_program`, a fresh `verify MRS_POTTS` still reported `status=clean`; the saved database now presents the stable clean body as `0x2ee70..0x2f7be` (`2383` address-count) with the same `54` dialogue calls and `gapCount=0`, so Mrs. Potts is durably clean again on the first fresh cycle. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change: `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Mr. Potts dialogue handler directly against the repaired live `HARVEST.LE` body at `handle_talk_to_mr_potts` (`0x26000..0x27d07`) as a fresh pass rather than trusting the older port. The fresh native audit found one remaining source drift in [`engines/harvester/npc/mr_potts_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/mr_potts_dialogue.cpp): Mr. Potts' casket-photo, Whaley/Herrill-photo, note/checkbook, and TV-deed item branches call the shared `get_set_discussed_casket_photo_evidence`, `get_set_discussed_whaley_herrill_photo`, `get_set_discussed_note_checkbook_evidence`, and `get_set_discussed_tv_deed_evidence` accessors, so the port now mirrors that by writing `sharedState.discussedCasketPhotoEvidence`, `sharedState.discussedWhaleyHerrillPhoto`, `sharedState.discussedNoteCheckbookEvidence`, and `sharedState.discussedTvDeedEvidence` alongside the existing startup-script flags. Re-auditing the auxiliary intro/revisit day gating, `HARVEST_BLADE`, `STEPHANIE_DEAD_IN_HALL_JUST_OUTSIDE`, `POTTSDAM_DIGGING`, `POTTSDAM_DIGGING_KARIN`, `KARIN_KIDNAPED`, `KARIN_FOUND_DEAD`, the accusation menus, and the late keyword loop confirmed the current C++ control flow already matches the saved native handler there, so no further source drift was found. The direct live callee set is now fully named (`play_c001b_fst_and_mark_steph_midgame_played`, `play_c016a_fst`, and the shared-state accessors), so no additional `FUN_*` rename was justified during this pass. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Live progress remains `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, treated `MR_POTTS` as a fresh `InspectCallTermination.java` repair in the live `HARVEST.LE` session. A fresh `verify MR_POTTS` reported `handle_talk_to_mr_potts @ 0x26000` in the stale short-body state (`0x26000..0x26915`, `823` address-count) with only `15` reachable dialogue calls and `15` stale `CALL_RETURN` gaps while both dialogue entry points already remained returning. Running `repair MR_POTTS` rebuilt the handler in three passes to the full saved body `0x26000..0x27d07` (`6853` address-count) with `152` reachable dialogue calls, `gapCount=0`, and `81` repair actions. After the normal settle and explicit `save_program`, a fresh `verify MR_POTTS` still reported `status=clean`, so Mr. Potts is durably clean again in the saved database on the first fresh cycle. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change: `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Mom dialogue handler directly against the repaired live `HARVEST.LE` body at `handle_talk_to_mom` (`0x31140..0x33523`) as a fresh pass rather than trusting the older port. The native item-use branches proved one remaining source drift in [`engines/harvester/npc/mom_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/mom_dialogue.cpp): Mom's note/checkbook, both-ledger, casket-photo, Whaley/Herrill-photo, and TV-deed evidence branches call the shared `get_set_discussed_note_checkbook_evidence`, `get_set_discussed_ledger_evidence`, `get_set_discussed_casket_photo_evidence`, `get_set_discussed_whaley_herrill_photo`, and `get_set_discussed_tv_deed_evidence` accessors, so the port now mirrors that by writing `sharedState.discussedNoteCheckbookEvidence`, `sharedState.discussedLedgerEvidence`, `sharedState.discussedCasketPhotoEvidence`, `sharedState.discussedWhaleyHerrillPhoto`, and `sharedState.discussedTvDeedEvidence` alongside the existing startup-script flags. Re-auditing the previously suspicious `JIMMY_ATTACKED`, `POTTSDAM`, `MEAT PLANT`, lodge-topic, and `0x26c6` fallback paths confirmed the current C++ control flow already matches the saved native handler there, so no further source drift was found. During the same Ghidra pass I renamed `FUN_00038270` to `get_set_dialogue_state_d2ea4` and `FUN_00038900` to `play_c010_fst`, saved `HARVEST.LE`, and rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Live progress remains `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, treated `MOM` as a fresh `InspectCallTermination.java` repair in the live `HARVEST.LE` session. A fresh `verify MOM` reported `handle_talk_to_mom @ 0x31140` in the stale short-body state (`0x31140..0x33523`, `3309` address-count) with only `58` reachable dialogue calls, `58` stale `CALL_RETURN` gaps, and both dialogue entry points flipped back to non-returning. The first `repair MOM` rebuilt the handler cleanly in memory to `0x31140..0x33523` (`8778` address-count, `251` reachable dialogue calls, `gapCount=0`, `116` repair actions), but the first saved `verify MOM` snapped straight back to the stale short body. An immediate second full `repair -> wait -> save_program -> verify` cycle rebuilt the same full body again (`117` repair actions on the retry) and this time the saved verify held with `play_dialogue_line` and `thunk_play_dialogue_line` both returning plus `status=clean`, so Mom is durably clean again in the current saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change: `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Jimmy dialogue handler directly against the saved live `HARVEST.LE` body at `handle_talk_to_jimmy` (`0x28880..0x28ec1`) as a fresh pass rather than trusting the older port. The recovered native body confirmed two source drifts in [`engines/harvester/npc/jimmy_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/jimmy_dialogue.cpp): Jimmy's `PHOTO_OF_WHALEY_HERRILL`, both-ledger / corpse-photo, and note/checkbook item branches call `get_set_discussed_whaley_herrill_photo`, `get_set_discussed_ledger_evidence` / `get_set_discussed_casket_photo_evidence`, and `get_set_discussed_note_checkbook_evidence`, so the port now mirrors that by writing `sharedState.discussedWhaleyHerrillPhoto`, `sharedState.discussedLedgerEvidence` / `sharedState.discussedCasketPhotoEvidence`, and `sharedState.discussedNoteCheckbookEvidence` instead of the non-native startup-script evidence flags; and the native `NEWSPAPER` handoff restores the paper to `RAH` before dispatching `ACTV_HOUSE_EXIT`, so the port now returns `NEWSPAPER` to room state before running the action tag. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Jimmy's direct Ghidra callees were re-audited during the pass, but none remained as `FUN_*` or met the rename bar for a new symbol change. Live progress remains `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, treated `JIMMY` as a fresh `InspectCallTermination.java` repair in the live `HARVEST.LE` session. A fresh `verify JIMMY` reported `handle_talk_to_jimmy @ 0x28880` in the stale short-body state (`0x28880..0x28eb9`, `807` address-count) with only `13` reachable dialogue calls and `13` stale `CALL_RETURN` gaps. Running `repair JIMMY` rebuilt the handler in one pass to the full saved body `0x28880..0x28ec1` (`1602` address-count) with `39` reachable dialogue calls, `gapCount=0`, and `27` repair actions. After a settle and explicit `save_program`, a fresh `verify JIMMY` still reported `status=clean` with both dialogue entry points returning, so Jimmy is durably clean again in the saved database on the first fresh cycle. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change: `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Hank dialogue handler directly against the saved live `HARVEST.LE` body at `handle_talk_to_hank` (`0x33a30..0x34bcb`) as a fresh pass rather than trusting the older port. The recovered native body confirmed three source drifts in [`engines/harvester/npc/hank_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/hank_dialogue.cpp): the same-day and later-day branch driven by `g_hank_rangshot_branch_pending` / `g_hank_rangshot_branch_active` plays `RANGSHOT.FST` via `play_rangshot_fst_sequence`, not `C021.FST`, so the port now uses `GRAPHIC/FST/RANGSHOT.FST` for those two followup paths while keeping `C021.FST` only on the separate TV topic; Hank's `CASKET_PHOTO` / `CASKET_PHOTOCOPY` and `PHOTO_OF_WHALEY_HERRILL` item branches call `get_set_discussed_casket_photo_evidence` and `get_set_discussed_whaley_herrill_photo`, so the port now mirrors that by writing `sharedState.discussedCasketPhotoEvidence` and `sharedState.discussedWhaleyHerrillPhoto` alongside the existing startup-script flags; and the native unknown-keyword fallback plays `0xa32` then loops back to the keyword menu instead of forcing the `0x8dc` goodbye line, so the port now continues after `0xa32` until the actual `0xf2` exit topic is chosen. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Hank's direct Ghidra callees were re-audited during the pass, including `play_c021_fst_sequence` and `play_rangshot_fst_sequence`, but none remained as `FUN_*` or met the rename bar for a new symbol change. Live progress remains `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, treated `HANK` as a fresh `InspectCallTermination.java` repair in the live `HARVEST.LE` session. A fresh `verify HANK` reported `handle_talk_to_hank @ 0x33a30` in the stale short-body state (`0x33a30..0x34bc2`, `1542` address-count) with only `28` reachable dialogue calls and `28` stale `CALL_RETURN` gaps. Running `repair HANK` rebuilt the handler in two passes to the full saved body `0x33a30..0x34bcb` (`4508` address-count) with `131` reachable dialogue calls, `gapCount=0`, and `58` repair actions. After a settle and explicit `save_program`, a fresh `verify HANK` still reported `status=clean` with both dialogue entry points returning, so Hank is durably clean again in the saved database on the first fresh cycle. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change: `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Dwayne dialogue handler directly against the saved live `HARVEST.LE` body at `handle_talk_to_dwayne` (`0x3a310..0x3c385`) as a fresh pass rather than trusting the older port. The recovered native body confirmed four source drifts in [`engines/harvester/npc/dwayne_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/dwayne_dialogue.cpp): Dwayne's note/checkbook and TV-deed item branches do not write the shared `get_set_discussed_note_checkbook_evidence` or `get_set_discussed_tv_deed_evidence` accessors, so the port now stops pre-arming `sharedState.discussedNoteCheckbookEvidence` and `sharedState.discussedTvDeedEvidence` from those item presentations; the normal `0x3245..0x3252` introduction, the sheriff-in-diner intro `0x3482..0x349b`, and the diner revisit `0x34ac` all return to the shared followup ladder without same-visit `0x34d3`, so the C++ control flow now suppresses that erroneous fallthrough; the `KARIN_FOUND_DEAD` no-purse response-1 path plays `0x379d` and returns immediately instead of continuing into the later absent-NPC/topic sweep; and the arrest cleanup always hides `GOOJF_CARD` in `RAH`, while the completed Karin-alive followup bit is only raised after the successful `0x3780/0x3786/0x378a` sequence. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Direct Dwayne callees and the additional helper at `0x38240` (`get_set_boyle_gascan_application_state`) were re-audited in Ghidra during the pass, but none remained as `FUN_*` or met the rename bar for a new symbol change. Live progress remains `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, treated `DWAYNE` as a fresh `InspectCallTermination.java` repair in the live `HARVEST.LE` session. A fresh `verify DWAYNE` reported `handle_talk_to_dwayne @ 0x3a310` in the stale short-body state (`0x3a310..0x3c385`, `3375` address-count) with only `59` reachable dialogue calls and `59` stale `CALL_RETURN` gaps. Running `repair DWAYNE` rebuilt the handler in two passes to the full saved body `0x3a310..0x3c385` (`8219` address-count) with `186` reachable dialogue calls, `gapCount=0`, and `118` repair actions. After a settle and explicit `save_program`, a fresh `verify DWAYNE` still reported `status=clean` with both dialogue entry points returning, so Dwayne is durably clean again in the saved database on the first fresh cycle. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change: `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Stephanie dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_stephanie` (`0x36710..0x37f5d`) as a fresh pass rather than reusing the prior port. The recovered native body confirmed three engine mismatches in [`engines/harvester/npc/stephanie_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/stephanie_dialogue.cpp): the note/checkbook, casket-photo, and dual-ledger item branches write shared dialogue state through `get_set_discussed_note_checkbook_evidence`, `get_set_discussed_casket_photo_evidence`, and `get_set_discussed_ledger_evidence`, so the port now mirrors that by writing `sharedState.discussedNoteCheckbookEvidence`, `sharedState.discussedCasketPhotoEvidence`, and `sharedState.discussedLedgerEvidence` alongside the existing startup-script flags; the native one-shot ladder for `SCRATCHED_TUCKER`, `BOLT_OF_CLOTH_TAKEN`, `BARBER_POLE_STOLEN`, and `DINER_BURNED` falls through into later event checks instead of returning early, so the early C++ returns were removed to match the live control flow; and the keyword branches for `dialog.rsp[0x2e5/0x2e6]` and `dialog.rsp[0x2ed]` continue through the shared `0x4441` and `0x44a2..0x44af` tails with topic-buffer rewrites instead of ending immediately, while Stephanie's `dialog.rsp[0x2f3]` lodge topic only reads the shared lodge-discussion state and does not set it locally. Rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Direct Stephanie callees were re-audited in Ghidra (`play_c022a_fst`, `play_c022b_fst`, `play_c023a_fst`, `play_c025a_fst`, `play_c025b_fst`, `play_c007_fst`, and the shared-state accessors), but none remained as `FUN_*` or met the rename bar for a new symbol change. Live progress remains `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, treated `STEPHANIE` as a fresh `InspectCallTermination.java` repair in the live `HARVEST.LE` session. A fresh `verify STEPHANIE` reported `handle_talk_to_stephanie @ 0x36710` as the short `0x36710..0x36cd8` body (`526` address-count) with `9` reachable dialogue calls and `9` stale `CALL_RETURN` gaps. Running `repair STEPHANIE` rebuilt the handler in two passes to `0x36710..0x37f5d` (`6166` address-count, `195` dialogue calls, `gapCount=0`, `29` repair actions); after a settle and explicit `save_program`, a fresh `verify STEPHANIE` still reported `status=clean` with both dialogue entry points returning. A targeted raw-byte audit of the recovered body then found two still-misdecoded in-body call sites at `0x3765c` and `0x3778d`; clearing and re-disassembling those spans restored the missing `CALL 0x7a690` instructions for the optional `0x4970` spyhole line and the shared `0x49ba` Karin-alive call site. Rechecking the repaired bytes confirmed that the live native handler still matches the current ScummVM Stephanie flow in [`engines/harvester/npc/stephanie_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/stephanie_dialogue.cpp), including the `0x45d8/0x45e2` first-followup tail, `0x4709` portrait variant, the `0x4984/0x4989/0x498f` Mrs. Potts alibi followup, and the gated `0x49c1 -> 0x49ea -> 0x49ef` Karin-alive tail, so no engine source patch was needed. Live progress is now `920` total functions, `808` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Whaley dialogue handler directly against the saved live `HARVEST.LE` body at `handle_talk_to_whaley` (`0x23ec0..0x249ca`). The earlier port in [`engines/harvester/npc/whaley_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/whaley_dialogue.cpp) already matched most of the recovered control flow, but three native mismatches remained: the casket-photo, dual-ledger, and note/checkbook item branches only wrote startup-script flags instead of the native shared `get_set_discussed_*` evidence state; the later `0x14dd[2]` Whaley/Herrill-photo follow-up was gated on the startup flag instead of shared `get_set_discussed_whaley_herrill_photo`; and the `KARIN_KIDNAPED` no-item branch collapsed `0x13e5..0x13ff` into one sequence instead of setting the shared kidnapped-dialogue state between `0x13e5` and `0x13ee`. Patched the handler to mirror those native state transitions, renamed the directly encountered cutscene helper at `0x38930` from `FUN_00038930` to `play_c001b_fst_and_mark_steph_midgame_played`, fixed its prototype to `void play_c001b_fst_and_mark_steph_midgame_played(void)`, updated [`ARCHITECTURE.md`](/Users/alex/Workspace/scummvm/ARCHITECTURE.md) with the confirmed helper semantics, saved `HARVEST.LE`, and rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully. Live progress is now `914` total functions, `802` named/documented, and `112` remaining `FUN_*`.
- On March 24, 2026, reran `repair WHALEY` with the current `InspectCallTermination.java` verifier in the live `HARVEST.LE` session. Whaley showed the same first-save snapback pattern seen on the broader retry bucket: the first `repair -> wait -> save_program -> verify` cycle rebuilt `handle_talk_to_whaley @ 0x23ec0` from the short `0x23ec0..0x244f5` body (`438` address-count, `7` dialogue calls, `7` stale `CALL_RETURN` gaps) to a clean in-memory `0x23ec0..0x249ca` body (`2808` address-count, `78` dialogue calls, `gapCount=0`), but the first saved verify fell back to the short body and flipped both dialogue entry points back to non-returning. An immediate second full cycle fixed it: the second `repair -> wait -> save_program -> verify` left Whaley durably clean again at `0x23ec0..0x249ca` with `gapCount=0`, `play_dialogue_line` and `thunk_play_dialogue_line` both returning, and `status=clean`. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 24, 2026, reimplemented the ScummVM Wasp Woman dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_wasp_woman` (`0x2fde0..0x3030b`). The old port in [`engines/harvester/npc/wasp_woman_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/wasp_woman_dialogue.cpp) missed the unconditional `C027B.FST` insert after topic `0x305`, omitted the full topic-`0x30c` branch `0x4c85[0] -> 0x4c8b[1] -> 0x4c8f[2]` plus the `dialog.rsp[0x30d]` buffer rewrite, ended the conversation after the `0x4c9a[0]` fallback instead of looping, and only set startup-script evidence flags instead of the native shared `get_set_discussed_*` state. Patched the handler to mirror the recovered branch structure, topic-buffer rewrites, and shared evidence writes; renamed the directly encountered FST wrapper at `0x38b00` from `FUN_00038b00` to `play_c027b_fst`; fixed its prototype to `void play_c027b_fst(void)`; saved `HARVEST.LE`; and rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with `make -j4 scummvm`. Live progress is now `933` total functions, `814` named/documented, and `119` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Vet dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_vet` (`0x2fd20..0x2fdd5`). The old stub in [`engines/harvester/npc/vet_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/vet_dialogue.cpp) only cleared `VET_INTERRUPT` and played `0x6d9`, while the recovered native handler is the full interrupt-gated exchange `0x6d9[0] -> PC 0x447[0] -> 0x6e1[2]`, response menu `dialog.rsp[0x2ff]`, response `1 -> 0x6ec[2]`, response `2 -> 0x6f0[0]`, and shared exit `0x6f4[2]`, with `VET_INTERRUPT` cleared before the dialogue and again on the shared tail. Patched [`engines/harvester/npc/vet_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/vet_dialogue.cpp), rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully from the configured repo-root `make -j4 scummvm` build, and audited the directly encountered native helpers (`get_flag_value`, `set_flag_value`, `load_dialogue_response_line`, `run_dialogue_response_menu`, and `play_dialogue_line`) without finding any `FUN_*` symbol that met the rename bar. Live progress remains `931` total functions, `812` named/documented, and `119` remaining `FUN_*`.
- On March 24, 2026, reran the full durable `repair WASP_WOMAN -> wait -> save_program -> verify WASP_WOMAN` cycle against the live `HARVEST.LE` session. `handle_talk_to_wasp_woman @ 0x2fde0` started as a truncated `0x2fde0..0x2ffd8` body (`291` address-count) with `4` reachable dialogue calls and `4` stale `CALL_RETURN` gaps. A single repair pass rebuilt it cleanly to `0x2fde0..0x3030b` (`1314` address-count) with `28` reachable dialogue calls, `gapCount=0`, and `8` repair actions. After the normal settle and explicit save, a fresh `verify WASP_WOMAN` still reported `status=clean` with both dialogue entry points returning, so Wasp Woman is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 24, 2026, reran the full durable `repair VET -> wait -> save_program -> verify VET` cycle against the live `HARVEST.LE` session. `handle_talk_to_vet @ 0x2fd20` started as a truncated `0x2fd20..0x2fdd5` body (`54` address-count) with `1` reachable dialogue call and `1` stale `CALL_RETURN` gap. A single repair pass rebuilt it cleanly to `0x2fd20..0x2fdd5` (`182` address-count) with `5` reachable dialogue calls, `gapCount=0`, and `3` repair actions. After the normal settle and explicit save, a fresh `verify VET` still reported `status=clean` with both dialogue entry points returning, so Vet is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 24, 2026, re-audited the live `HARVEST.LE` VALET handler after the saved repair and confirmed the ScummVM flow at [`engines/harvester/npc/valet_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/valet_dialogue.cpp) is exactly the native `handle_talk_to_valet @ 0x2eda0`: intro lines `0xf2c[1] -> PC 0xf31[0] -> 0xf35[0] -> PC 0xf39[0]`, the `GRAPHIC/FST/MIDGET02.FST` playback, response menu `0x2fe`, optional reply `1 -> 0xf4a[0]` or `2 -> 0xf50[2]`, and the unconditional exit line `0xf56[0]`. I rewrote the handler around explicit native constants so that control flow matches the recovered body one-for-one, renamed the directly encountered wrapper at `0x38b30` from `FUN_00038b30` to `play_valet_midget02_fst_sequence`, fixed its prototype to `void play_valet_midget02_fst_sequence(void)`, and saved `HARVEST.LE`. The live database now reports `931` total functions, `812` named/documented, and `119` remaining `FUN_*`.
- On March 24, 2026, reran the full durable `repair VALET -> wait -> save_program -> verify VALET` cycle against the live `HARVEST.LE` session. `handle_talk_to_valet @ 0x2eda0` started as a truncated `0x2eda0..0x2edb5` body (`22` address-count) with `1` reachable dialogue call and `1` stale `CALL_RETURN` gap. A single repair pass rebuilt it cleanly to `0x2eda0..0x2ee3b` (`156` address-count) with `6` reachable dialogue calls, `gapCount=0`, and `3` repair actions. After the normal settle and explicit save, a fresh `verify VALET` still reported `status=clean` with both dialogue entry points returning, so Valet is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 24, 2026, re-audited the live `HARVEST.LE` Swell handler after the earlier port and confirmed two remaining ScummVM mismatches in [`engines/harvester/npc/swell_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/swell_dialogue.cpp): the native evidence branches call `get_set_discussed_whaley_herrill_photo`, `get_set_discussed_casket_photo_evidence`, `get_set_discussed_ledger_evidence`, and `get_set_discussed_note_checkbook_evidence`, so the port now also writes the matching shared dialogue-state ints alongside the existing startup-script flags; and the keyword loop exits only on native response line `0x21e`, so the extra `runtime.genericByeTopic()` shortcut was removed to match the recovered `strcmp_ascii_case_insensitive` branch exactly. During the same pass I renamed the proven SWELL room-state reset helper at `0x29700` to `reset_swell_room_dialogue_state` and relabeled its backing globals as `g_swell_intro_pending`, `g_swell_steph_midgame_shown`, `g_swell_barber_pole_stolen_shown`, `g_swell_bolt_of_cloth_taken_shown`, `g_swell_burned_tv_station_shown`, and `g_swell_karin_kidnaped_shown`, then rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully and saved `HARVEST.LE`. No additional `FUN_*` rename was justified, so live progress remains `930` total functions, `811` named/documented, and `119` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Swell dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_swell` (`0x29110..0x296d4`) and its reset helper `reset_serialized_talk_state_block_d2c30` (`0x29700..0x2972f`). The old stub in [`engines/harvester/npc/swell_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/swell_dialogue.cpp) only handled the `0xf61` first-talk bark, the `0xfb3` revisit bark, and one-line evidence replies, but the recovered native handler is a much wider flow: item use now includes the full ledger exchange `0xfd4 -> PC 0xfd9 -> 0xfde` and the correct portrait variants on `0xfc6` and `0xfe6`; the first no-item talk now plays the full `0xf61 -> PC 0xf65 -> 0xf69 -> PC 0x3d50 -> 0xf72 -> 0xf7f` intro and seeds keyword buffer `dialog.rsp[0x21d]`; revisit logic now preserves the native one-shot local gates for `STEPH_MIDGAME_PLAYED`, `BARBER_POLE_STOLEN`, `BOLT_OF_CLOTH_TAKEN`, `BURNED_TV_STATION`, and `KARIN_KIDNAPED`; and the keyword loop now matches the recovered topic rewrites and responses for `0x21e` through `0x229`, including the `0xf88`, `0xf95`, `0xfa0`, `0xfaa`, and `0xfba` branches. Patched [`engines/harvester/npc/swell_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/swell_dialogue.cpp) and [`engines/harvester/npc/swell_dialogue.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/swell_dialogue.h), bumped the Harvester save blob version to `13` in [`engines/harvester/saveload.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/saveload.cpp) so the five new Swell-local one-shot bits serialize compatibly, and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully. The directly encountered native helpers (`get_flag_value`, `get_set_discussed_whaley_herrill_photo`, `get_set_discussed_casket_photo_evidence`, `get_set_discussed_ledger_evidence`, `get_set_discussed_note_checkbook_evidence`, `load_dialogue_response_line`, `run_dialogue_keyword_menu`, and `reset_serialized_talk_state_block_d2c30`) were already stably named, so no additional `FUN_*` rename was justified. Live progress remains `930` total functions, `811` named/documented, and `119` remaining `FUN_*`.
- On March 24, 2026, reran the full durable `repair SWELL -> wait -> save_program -> verify SWELL` cycle against the live `HARVEST.LE` session. `handle_talk_to_swell @ 0x29110` started as a truncated `0x29110..0x29378` body (`431` address-count) with `6` reachable dialogue calls and `6` stale `CALL_RETURN` gaps. A single repair pass rebuilt it cleanly to `0x29110..0x296d4` (`1477` address-count) with `26` reachable dialogue calls, `gapCount=0`, and `12` repair actions. After the normal settle and explicit save, a fresh `verify SWELL` still reported `status=clean` with both dialogue entry points returning, so Swell is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 24, 2026, rechecked the ScummVM PTA Mom dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_pta_mom` (`0x34e30..0x34f79`). The existing port in [`engines/harvester/npc/pta_mom_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/pta_mom_dialogue.cpp) already matched the native `PTA_RESPOND_TO_TV` early return, the three-opener `0x31ee` / `0x31f2` / `0x31f6` random bark, `dialog.rsp[0x297]`, response `2 -> 0x320f[0] -> 0x3213[0]`, and response `3 -> 0x3217[0] -> 0x321c[0] -> PC 0x3221[0] -> 0x3226[0]`, but the response-`1` tail was one portrait variant off. Native disassembly at `0x34ee0..0x34f70` confirms `0x3204` uses `PTA_MOM1` head variant `2` while `0x320b` falls through the shared tail with head variant `0`, so I patched the handler and corrected the matching architecture note. The directly encountered native callees (`get_flag_value`, `set_flag_value`, `next_random_value`, `load_dialogue_response_line`, `run_dialogue_response_menu`, and `play_dialogue_line`) are all already stably named, so no additional `FUN_*` rename was justified. Live progress remains `930` total functions, `811` named/documented, and `119` remaining `FUN_*`.

- On March 24, 2026, reimplemented the ScummVM Priest dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_priest` (`0x29730..0x29911`). The old stub in [`engines/harvester/npc/priest_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/priest_dialogue.cpp) only set `PC_TALKED_TO_PRIEST` and played `0xcb2[0]`, while the recovered native handler is a one-shot intro `0xcb2[0]` into response menus `0x27a`, `0x27b`, `0x27c`, `0x27d`, and `0x27e`, with immediate follower-monsterfy exits after `0xcc4[2]` on responses `2`, `2`, `1`, and `2` respectively; deeper branch lines `0xcbf[1]`, `0xccf[1]`, `0xce5[1]`, and `0xcf2[1]`; and a final split where response `4` plays `0xd0e[1]`, clears `PRIEST_FLAG`, and dispatches `RELIG_EXITS_A`, while every other final response plays `0xd13[3]`, sets `PRIEST_FLAG`, and dispatches `FOLOWR_MNSTFY_A`. Patched [`engines/harvester/npc/priest_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/priest_dialogue.cpp) to mirror that control flow, audited the directly encountered Ghidra callees without finding any `FUN_*` symbol that met the rename bar, and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully. Live progress remains `930` total functions, `811` named/documented, and `119` remaining `FUN_*`.

- On March 24, 2026, reran the full durable `repair PTA_MOM -> wait -> save_program -> verify PTA_MOM` cycle against the live `HARVEST.LE` session. `handle_talk_to_pta_mom @ 0x34e30` started as a truncated `0x34e30..0x34f79` body (`222` address-count) with `7` reachable dialogue calls and `7` stale `CALL_RETURN` gaps. The rebuild needed two in-memory passes and finished cleanly at `0x34e30..0x34f79` (`330` address-count) with `11` reachable dialogue calls, `gapCount=0`, and `17` repair actions. After the normal settle and explicit save, a fresh `verify PTA_MOM` still reported `status=clean` with both dialogue entry points returning, so PTA Mom is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 24, 2026, reran the full durable `repair PRIEST -> wait -> save_program -> verify PRIEST` cycle against the live `HARVEST.LE` session. `handle_talk_to_priest @ 0x29730` started as a truncated `0x29730..0x29911` body (`57` address-count) with `1` reachable dialogue call and `1` stale `CALL_RETURN` gap. A single repair pass rebuilt it cleanly to `0x29730..0x29911` (`482` address-count) with `11` reachable dialogue calls, `gapCount=0`, and `3` repair actions. After the normal settle and explicit save, a fresh `verify PRIEST` still reported `status=clean` with both dialogue entry points returning, so Priest is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 24, 2026, reimplemented the ScummVM Memb Dir dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_memb_dir` (`0x396d0..0x39781`). The old stub in [`engines/harvester/npc/memb_dir_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/memb_dir_dialogue.cpp) only played `0x2b7[1]` before `PC_KILLED_KEWPIE` and a one-shot `0x2d5[1]` after it, while the recovered native handler is the pre-kill bark `0x2b7[1]` into response menu `0xfe` with response `1 -> 0x2c2[0]`, response `2 -> 0x2cb[0]`, and dismissed/other responses returning silently; plus the post-kill one-shot `0x2d5[1]` gated by local state `d2f64`, followed by `TOOK_THE_DOLL -> 0x2e3[0]` or `0x2ea[0]`. Patched [`engines/harvester/npc/memb_dir_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/memb_dir_dialogue.cpp) to mirror that exact control flow, audited the directly encountered Ghidra helpers and reset routine without finding any `FUN_*` symbol that met the rename bar, and kept live progress unchanged at `930` total functions, `811` named/documented, and `119` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Mcknight dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_mcknight` (`0x2c770..0x2caf3`). The old stub in [`engines/harvester/npc/mcknight_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/mcknight_dialogue.cpp) only played a single `0x1587[2]` no-item bark, simple one-line evidence responses, and never latched the `LOOK_SAFE_1ST` one-shot gate, while the recovered native handler is `LOOK_SAFE_2ND -> 0x15d0[2]`, one-shot `LOOK_SAFE_1ST -> 0x15c9[2]`, first empty-hand talk `0x1587[2] -> PC 0x158b[0] -> 0x158f[0] -> PC 0x1594[0] -> 0x1598[2] -> PC 0x159d[0]` plus shared state `d2f04`, default empty-hand bark `0x15e4`, corpse-photo follow-up `0x15f2` plus either `PC 0x15f8 -> 0x15fd` or `PC 0x1605 -> 0x1609` depending on shared state `d2ebc`, ledgers `0x1611[1]`, and note/checkbook evidence `0x161b[1] -> PC 0x1621[0] -> 0x1626[3] -> PC 0x162b[4] -> 0x1631[0]`, with wrong items still falling back to `0x15de`. Patched [`engines/harvester/npc/mcknight_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/mcknight_dialogue.cpp) to mirror that exact control flow, audited the directly encountered Ghidra callees without finding any `FUN_*` symbol that met the rename bar, and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully. Live progress remains `930` total functions, `811` named/documented, and `119` remaining `FUN_*`.
- On March 24, 2026, reran the full durable `repair MEMB_DIR -> wait -> save_program -> verify MEMB_DIR` cycle against the live `HARVEST.LE` session. `handle_talk_to_memb_dir @ 0x396d0` started as a truncated `0x396d0..0x39781` body (`76` address-count) with `2` reachable dialogue calls and `2` stale `CALL_RETURN` gaps. The rebuild needed two in-memory passes and finished cleanly at `0x396d0..0x39781` (`178` address-count) with `4` reachable dialogue calls, `gapCount=0`, and `7` repair actions. After the normal settle and explicit save, a fresh `verify MEMB_DIR` still reported `status=clean` with both dialogue entry points returning, so Memb Dir is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 24, 2026, refactored [`ARCHITECTURE.md`](/Users/alex/Workspace/scummvm/ARCHITECTURE.md) into a tighter subsystem-first layout without changing any confirmed findings. The file now opens with a concise summary plus an at-a-glance index, and the former flat heading sprawl is grouped under `Runtime And Gameplay`, `Interface And Input`, `Data Types And Helper Layer`, `UI And Inventory Details`, and `Open Questions`. Progress counts remain `930` total functions, `811` named/documented, and `119` remaining `FUN_*` because this was documentation cleanup only.
- On March 24, 2026, reran the full durable `repair MCKNIGHT -> wait -> save_program -> verify MCKNIGHT` cycle against the live `HARVEST.LE` session. `handle_talk_to_mcknight @ 0x2c770` started as a truncated `0x2c770..0x2caed` body (`472` address-count) with `8` reachable dialogue calls and `8` stale `CALL_RETURN` gaps. The rebuild needed two in-memory passes and finished cleanly at `0x2c770..0x2caf3` (`730` address-count) with `17` reachable dialogue calls, `gapCount=0`, and `20` repair actions. After the normal settle and explicit save, a fresh `verify MCKNIGHT` still reported `status=clean` with both dialogue entry points returning, so McKnight is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 24, 2026, reimplemented the ScummVM Maint Man dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_maint_man` (`0x2b6a0..0x2b724`). The native routine only checks `MAINTENANCE_MAN_FIRST_CONVERSATION`, `MAINTENANCE_MAN_THIRD_CONVERSATION`, and `MAINTENANCE_MAN_FOURTH_CONVERSATION`, returning `MAINT_MAN 0xdbc[2]` or `0xdc4[2]` on the latter two flags and otherwise sharing a single tail call that selects either `0xdaf[1]` for the first-conversation bark or `0xdb6[0]` for the default bark. Patched [`engines/harvester/npc/maint_man_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/maint_man_dialogue.cpp) to mirror that recovered control flow, audited the directly encountered Ghidra callees without finding any `FUN_*` symbol that met the rename bar, and verified progress remains `930` total functions, `811` named/documented, and `119` remaining `FUN_*`.
- On March 24, 2026, reimplemented the ScummVM Lodge Chef dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_lodge_chef` (`0x38170..0x38220`). The old stub in [`engines/harvester/npc/lodge_chef_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/lodge_chef_dialogue.cpp) only played `0x1b10[2]`, while the recovered native handler is a fixed four-line intro `0x1b10[2] -> 0x1b19[0] -> 0x1b1a[3] -> 0x1b1b[0]`, a single `dialog.rsp[0x21]` branch where response `1` plays `0x1b2c[3]` and response `2` plays `0x1b31[0]`, and a fixed tail `PC 0x1b36[0] -> LODGE_CHEF 0x1b3a[3]`. Patched the handler to mirror that exact flow, audited the directly encountered Ghidra callees without finding any `FUN_*` symbol that met the rename bar, and rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully with progress unchanged at `930` total functions, `811` named/documented, and `119` remaining `FUN_*`.

- On March 23, 2026, reimplemented the ScummVM Madam dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_madam` (`0x2adc0..0x2af64`). The old stub in [`engines/harvester/npc/madam_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/madam_dialogue.cpp) only cleared `MADAM_INTERRUPT_CONVERSATION` and played `0x1537[1]`, while the recovered native handler is a single interrupt-gated two-menu exchange: `0x1537[1] -> dialog.rsp[0xfb]`, any non-`1` top-level response taking `0x1577[0] -> 0x157b[2] -> queue live HOOKER death-or-monsterfy`, and response `1` taking `0x1542[1]` into either the no-cash branch `PC 0x156e[0] -> MADAM 0x1572[0] -> 0x157b[2] -> queue live HOOKER death-or-monsterfy`, the inner `dialog.rsp[0xfc]` response-`1` branch `0x154e[0] -> 0x157b[2] -> queue live HOOKER death-or-monsterfy`, or the paid branch `0x1555[1] -> hide INVENTORY:BARCASH200 -> HOARLOVE.FST -> 0x155a[0] -> 0x1563[0] -> C117.FST -> HURT_PC_0 -> ACTV_LUST_EXIT`. Patched the handler to mirror that exact flow, renamed the directly evidenced FST wrapper `FUN_000387c0` at `0x387c0` to `play_c117_fst_sequence`, saved `HARVEST.LE`, and refreshed live progress to `930` total functions, `811` named/documented, and `119` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair MADAM -> wait -> save_program -> verify MADAM` cycle against the live `HARVEST.LE` session. `handle_talk_to_madam @ 0x2adc0` started as a truncated `0x2adc0..0x2af64` body (`57` address-count) with `1` reachable dialogue call and `1` stale `CALL_RETURN` gap. A single repair pass rebuilt it cleanly to `0x2adc0..0x2af64` (`421` address-count) with `12` reachable dialogue calls, `gapCount=0`, and `3` repair actions. After the normal settle and explicit save, a fresh `verify MADAM` still reported `status=clean` with both dialogue entry points returning, so Madam is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, reimplemented the ScummVM Librarian dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_librarian` (`0x28f40..0x2910a`). The old stub in [`engines/harvester/npc/librarian_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/librarian_dialogue.cpp) collapsed the native flow into a single `0x1d5b` bark or an immediate `CLUE` award, but the recovered handler is a larger three-way split: visible `CAINBOOK` inventory auto-triggers the trade branch `0x1db1[1] -> 0x1db7 -> 0x1dbb -> 0x1dc2[PC4] -> 0x1dc6` plus `CLUE` award and `set_object_visibility_for_owner_or_room("RAH", "CAINBOOK", 0)`; wrong presented items only play `0x1d5b`; and empty-hand talks run `0x1d5b -> 0x1d5f -> 0x1d63[2] -> 0x1d67 -> 0x1d6c -> 0x1d77 -> dialog.rsp[0xfa]`, optionally `0x1d85` or `0x1d8b[4]`, then the shared tail `0x1d8f -> 0x1d94 -> 0x1d98 -> 0x1da1 -> 0x1da5`. Patched the ScummVM handler to mirror that exact control flow, kept the native visible-inventory semantics so the hidden Cainbook no longer counts as owned, rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully, and audited the directly touched helper chain in Ghidra without finding any encountered `FUN_*` function that met the rename bar. Live progress remains `929` total functions, `810` named/documented, and `119` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair LIBRARIAN -> wait -> save_program -> verify LIBRARIAN` cycle against the live `HARVEST.LE` session. `handle_talk_to_librarian @ 0x28f40` started as a truncated `0x28f40..0x2901d` body (`124` address-count) with `3` reachable dialogue calls and `3` stale `CALL_RETURN` gaps. A single repair pass rebuilt it cleanly to `0x28f40..0x2910a` (`459` address-count) with `18` reachable dialogue calls, `gapCount=0`, and `7` repair actions. After the normal settle and explicit save, a fresh `verify LIBRARIAN` still reported `status=clean` with both dialogue entry points returning, so Librarian is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, reimplemented the ScummVM Inquisitor dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_inquisitor` (`0x2af70..0x2afe8`). The old stub in [`engines/harvester/npc/inquisitor_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/inquisitor_dialogue.cpp) only gated on `PAIN_SOLVED`, cleared the local talk-state bit, and played `0x18e7`, while the recovered native handler is a one-shot straight-line sequence: `0x18e7 -> 0x18e8 -> 0x18e9 -> TORCH2.FST -> 0x18f8 -> START_INQ_TIM`, still gated by `PAIN_SOLVED` and `g_inquisitor_talk_state_block`. Patched the handler to mirror that exact flow, renamed the directly evidenced FST wrapper `FUN_00038810` at `0x38810` to `play_torch2_fst_sequence`, saved `HARVEST.LE`, and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully. Live progress is now `929` total functions, `810` named/documented, and `119` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair INQUISITOR -> wait -> save_program -> verify INQUISITOR` cycle against the live `HARVEST.LE` session. `handle_talk_to_inquisitor @ 0x2af70` started as a truncated `0x2af70..0x2afe8` body (`55` address-count) with `1` reachable dialogue call and `1` stale `CALL_RETURN` gap. A single repair pass rebuilt it cleanly to `0x2af70..0x2afe8` (`121` address-count) with `4` reachable dialogue calls, `gapCount=0`, and `3` repair actions. After the normal settle and explicit save, a fresh `verify INQUISITOR` still reported `status=clean` with both dialogue entry points returning, so Inquisitor is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, reimplemented the ScummVM Gladiator dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_gladiator` (`0x2c630..0x2c762`). The old stub in [`engines/harvester/npc/gladiator_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/gladiator_dialogue.cpp) only cleared `GLADIATOR_INTERRUPT_CONVERSATION` and played `0x69c[1]`, while the recovered native handler is a two-menu interrupt conversation: `0x69c[1] -> 0x6a3[0] -> dialog.rsp[0xc3]`, response `1` taking the death tail `0x6af[2] -> 0x6cd[2] -> queue live GLADIATOR death-or-monsterfy`, and response `2` taking `0x6b3[0] -> C110.FST -> dialog.rsp[0xc4]` where response `1` uses the alternate death tail `0x6c3[2] -> 0x6cd[2]` and response `2` plays `0x6c7[1] -> 0x6cd[0] -> START_MERCY_TIMR`. Patched the handler to mirror that exact control flow, confirmed that the only Gladiator-path helpers hit by this audit were already stably named (`play_c110_fst_sequence`, `queue_live_named_npc_death_or_monsterfy_transition`, and the shared room-event dispatcher), rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully, and left live progress unchanged at `928` total functions, `809` named/documented, and `119` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair GLADIATOR -> wait -> save_program -> verify GLADIATOR` cycle against the live `HARVEST.LE` session. `handle_talk_to_gladiator @ 0x2c630` started as a truncated `0x2c630..0x2c762` body (`57` address-count) with `1` reachable dialogue call and `1` stale `CALL_RETURN` gap. A single repair pass rebuilt it cleanly to `0x2c630..0x2c762` (`307` address-count) with `9` reachable dialogue calls, `gapCount=0`, and `3` repair actions. After the normal settle and explicit save, a fresh `verify GLADIATOR` still reported `status=clean` with both dialogue entry points returning, so Gladiator is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, reimplemented the ScummVM Dark Woman dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_dark_woman` (`0x33610..0x33819`). The old stub in [`engines/harvester/npc/dark_woman_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/dark_woman_dialogue.cpp) only handled direct `HANDMIRROR` use as `0x4d03[2]` and a single first-talk bark `0x4cd5[1]`, while the recovered native handler is a four-menu tree: `0x4cd5[1]`, response line `0x4b`, and three nested `0x4ce6[4]` follow-ups through response lines `0x4c` and `0x4d`, with response `1` on any menu taking the `0x4ce2[2]` live NPC death-or-monsterfy branch. The fourth menu switches between `dialog.rsp[0x4e]` and `dialog.rsp[0x4f]` based on whether `HANDMIRROR` is already in inventory, then either kills Dark Woman, falls through to `0x4cfc[2]` when the mirror is absent, or runs the shared reveal branch `0x4d03[2] -> C109.FST -> 0x4d0d[3] -> hide HANDMIRROR -> SET_D_W_FLAG`. Patched the handler to mirror that control flow, preserved the native wrong-item fallthrough to the intro gate, renamed the directly evidenced FST wrapper `FUN_00038ac0` at `0x38ac0` to `play_c109_fst_sequence`, saved `HARVEST.LE`, and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully. Live progress is now `928` total functions, `809` named/documented, and `119` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair DARK_WOMAN -> wait -> save_program -> verify DARK_WOMAN` cycle against the live `HARVEST.LE` session. `handle_talk_to_dark_woman @ 0x33610` started as a truncated `0x33610..0x33819` body (`94` address-count) with `2` reachable dialogue calls and `2` stale `CALL_RETURN` gaps. A single repair pass rebuilt it cleanly to `0x33610..0x33819` (`502` address-count) with `10` reachable dialogue calls, `gapCount=0`, and `5` repair actions. After the normal settle and explicit save, a fresh `verify DARK_WOMAN` still reported `status=clean` with both dialogue entry points returning, so Dark Woman is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, reimplemented the ScummVM Curator dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_curator` (`0x34ca0..0x34d3c`). The old stub in [`engines/harvester/npc/curator_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/curator_dialogue.cpp) only played `0x425[1]`, while the recovered native flow is `0x425[1]`, response menu `0x4a`, branch line `0x430[3]` or `0x435[0]`, then the shared tail `0x43a[0] -> 0x43b[0] -> PC 0x447[0] -> 0x44b[3]`. Patched the ScummVM handler to mirror that exact sequence, and checked the Curator-adjacent Ghidra symbols while auditing the path, but no encountered `FUN_*` function met the project's confidence bar for renaming. Live progress remains `927` total functions, `808` named/documented, and `119` remaining `FUN_*`.
- On March 23, 2026, re-audited the ScummVM Cloak Attendant dialogue handler against the live `HARVEST.LE` Ghidra body at `handle_talk_to_cloak_atnd` (`0x2db80..0x2dbce`). The native handler is still only a two-branch worker: if `g_dialogue_presented_object_name` is set and matches `BARCASHFIVE`, dispatch `CLEAN_CLOTHES`; otherwise, on empty-hand talks, suppress dialogue once `CLEANED_CLOTHES` is set and fall through to `CLOAK_ATND 0x1066` while the flag is clear. The existing ScummVM logic already matched that recovered behavior, so the source patch in [`engines/harvester/npc/cloak_atnd_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/cloak_atnd_dialogue.cpp) was limited to reordering the branches and naming the constants so the handler mirrors the native item-first flow explicitly. I also checked the directly adjacent Ghidra symbols on this path, but no encountered `FUN_*` function met the project confidence bar for renaming. Live progress remains `927` total functions, `808` named/documented, and `119` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair CURATOR -> wait -> save_program -> verify CURATOR` cycle against the live `HARVEST.LE` session. `handle_talk_to_curator @ 0x34ca0` started as a truncated `0x34ca0..0x34cb5` body (`22` address-count) with `1` reachable dialogue call and `1` stale `CALL_RETURN` gap. A single repair pass rebuilt it cleanly to `0x34ca0..0x34d3c` (`157` address-count) with `6` reachable dialogue calls, `gapCount=0`, and `3` repair actions. After the normal settle and explicit save, a fresh `verify CURATOR` still reported `status=clean` with both dialogue entry points returning, so Curator is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, reran the full durable `repair CLOAK_ATND -> wait -> save_program -> verify CLOAK_ATND` cycle against the live `HARVEST.LE` session. `handle_talk_to_cloak_atnd @ 0x2db80` started as a `0x2db80..0x2dbce` body (`79` address-count) with `1` reachable dialogue call and `1` stale `CALL_RETURN` gap at the final `play_dialogue_line`. A single repair pass cleared the stale flow metadata in place, leaving the same `79`-address body but restoring `gapCount=0` with `2` repair actions. After the normal settle and explicit save, a fresh `verify CLOAK_ATND` still reported `status=clean` with both dialogue entry points returning, so Cloak Attendant is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body metadata repair rather than a symbol-count change.
- On March 23, 2026, re-audited the ScummVM Ryder dialogue handler against the live `HARVEST.LE` Ghidra body at `handle_talk_to_ryder` (`0x38c80..0x391fa`). The existing port already matched Ryder's rebuilt control flow, response menus, and FST sequence order, but the presented-item branches were still missing three native shared-state writes: `PHOTO_OF_WHALEY_HERRILL` only set the startup-script flag instead of also setting `discussedWhaleyHerrillPhoto`, `CASKET_PHOTO` / `CASKET_PHOTOCOPY` omitted `discussedCasketPhotoEvidence`, and note/checkbook evidence omitted `discussedNoteCheckbookEvidence`. Patched [`engines/harvester/npc/ryder_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/ryder_dialogue.cpp) to restore those native side effects, confirmed the topic/response lines from `DIALOG.RSP` (`0x29c..0x2aa`) while auditing the keyword loop, renamed the Ryder-only FST dispatcher `FUN_000386b0` at `0x386b0` to `play_ryder_dialogue_fst_sequence`, saved `HARVEST.LE`, and refreshed live progress to `927` total functions, `808` named/documented, and `119` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair RYDER -> wait -> save_program -> verify RYDER` cycle against the live `HARVEST.LE` session. `handle_talk_to_ryder @ 0x38c80` started as a truncated `0x38c80..0x38f59` body (`395` address-count) with `7` reachable dialogue calls and `7` stale `CALL_RETURN` gaps. The repair needed three in-memory passes: pass 1 expanded the handler to `0x38c80..0x391fa` with `23` dialogue calls and `4` remaining gaps, pass 2 kept the same body end but exposed `37` dialogue calls with `1` remaining gap, and pass 3 finished the rebuild cleanly at `0x38c80..0x391fa` (`1403` address-count) with `37` reachable dialogue calls, `gapCount=0`, and `25` total repair actions. After the normal settle and explicit save, a fresh `verify RYDER` still reported `status=clean` with both dialogue entry points returning, so Ryder is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, reimplemented the ScummVM Chessmaster dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_chessmaster` (`0x2d900..0x2d9ac`). The old stub in [`engines/harvester/npc/chessmaster_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/chessmaster_dialogue.cpp) only played `0x4115`, while the recovered native flow is `0x4115`, response menu `0x22`, line `0x4122[1]` before a three-stage fullscreen chess puzzle, then either the success tail `CHESS_SOLVED -> C125.FST -> queued Chessmaster slash death` or the failure tail `0x4126[2] -> ADD_CHESS_KING_MONSTER -> KINGGROW.FST`. Patched the ScummVM handler to mirror that control flow, implemented the native `CHOOSE1/2/3.FLC` plus `CHESMOV1/2/3.FLC` puzzle sequence with the confirmed hotspot rectangles from Ghidra, renamed `FUN_0005a550` at `0x5a550` to `run_chessmaster_three_move_puzzle`, saved `HARVEST.LE`, and rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully. Live progress is now `926` total functions, `807` named/documented, and `119` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair CHESSMASTER -> wait -> save_program -> verify CHESSMASTER` cycle against the live `HARVEST.LE` session. `handle_talk_to_chessmaster @ 0x2d900` started as a truncated `0x2d900..0x2d914` body (`21` address-count) with `1` reachable dialogue call and `1` stale `CALL_RETURN` gap. The repair needed two in-memory passes: pass 1 expanded the handler to `0x2d900..0x2d9ac` with `3` dialogue calls but left `1` newly exposed gap, and pass 2 finished the rebuild cleanly at `0x2d900..0x2d9ac` (`173` address-count) with `3` reachable dialogue calls, `gapCount=0`, and `6` total repair actions. After the normal settle and explicit save, a fresh `verify CHESSMASTER` still reported `status=clean` with both dialogue entry points returning, so Chessmaster is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, reimplemented the ScummVM Beggar dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_beggar` (`0x34d40..0x34e29`). The old stub in [`engines/harvester/npc/beggar_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/beggar_dialogue.cpp) collapsed the native flow into the `BEGGAR_DIALOG_3` action-tag dispatch plus a single `0x1276[1]` line, while the recovered handler is a fixed two-response exchange: `0x1276[1] -> 0x127a[2] -> 0x127e[0]`, response line `0x0` selecting `0x128d[1]` or `0x1291[3]`, then `0x1295[0]`, response line `0x1`, and `0x12ab[2]` or `0x12a5[2]`. Patched [`engines/harvester/npc/beggar_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/beggar_dialogue.cpp) to match that control flow, kept the native `BEGGAR_DIALOG_3` action-tag dispatch at the top, and renamed the directly evidenced live-entity tail helper `FUN_0007ca90` at `0x7ca90` to `queue_live_named_npc_death_or_monsterfy_transition`. That helper only resolves the named live NPC entity and queues the already-documented actor state `0x35`, so the current ScummVM port still relies on the action-tag interaction path for room-state mutation while the exact visual tail remains an engine-side follow-up rather than a guessed rewrite. Live progress is now `919` total functions, `806` named/documented, and `113` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair BEGGAR -> wait -> save_program -> verify BEGGAR` cycle against the live `HARVEST.LE` session. `handle_talk_to_beggar @ 0x34d40` started as a truncated `0x34d40..0x34e29` body (`55` address-count) with `1` reachable dialogue call and `1` stale `CALL_RETURN` gap. A single repair pass rebuilt it cleanly to `0x34d40..0x34e29` (`234` address-count) with `6` reachable dialogue calls, `gapCount=0`, and `3` repair actions. After the normal settle and explicit save, a fresh `verify BEGGAR` still reported `status=clean` with both dialogue entry points returning, so Beggar is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, recreated [`OPCODES.md`](/Users/alex/Workspace/scummvm/OPCODES.md) in the repo and expanded it with explicit documentation for the Harvester script command-label model. The new notes distinguish `triggerTag` as the stored `COMMAND` label from the interpreter's runtime `currentTag`, cite the exact lookup and execution sites in `script.cpp`, and add concrete straight-line, branching, and deferred-continuation examples so the command graph is described in terms of how ScummVM actually walks it. Progress counts are unchanged because this was documentation only.
- On March 23, 2026, reimplemented the ScummVM Authority dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_authority` (`0x2b730..0x2b7b2`). The confirmed source drift in [`engines/harvester/npc/authority_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/authority_dialogue.cpp) was small but real: the old stub collapsed the native `IF_DON_T_EAT_THE_FOOD` branch into a single `0x3992` bark, while the recovered handler actually falls through to a second `0x399b` Authority line when `IF_YOU_EAT_THE_FOOD_AND_DEFEAT_THE_ENEMIES` is also set. Patched the ScummVM handler to preserve the native early `STOP_AUTHOR_TALK` exit, restore that two-line food branch, and keep the `0x398a` versus `0x399b` fallback when the earlier food flag is clear. While auditing the same dialogue helper cluster, also renamed the directly evidenced one-call `play_fst_sequence` wrappers at `0x38700`, `0x38710`, and `0x38720` to `play_c101_fst_sequence`, `play_c021_fst_sequence`, and `play_c110_fst_sequence`, then saved `HARVEST.LE`. Live progress is now `919` total functions, `805` named/documented, and `114` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair AUTHORITY -> wait -> save_program -> verify AUTHORITY` cycle against the live `HARVEST.LE` session because Authority had regressed back to the short body in the saved database. `handle_talk_to_authority @ 0x2b730` started again as a truncated `0x2b730..0x2b7b2` body (`95` address-count) with `2` reachable dialogue calls and `2` stale `CALL_RETURN` gaps. A single repair pass rebuilt it cleanly to `0x2b730..0x2b7b2` (`131` address-count) with `3` reachable dialogue calls, `gapCount=0`, and `4` repair actions. After the normal settle and explicit save, a fresh `verify AUTHORITY` still reported `status=clean` with both dialogue entry points returning, so Authority is now durably clean again in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, reran the full durable `repair FIREMAN2 -> wait -> save_program -> verify FIREMAN2` cycle against the live `HARVEST.LE` session. `handle_talk_to_fireman2 @ 0x33940` started as a truncated `0x33940..0x33979` body (`58` address-count) with `1` reachable dialogue call and `1` stale `CALL_RETURN` gap. A single repair pass rebuilt it cleanly to `0x33940..0x339e9` (`170` address-count) with `3` reachable dialogue calls, `gapCount=0`, and `3` repair actions. After the normal settle and explicit save, a fresh `verify FIREMAN2` still reported `status=clean` with both dialogue entry points returning, so Fireman2 is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, reimplemented the ScummVM Fireman2 dialogue handler directly against the live `HARVEST.LE` Ghidra bodies at `handle_talk_to_fireman2` (`0x33940..0x33979`) and `reset_talk_to_fireman2_state` (`0x33a10..0x33a25`). The native dialogue logic itself is still only a two-line switch: when `g_fireman2_talk_state_block` is nonzero, Fireman2 plays `0x5b9[2]` and clears that block; otherwise the handler plays `0x5c7[2]`. The real source drift in [`engines/harvester/npc/fireman2_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/fireman2_dialogue.cpp) was state fidelity rather than missing branches: native reset clears a three-dword local state block, while ScummVM only modeled the first flag. Patched [`engines/harvester/npc/fireman2_dialogue.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/fireman2_dialogue.h) and [`engines/harvester/npc/fireman2_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/fireman2_dialogue.cpp) to carry the full local block (`talkStateBlock`, `dialogueStateD2de0`, `dialogueStateD2de4`) and serialize the two additional native ints starting with save version `12`; bumped the Harvester save blob version to `12` in [`engines/harvester/saveload.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/saveload.cpp); rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully; and inspected the directly related Ghidra path for unnamed helpers, but no `FUN_*` function on the Fireman2 route met the project evidence bar for renaming. Live progress counts remain `919` total functions, `802` named/documented, and `117` remaining `FUN_*`.
- On March 23, 2026, reimplemented the ScummVM Fireman1 dialogue handler directly against the live `HARVEST.LE` Ghidra bodies at `handle_talk_to_fireman1` (`0x33850..0x338f6`) and `reset_talk_to_fireman1_state` (`0x33920..0x33937`). The recovered native flow is still itemless, but the old stub in [`engines/harvester/npc/fireman1_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/fireman1_dialogue.cpp) was missing both native one-shot follow-ups: after the first-talk `0x453` or revisit `0x463` bark, Fireman1 now conditionally plays `0x45a[3]` when `STEPH_MIDGAME_PLAYED` is set and `0x46a[2]` when `BOLT_OF_CLOTH_TAKEN` is set, each guarded by Fireman1-local room state. Patched [`engines/harvester/npc/fireman1_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/fireman1_dialogue.cpp), extended Fireman1's save-backed room state in [`engines/harvester/npc/fireman1_dialogue.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/fireman1_dialogue.h), bumped the Harvester save version to `11` in [`engines/harvester/saveload.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/saveload.cpp) for the two new local state bits, and renamed the directly evidenced Fireman1 globals at `0xd2dd4` and `0xd2dd8` to `g_fireman1_steph_midgame_followup_shown` and `g_fireman1_bolt_of_cloth_followup_shown`. No standalone `FUN_*` helper surfaced in this analysis thread, so live progress remains `919` total functions, `802` named/documented, and `117` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair FIREMAN1 -> wait -> save_program -> verify FIREMAN1` cycle against the live `HARVEST.LE` session. `handle_talk_to_fireman1 @ 0x33850` started as a truncated `0x33850..0x33883` body (`52` address-count) with `1` reachable dialogue call and `1` stale `CALL_RETURN` gap. A single repair pass rebuilt it cleanly to `0x33850..0x338f6` (`167` address-count) with `3` reachable dialogue calls, `gapCount=0`, and `3` repair actions. After the normal settle and explicit save, a fresh `verify FIREMAN1` still reported `status=clean` with both dialogue entry points returning, so Fireman1 is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, reimplemented the ScummVM Buster dialogue handler directly against the live `HARVEST.LE` Ghidra bodies at `handle_talk_to_buster` (`0x2b7c0..0x2c575`) and `reset_talk_to_buster_state` (`0x2c600..0x2c613`). The recovered native flow was much wider than the old stub in [`engines/harvester/npc/buster_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/buster_dialogue.cpp): item handling now matches the native casket-photo follow-up and blackmail response menu, no-item dialogue restores the full `0x163c..0x168e` opener plus the `0x1828..0x185e` / `0x18c5..0x18cd` follow-ups, the looping keyword tree now covers the recovered `dialog.rsp` topic groups `0x28/0x29`, `0x2c/0x2d`, `0x32`, `0x34`, `0x36`, `0x38`, `0x3b`, `0x3e`, `0x40/0x41`, `0x44/0x45/0x46`, `0x48`, and `0x49`, and Buster's fatal branches now run the native `C086.FST` blackout plus a restored game-over screen instead of falling through to short barks. Patched [`engines/harvester/npc/buster_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/buster_dialogue.cpp), extended Buster's save-backed room state in [`engines/harvester/npc/buster_dialogue.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/buster_dialogue.h), added a reusable dialogue-side game-over helper in [`engines/harvester/dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/dialogue.cpp) and [`engines/harvester/npc/dialogue_runtime.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/dialogue_runtime.h), bumped the Harvester save blob version to `10` in [`engines/harvester/saveload.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/saveload.cpp) for the new Buster-local state bit, rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully, and renamed the directly encountered live Ghidra `FUN_*` helpers to `play_c085_fst_sequence` (`0x387d0`), `play_c086_blackout_fst_sequence` (`0x387e0`), and `play_c058_fst_sequence` (`0x38800`). After saving `HARVEST.LE`, live progress stands at `919` total functions, `802` named/documented, and `117` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair BUSTER -> wait -> save_program -> verify BUSTER` cycle against the live `HARVEST.LE` session. `handle_talk_to_buster @ 0x2b7c0` started as a truncated `0x2b7c0..0x2c56c` body (`449` address-count) with `8` reachable dialogue calls and `8` stale `CALL_RETURN` gaps. The repair needed two in-memory passes: pass 1 expanded the handler to `0x2b7c0..0x2c575` with `95` dialogue calls but left `1` newly exposed gap, and pass 2 finished the rebuild cleanly at `0x2b7c0..0x2c575` (`3510` address-count) with `95` reachable dialogue calls, `gapCount=0`, and `19` total repair actions. After the normal settle and explicit save, a fresh `verify BUSTER` still reported `status=clean` with both dialogue entry points returning, so Buster is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, re-audited the ScummVM Sparky dialogue handler directly against the live `HARVEST.LE` Ghidra bodies at `handle_talk_to_sparky` (`0x39240..0x39652`) and `reset_talk_to_sparky_state` (`0x39690..0x396c5`). The native body showed two concrete source drifts in [`engines/harvester/npc/sparky_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/sparky_dialogue.cpp): most of Sparky's talk branches were still using the wrong portrait/head variants, including the light-switch bark, item rejection, intro exchange, return-visit lines, and the `0x34c`, `0x37a`, `0x38f`, `0x39e`, and `0x3c1` keyword replies; and the ScummVM port was incorrectly seeding a persistent keyword buffer from `dialog.rsp[0x2c3]`, while the native handler starts each conversation with an empty keyword list and only rewrites that list to `dialog.rsp[0x2c9]` after topic `0x2c8`. Patched [`engines/harvester/npc/sparky_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/sparky_dialogue.cpp) to match the recovered variants and one-call topic rewrite, rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully, and checked the directly encountered `FUN_*` symbols surfaced during the keyword-menu trace. Those `FUN_*` hits (`FUN_0002e3b4`, `FUN_0002e41b`, and `FUN_0002f7fd`) are overlapping mid-body dialogue entries inside larger Herrill and Mrs. Potts handlers rather than clean standalone helpers, so no new rename met the project evidence bar without first repairing the underlying function boundaries. Live progress now reflects the current saved database totals of `916` functions, `799` documented, and `117` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair SPARKY -> wait -> save_program -> verify SPARKY` cycle against the live `HARVEST.LE` session. `handle_talk_to_sparky @ 0x39240` started as a truncated `0x39240..0x39649` body (`559` address-count) with `10` reachable dialogue calls and `10` stale `CALL_RETURN` gaps. The first repair pass rebuilt Sparky cleanly in-session to `0x39240..0x39652` (`860` address-count, `18` dialogue calls, `20` repair actions), but the first saved verify snapped back to the old body and also flipped both dialogue entry points back to non-returning. An immediate second full cycle fixed it: the retry repair rebuilt `handle_talk_to_sparky` cleanly again (`21` repair actions on the retry pass), and after the second settle and explicit save a fresh `verify SPARKY` held at `0x39240..0x39652` with `gapCount=0`, `status=clean`, and both dialogue entry points returning. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, re-audited the ScummVM Phelps dialogue handler directly against the live `HARVEST.LE` Ghidra bodies at `handle_talk_to_phelps` (`0x29920..0x2a0ef`) and `reset_talk_to_phelps_state` (`0x2a110..0x2a139`). The branch structure in [`engines/harvester/npc/phelps_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/phelps_dialogue.cpp) was already close, but the native body showed two concrete source drifts: most of Phelps's talk branches were still using the wrong portrait/head variants, including the intro, default revisit, Whaley photo, casket photo, quarter, blackmail evidence, Jimmy-death follow-up, and the one-shot event barks for `STEPH_MIDGAME_PLAYED`, day 5, `SCRATCHED_TUCKER`, `BARBER_POLE_STOLEN`, `PC_ESCAPED_JAIL`, `GOT_REMAINS_FOR_LODGE`, Butcher death, Moynahan death, and both Karin outcomes; and the engine was setting shared dialogue state `d2f04` before the entire intro instead of after native line `0x1b56` and before the final `PC 0x1b5a` line. Patched [`engines/harvester/npc/phelps_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/phelps_dialogue.cpp) to match the recovered variants and intro ordering, rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully, and checked the directly related Ghidra callees; no new `FUN_*` rename in this thread met the project evidence bar, so live progress remains `913` total functions, `796` named/documented, and `117` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair PHELPS -> wait -> save_program -> verify PHELPS` cycle against the live `HARVEST.LE` session. `handle_talk_to_phelps @ 0x29920` started as a truncated `0x29920..0x29cac` body (`501` address-count) with `8` reachable dialogue calls and `8` stale `CALL_RETURN` gaps. The repair needed two in-memory passes: pass 1 expanded the handler to `0x29920..0x2a0ef` with `43` dialogue calls but left `2` newly exposed gaps, and pass 2 finished the rebuild cleanly at `0x29920..0x2a0ef` (`1849` address-count) with `43` reachable dialogue calls, `gapCount=0`, and `20` total repair actions. After the normal settle and explicit save, a fresh `verify PHELPS` still reported `status=clean` with both dialogue entry points returning, so Phelps is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, re-audited the live `HARVEST.LE` Nude Man path in Ghidra instead of treating the existing ScummVM port as settled. `handle_talk_to_nude_man` (`0x2d9b0..0x2db3e`) still matched the repaired native dialogue body closely, but the Ghidra helper call at `0x2da7d` showed the second response menu is gated by `get_set_sergeant_completed_first_task_state @ 0x384e0`, not by the broader Sergeant shared bit `d2f08` that the engine was using. Patched [`engines/harvester/npc/nude_man_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/nude_man_dialogue.cpp) to read the dedicated `sergeantCompletedFirstTaskState`, updated [`engines/harvester/npc/sergeant_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/sergeant_dialogue.cpp) to set that state on the native `SCRATCHED_TUCKER` completion branch, and added a load-time shared-state migration hook in [`engines/harvester/dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/dialogue.cpp), [`engines/harvester/npc/dialogue_handler.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/dialogue_handler.h), and [`engines/harvester/npc/sergeant_dialogue.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/sergeant_dialogue.h) so older ScummVM saves that only persisted Sergeant's legacy local bool still unlock Nude Man correctly after load. No new `FUN_*` helpers were touched in this thread, so live Ghidra progress remains unchanged at `913` functions, `796` named/documented, and `117` remaining `FUN_*`.
- On March 23, 2026, reran the full durable `repair NUDE_MAN -> wait -> save_program -> verify NUDE_MAN` cycle against the live `HARVEST.LE` session. `handle_talk_to_nude_man @ 0x2d9b0` started as a truncated `0x2d9b0..0x2db39` body (`96` address-count) with `3` reachable dialogue calls and `3` stale `CALL_RETURN` gaps. A single repair pass rebuilt it cleanly to `0x2d9b0..0x2db3e` (`399` address-count) with `14` reachable dialogue calls, `gapCount=0`, and `6` repair actions. After the normal settle and explicit save, a fresh `verify NUDE_MAN` still reported `status=clean` with both dialogue entry points returning, so Nude Man is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, reimplemented the ScummVM Parsons dialogue handler directly against the live `HARVEST.LE` Ghidra bodies at `handle_talk_to_parsons` (`0x397c0..0x39f1b`) and `reset_talk_to_parsons_state` (`0x39f40..0x39f69`). The recovered native flow was much wider than the old evidence stub: no-item dialogue now runs the full `0xdcc/0xdd2/0xdd6/0xde3` intro with Swell, the one-shot event barks for `STEPH_MIDGAME_PLAYED`, `SCRATCHED_TUCKER`, `BARBER_POLE_STOLEN`, `DINER_BURNED`, `BURNED_TV_STATION`, and `KARIN_KIDNAPED`, and the looping `dialog.rsp` keyword branches for `Pole`, `Alarm System`, and the `Aliens` / `Pastorelli` group with the native `C093.FST` and `C094.FST` inserts. The item logic also had three native continuations the stub was missing: `CASKET_PHOTO` / `CASKET_PHOTOCOPY` optionally extends through `PC 0xe84 -> PARSONS 0xe88` when shared state `d2ebc` is already set, note/checkbook evidence is the three-line `0xe95 -> 0xe99 -> 0xe9d` exchange rather than a single bark, and all native portrait variants now match the live assembly. Patched [`engines/harvester/npc/parsons_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/parsons_dialogue.cpp), extended Parsons's save-backed room state in [`engines/harvester/npc/parsons_dialogue.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/parsons_dialogue.h), bumped the Harvester save blob version to `9` in [`engines/harvester/saveload.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/saveload.cpp) so the six new Parsons-local one-shot bits persist cleanly, and renamed the two directly referenced `FUN_*` wrappers in the live Ghidra database to `play_c093_fst_sequence` (`0x38730`) and `play_c094_fst_sequence` (`0x38740`) because each is a single-call wrapper around the exact `GRAPHIC/FST/C093.FST` / `GRAPHIC/FST/C094.FST` assets. Progress now aligns with the saved live database totals (`913` functions, `796` documented, `117` remaining `FUN_*`).
- On March 23, 2026, reran the full durable `repair PARSONS -> wait -> save_program -> verify PARSONS` cycle against the live `HARVEST.LE` session. `handle_talk_to_parsons @ 0x397c0` started as a truncated `0x397c0..0x39a7a` body (`468` address-count) with `7` reachable dialogue calls and `7` stale `CALL_RETURN` gaps. The repair needed two in-memory passes: pass 1 expanded the handler to `0x397c0..0x39f1b` with `32` dialogue calls but left `7` newly exposed gaps, and pass 2 finished the rebuild cleanly at `0x397c0..0x39f1b` (`1884` address-count) with `43` reachable dialogue calls, `gapCount=0`, and `28` total repair actions. After the normal settle and explicit save, a fresh `verify PARSONS` still reported `status=clean` with both dialogue entry points returning, so Parsons is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, reimplemented the ScummVM Mother dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_mother` (`0x2b5b0..0x2b66f`) plus `reset_talk_to_mother_state` (`0x2b690..0x2b69a`). The recovered native flow was not the old one-line bark: it gates on a single local talk-state bit, plays `0x26d6[3]`, runs `C072.FST`, plays `0x26e9[0]`, and opens response menu `0x173`; response `1` exits through `0x26f5[3] -> C073.FST`, response `2` exits through `C073.FST -> 0x2709[3] -> C072.FST -> 0x2714[0] -> 0x2719[3]`, and any other menu result falls through silently after the first menu. Patched [`engines/harvester/npc/mother_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/mother_dialogue.cpp), rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully, and renamed the two directly referenced `FUN_*` wrappers in the live Ghidra database to `play_dialogue_c072_fst_sequence` (`0x38ae0`) and `play_dialogue_c073_fst_sequence` (`0x38910`) because each is a single-call wrapper around the exact `GRAPHIC/FST/C072.FST` / `GRAPHIC/FST/C073.FST` assets. After saving `HARVEST.LE`, progress now aligns with the live database totals (`913` functions, `794` documented, `119` remaining `FUN_*`).
- On March 23, 2026, reran the full durable `repair MOTHER -> wait -> save_program -> verify MOTHER` cycle against the live `HARVEST.LE` session. `handle_talk_to_mother @ 0x2b5b0` started as a truncated `0x2b5b0..0x2b66f` stub with `1` reachable dialogue call and `1` stale `CALL_RETURN` gap at `0x2b5d6`, then rebuilt cleanly on the first pass to `0x2b5b0..0x2b66f` (`192` address-count, `6` dialogue calls, `3` repair actions). After the normal settle and explicit save, a fresh `verify MOTHER` still reported `gapCount=0`, `status=clean`, and both dialogue entry points returning, so Mother is now durably clean in the saved database. Progress counts are unchanged because this was a handler-body recovery rather than a symbol-count change.
- On March 23, 2026, fixed [`InspectCallTermination.java`](/Users/alex/ghidra_scripts/InspectCallTermination.java) to recognize exact `dispatch_action_tag_if_set` talk stubs as valid data-driven handlers instead of false `noDialogueCalls` repair failures. `handle_talk_to_pastorelli @ 0x38160` is one of those cases: the live `HARVEST.LE` body is an exact 13-byte stub (`0x38160..0x3816c`) that loads `PASTORELLI_TALK`, calls `dispatch_action_tag_if_set`, and returns, while the real dialogue work is deferred into `dispatch_room_event_actions`, whose opcode `0x1a` path runs NPC dialogue from room-command data. After the script patch, both `repair PASTORELLI` and a fresh `verify PASTORELLI` report `dispatchOnly=true`, `gapCount=0`, and `status=clean` with no database repair actions required. Progress counts are unchanged because the Ghidra program state did not change; this was a verifier classification fix.
- On March 23, 2026, reimplemented the ScummVM Boyle dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_boyle` (`0x2cb20..0x2d885`) plus `reset_talk_to_boyle_state` (`0x2d8a0..0x2d8b7`). The confirmed source drift was concentrated in five native areas the earlier port had flattened or miswired: the handler was assigning the wrong speakers on Boyle's intro, keyword, button, gascan, blackmail, and burned-TV exchanges, especially the native PC turns at `0xb`, `0x15a`, `0x179`, `0x1a9`, `0x202`, `0x28e`, and the `0x8c` / `0x94` / `0x9d` / `0xee` / `0xf6` / `0x12c` keyword branches; the port was missing Boyle's sixth save-backed local bit at `0xd2cbc`, so intro response menu `0x7` choice `2` was incorrectly writing shared `d2ebc` instead of a Boyle-local evidence-reply gate that later changes the `BOYLES_BUTTON` and note/checkbook continuations; the item paths were mutating the wrong runtime state by setting generic evidence flags that native never touches here, by resetting `BOYLES_BUTTON` / `GASCAN` to their initial startup records instead of simply hiding them from inventory, and by clearing the Boyle gascan-application bit plus reasserting `SHERIFF_IN_DINER` during the gascan reward even though native only hides `GASCAN`, awards `LODGE_APPLICATION`, and sets `HAVE_LODGE_APP`; the post-intro no-item evidence follow-up was gated off the wrong ScummVM flag instead of native shared state `discussedNoteCheckbookEvidence`; and the intro response-menu `0x5` cancel path was incorrectly seeding the button keyword buffer even though native only seeds `0xa` after the two handled intro branches. Patched [`engines/harvester/npc/boyle_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/boyle_dialogue.cpp), extended Boyle's save-backed room state in [`engines/harvester/npc/boyle_dialogue.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/boyle_dialogue.h), bumped the Harvester save blob version to `8` in [`engines/harvester/saveload.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/saveload.cpp) so the new Boyle-local bit persists cleanly, corrected the confirmed Boyle wrapper notes in [`ARCHITECTURE.md`](/Users/alex/Workspace/scummvm/ARCHITECTURE.md), and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully. Progress counts remain aligned with the current live Ghidra database totals (`912` functions, `120` remaining `FUN_*`).
- On March 23, 2026, reran the full durable `repair BOYLE -> wait -> save_program -> verify BOYLE` cycle against the live `HARVEST.LE` session to confirm Boyle still survives cleanly with the current `InspectCallTermination.java` behavior. `handle_talk_to_boyle` rebuilt to `0x2cb20..0x2d885` (`3430` address-count, `85` dialogue calls, `52` repair actions) and the first post-save verify held without needing a retry: both dialogue entry points still verify as returning and `handle_talk_to_boyle` reports `gapCount=0` / `status=clean` in the saved database. Progress is unchanged because Boyle was already in the confirmed-clean set, but this revalidation confirms it is not currently regressing under the newer verifier logic.
- On March 23, 2026, reimplemented the ScummVM Moynahan dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_moynahan` (`0x24a20..0x25f43`). The confirmed source drift was concentrated in five native areas the earlier port had flattened or miswired: the `IF_TRY_TO_TAKE_THE_GLUE` interrupt and several presented-item branches were using wrong head variants and were missing the native response-menu continuations for `CASKET_PHOTO` / `CASKET_PHOTOCOPY`, both ledgers, note/checkbook evidence, and `WED_MATCHES`; the handler was writing the wrong shared state for Whaley-photo and blackmail evidence, rearming the intro off the wrong corpse-photo state, and omitting the `KARIN_KIDNAPED` tail's `0x4041` line plus the shared kidnapped-discussion writes; the late no-item bark chain after `0x3fd1` still had truncated control flow and non-native portrait variants on the slash/body/day-5/Butcher/Jimmy/Karin-dead branches; the typed keyword loop was incorrectly treated as mostly terminal single-line replies instead of a looping menu that rewrites the live topic buffer through `0x19c`, `0x1a1`, `0x1a6` / `0x1a7` / `0x1a8`, `0x1ad`, `0x1b0` / `0x1b1`, and `0x1b6`; and the day-5 glue-topic path `0x1bf..0x1c2` plus response menu `0x1c3` was missing its native `GLUE` inventory / `JUST_GOT_GLUE` side effects. Patched [`engines/harvester/npc/moynahan_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/moynahan_dialogue.cpp), preserved the existing save layout in [`engines/harvester/npc/moynahan_dialogue.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/moynahan_dialogue.h), rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully, and refreshed the tracker progress totals from the current live Ghidra database (`912` functions, `120` remaining `FUN_*`).
- On March 23, 2026, reran the full durable `repair MOYNAHAN -> wait -> save_program -> verify MOYNAHAN` cycle against the live `HARVEST.LE` session to confirm Moynahan still survives cleanly with the current `InspectCallTermination.java` behavior. `handle_talk_to_moynahan` rebuilt to `0x24a20..0x25f43` (`5254` address-count, `117` dialogue calls, `59` repair actions) and the first post-save verify held without needing a retry: both dialogue entry points still verify as returning and `handle_talk_to_moynahan` reports `gapCount=0` / `status=clean` in the saved database. Progress is unchanged because Moynahan was already in the confirmed-clean set, but this revalidation confirms it is not currently regressing under the newer verifier logic.
- On March 23, 2026, reimplemented the remaining confirmed Herrill dialogue mismatches in ScummVM directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_herrill` (`0x2dc00..0x2e987`). The confirmed source drift was concentrated in four native areas the earlier port had flattened or misread: the `HERRILL_IN_LODGE` branch was truncated to a single `0x31ca` bark and was missing the native `0x31ce` / `0x31d2` / `0x31da` / `0x31de` / optional `0x31e0` / `0x31e2` sequence plus the `GRAND_MUCK` slash-transition and `PC_TALKED_TO_HERRILL` write; the `PHOTO_OF_WHALEY_HERRILL` item branch was incorrectly writing the shared Whaley/Herrill-photo runtime flag even though native only plays `0x309f`; the steady-state `KARIN_FOUND_ALIVE` / `KARIN_FOUND_DEAD` followups had non-native extra lines (`0x3050` / `0x3057` and `PC 0x461b`) that are absent from the real handler; and the keyword loop mishandled `The Lodge`, the second `BYE` line at `0x293`, and the unmatched typed-topic fallback that should run `0x3099` before `0x2ff4`. Patched [`engines/harvester/npc/herrill_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/herrill_dialogue.cpp), corrected the confirmed Herrill notes in [`ARCHITECTURE.md`](/Users/alex/Workspace/scummvm/ARCHITECTURE.md), rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully, and refreshed the tracker progress totals from the current live Ghidra database (`912` functions, `120` remaining `FUN_*`).
- On March 23, 2026, reran `repair HERRILL` with the current `InspectCallTermination.java` verifier in the live `HARVEST.LE` session. Herrill showed the same first-save snapback pattern seen on other retry-bucket NPCs: the first `repair -> wait -> save -> verify` cycle rebuilt `handle_talk_to_herrill` cleanly to `0x2dc00..0x2e987` (`764` address-count, `17` dialogue calls under the current tail-jump-aware verifier) but the fresh verify fell back to the short `0x2dc00..0x2e987` body with only `4` reachable dialogue calls, `3` stale dialogue gaps, and both dialogue entry points marked non-returning again. An immediate second full cycle fixed it: the second `repair -> wait -> save -> verify` left Herrill durably clean again at `0x2dc00..0x2e987` with `gapCount=0`, `play_dialogue_line` and `thunk_play_dialogue_line` both returning, and `status=clean`. This confirms Herrill now behaves like the broader post-save retry class rather than the old verifier-only anomaly.
- On March 23, 2026, reimplemented the ScummVM Dad dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_dad` (`0x2b020..0x2b581`) plus `reset_talk_to_dad_state` (`0x2b5a0..0x2b5a6`) and the shared Dad permission helper at `get_set_dad_meat_permission_state` (`0x38400..0x3840f`). The confirmed source drift was concentrated in four native areas the earlier port had flattened or miswired: the `TAKING_BONDAGE` branch was missing Dad's second `0x3a6b` line and the intervening `C011.FST` tail after `0x3a61`; the item branches were writing the wrong ScummVM runtime evidence flags, missing Dad's multi-line Whaley-photo and note/checkbook exchanges, and omitting the `MEAT_PERMISSION0` conversion path that plays `0x39f2` / `0x39f7`, runs `C112S4.FST`, sets shared state `d2eb4`, adds `MEAT_PERMISSION`, and restores `MEAT_PERMISSION0` visibility in inventory; the first no-item visit was missing Dad's full `0x39a5` / `0x39a9` / `0x39ad` / `0x39b1[PC2]` / `0x39b5` intro plus the shared `d2f04` write; and the revisit path was incorrectly ending after `0x3a17` instead of running Dad's native Other-only keyword loop, which leaves the visible topic list empty, only special-cases typed `Lodge` / `The Lodge` through `0x3a7d` / `0x3a83` / `0x3a88`, and otherwise falls back to `0x3a2c`. Patched [`engines/harvester/npc/dad_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/dad_dialogue.cpp), preserved the existing save layout in [`engines/harvester/npc/dad_dialogue.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/dad_dialogue.h), rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully, and refreshed the tracker progress totals from the current live Ghidra database (`915` functions, `123` remaining `FUN_*`).
- On March 23, 2026, reran `repair DAD` with the current `InspectCallTermination.java` verifier in the live `HARVEST.LE` session. Dad showed the same first-save snapback pattern seen on other retry-bucket NPCs: the first `repair -> wait -> save -> verify` cycle rebuilt `handle_talk_to_dad` cleanly to `0x2b020..0x2b581` (`954` address-count, `20` dialogue calls) but the fresh verify fell back to the short `0x2b020..0x2b2e9` body with `7` stale dialogue gaps and both dialogue entry points marked non-returning again. An immediate second full cycle fixed it: the second `repair -> wait -> save -> verify` left Dad durably clean again at `0x2b020..0x2b581` with `gapCount=0`, `play_dialogue_line` and `thunk_play_dialogue_line` both returning, and `status=clean`. This confirms Dad belongs in the same post-save retry class as Stephanie, Mr. Potts, Johnson, Loomis, and similar handlers.
- On March 23, 2026, reimplemented the ScummVM Butcher dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_butcher` (`0x2a180..0x2ad7c`) plus `reset_talk_to_butcher_state` (`0x2ada0..0x2adb0`). The confirmed source drift was concentrated in five native areas the earlier stub omitted: the first no-item visit was missing Butcher's full `0x1902..0x1921` intro sequence, the `dialogueStateD2ed0` conditional on `0x190f` vs `0x1913`, and the correct initial topic buffer; the revisit path was missing the native topic-menu gating on `MEAT_PERMISSION0` / `MEAT_PERMISSION` ownership and story day; the handler had no recovered keyword loop for topics `0x263`, `0x267`, `0x26b`, `0x26e`, `0x270`, `0x273/0x274`, `0x277`, and the `0x279` no-op path, including the `C119.FST`, `C060.FST`, `C119A.FST`, `C119B.FST`, `C123.FST`, `C123A.FST`, `C123B.FST`, and `C119C.FST` tails plus the menu-driven topic-buffer rewrites; the item branches for Whaley's photo, the corpse photo, both ledgers, note/checkbook evidence, and presented `MEAT_PERMISSION` were missing their native multi-line exchanges, the `C060A.FST` reward tail, and the `MEAT` inventory / `GOT_MEAT` side effects; and the native cross-handler state writes into `dad_meat_permission_state`, `dialogueStateD2ed0`, and `discussedLodgeTopic` were absent. Patched [`engines/harvester/npc/butcher_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/butcher_dialogue.cpp), extended Butcher's save-backed room state in [`engines/harvester/npc/butcher_dialogue.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/butcher_dialogue.h), fixed Dad's two native shared-state dependencies in [`engines/harvester/npc/dad_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/dad_dialogue.cpp), bumped the Harvester save format to `7` in [`engines/harvester/saveload.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/saveload.cpp), and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully. Progress counts now align with the current live Ghidra database totals (`924` functions, `132` remaining `FUN_*`).
- On March 23, 2026, reran the full durable `repair BUTCHER -> wait -> save_program -> verify BUTCHER` cycle against the live `HARVEST.LE` session to confirm Butcher still survives cleanly with the current `InspectCallTermination.java` behavior. `handle_talk_to_butcher` rebuilt to `0x2a180..0x2ad7c` (`2936` address-count, `60` dialogue calls, `42` repair actions) and the first post-save verify held without needing a retry: both dialogue entry points still verify as returning and `handle_talk_to_butcher` reports `gapCount=0` / `status=clean` in the saved database. Progress is unchanged because Butcher was already in the confirmed-clean set, but this revalidation confirms it is not currently regressing under the newer verifier logic.
- On March 23, 2026, reimplemented the ScummVM Edna dialogue handler directly against the repaired live `HARVEST.LE` Ghidra body at `handle_talk_to_edna` (`0x358a0..0x3667b`) plus `reset_talk_to_edna_state` (`0x366c0..0x366e8`). The confirmed source drift was concentrated in four native areas the earlier port had truncated or flattened: the top-level `KILLED_KARIN1`, `DNA_S_SUICIDE_NOTE`, purse handoff, and `BRING_KARIN_TO_SHERIFF` branches were missing native flag clears plus the reward-money / `K_PURSE` room-state side effects; the first no-item visit was missing Edna's full `0x3a95` intro flow, the `0x3aa1..0x3aae` / `0x3ab2..0x3ac2` response branches, the `0x3ac6..0x3acf` shared tail, and the native `d2f04` write; the revisit path after `0x3bf8` / `0x3bff` was missing every recovered one-shot story branch for day 5, `SCRATCHED_TUCKER`, `BARBER_POLE_STOLEN`, `BOLT_OF_CLOTH_TAKEN`, `PC_ESCAPED_JAIL`, `BURNED_TV_STATION`, and the full `KARIN_FOUND_DEAD` nested response tree including the purse-dependent `0xa3` / `0xa4` menu split; and the entire keyword loop from `0xa5` through `0xc2` was absent, including the `C091.FST` and `C070.FST` topic exits plus the shared lodge / kidnapped-dialogue state writes. Patched [`engines/harvester/npc/edna_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/edna_dialogue.cpp), extended Edna's save-backed room flags in [`engines/harvester/npc/edna_dialogue.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/edna_dialogue.h), bumped the Harvester save blob version in [`engines/harvester/saveload.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/saveload.cpp) so the new Edna state persists cleanly, and rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully. Progress counts now align with the current live Ghidra database totals (`914` functions, `122` remaining `FUN_*`).
- On March 23, 2026, reimplemented the ScummVM Karin dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_karin` (`0x2ea90..0x2ed7f`). The confirmed source drift was concentrated in four native paths the earlier port had flattened: the `KARIN_FOUND_ALIVE` + `IN_CEM10` branch is an eight-line early-return sequence (`0x10a5`..`0x10c3`) rather than a one-line opener that falls back into normal handling; the first no-item visit was missing Karin's full `0x1076`..`0x108b` exchange before the shared `0x10dc` tail; the photo evidence branch was missing Edna's `0x10e7` follow-up, the portrait reset, the one-shot local override bit, and the repeat-photo `DNALFT_PERVERT` / `CALL_POLICE` escalation while also incorrectly writing the broader runtime photo flags instead of Karin's native shared dialogue-state ints; and `INV_MAG` was missing Edna `0x10f2[2]` plus the same police side effects. Patched [`engines/harvester/npc/karin_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/karin_dialogue.cpp), corrected the confirmed Karin handler notes in [`ARCHITECTURE.md`](/Users/alex/Workspace/scummvm/ARCHITECTURE.md), and rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully. Progress counts remain aligned with the current live Ghidra database totals (`912` functions, `120` remaining `FUN_*`).
- On March 23, 2026, reran `repair EDNA` with the current `InspectCallTermination.java` verifier in the live `HARVEST.LE` session. Edna showed the same first-save snapback pattern seen on other retry-bucket NPCs: the first `repair -> wait -> save -> verify` cycle rebuilt `handle_talk_to_edna` cleanly to `0x358a0..0x3667b` (`3505` address-count, `64` dialogue calls) but the fresh verify fell back to the short `0x358a0..0x35d1f` body with `11` stale dialogue gaps and both dialogue entry points marked non-returning again. An immediate second full cycle fixed it: the second `repair -> wait -> save -> verify` left Edna durably clean again at `0x358a0..0x3667b` with `gapCount=0`, `play_dialogue_line` and `thunk_play_dialogue_line` both returning, and `status=clean`. This confirms Edna belongs in the same post-save retry class as Stephanie, Mr. Potts, Johnson, Loomis, and similar handlers.
- On March 23, 2026, reimplemented the ScummVM Dwayne dialogue handler directly against the repaired live `HARVEST.LE` Ghidra body at `handle_talk_to_dwayne` (`0x3a310..0x3c385`) plus `reset_talk_to_dwayne_state` (`0x3c490..0x3c531`). The confirmed source drift was concentrated in five native areas the earlier port had flattened or inverted: the top-level `ARREST_FLAG`, `CHECK_EVIDENCE_DOOR`, `ST_BEDRM`, and sheriff-in-diner intro/revisit flow was returning too early and missing native flag clears plus Edna/PC lines; the evidence-item branches for Boyle's button, Karin's purse, note/checkbook originals vs photocopies, ledgers, corpse photo, Whaley photo, and especially `TV_DEED` / `TV_DEED_PHOTOCOPY` were missing native menus, PC turns, confiscation side effects, and the real-deed vs photocopy split; the note/checkbook and TV-deed follow-up state was not wired to the native save-backed shared fields, so Dwayne's later `0x355f..0x3573` reaction and the Karin-purse gate were wrong after save/load; the one-shot event branches for `BARBER_POLE_STOLEN`, `DINER_BURNED`, `BURNED_TV_STATION`, `KARIN_KIDNAPED`, and `KARIN_FOUND_DEAD` were truncated and skipped their recovered response menus; and the keyword loop still had native speaker/portrait drift on the Loomis, Stephanie-midgame, lodge, and `0x98` topic continuations. Patched [`engines/harvester/npc/dwayne_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/dwayne_dialogue.cpp) to match those confirmed branches and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully. Progress counts remain aligned with the current live Ghidra totals (`912` functions, `120` remaining `FUN_*`).
- On March 23, 2026, reran the full durable `repair KARIN -> wait -> save_program -> verify KARIN` cycle against the live `HARVEST.LE` session to confirm Karin still survives cleanly with the current `InspectCallTermination.java` behavior. `handle_talk_to_karin` rebuilt to `0x2ea90..0x2ed7f` (`752` address-count, `21` dialogue calls, `14` repair actions) and the first post-save verify held without needing a retry: both dialogue entry points still verify as returning and `handle_talk_to_karin` reports `gapCount=0` / `status=clean` in the saved database. Progress is unchanged because Karin was already in the confirmed-clean set, but this revalidation confirms it is not currently regressing under the newer verifier logic.
- On March 22, 2026, repaired `DWAYNE` durably in the live `HARVEST.LE` Ghidra database with the current `InspectCallTermination.java` workflow. The first `repair DWAYNE` pass expanded `handle_talk_to_dwayne` from the short `0x3a310..0x3c385` body with `59` reachable dialogue calls to `185` calls, but it stalled on one real fallthrough hole at `0x3baa7 -> 0x3baac` where the raw bytes were valid code and Ghidra had misaligned the instruction stream. Cleared and re-disassembled `0x3baac..0x3bb0b`, which restored the missing fallthrough head as `MOV EDX,0xb8c8e`, then reran `repair DWAYNE`; that final pass rebuilt `handle_talk_to_dwayne` cleanly to `0x3a310..0x3c385 (8219 bytes)` with `186` reachable dialogue calls, `gapCount=0`, and both `play_dialogue_line` entry points marked returning. After a short settle, saved `HARVEST.LE` and re-ran `verify DWAYNE`; the fresh verify still reported `status=clean`, so Dwayne is now confirmed durable in the saved database. Progress counts remain aligned with the current live Ghidra totals (`912` functions, `120` remaining `FUN_*`).
- On March 22, 2026, reimplemented the remaining confirmed Johnson dialogue mismatches in ScummVM directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_johnson` (`0x27dd0..0x28802`). The confirmed source drift was concentrated in four native areas the earlier port had flattened or miswired: the Whaley-photo, casket-photo, and dual-ledger item branches were missing their native follow-up lines and PC speaker turns (`0xb7b`, `0xb80`, `0xb90`, `0xba4`, `0xba8`, plus the conditional `0xb94` / `0xb99` casket tail behind shared state `d2ebc`); the `OPENING_MANHOLE_DAYTIME` short-circuit was not clearing its runtime flag after `0xbe0`; the `SCRATCHED_TUCKER` re-entry was incorrectly dropping straight back into the intro instead of playing the native `0xc25` / `0xc2b` / `0xc30` sequence, preserving the re-armed intro gate for a later visit, and conditionally running Johnson's local `BOLT_OF_CLOTH_TAKEN` one-shot `0xc5f` / `0xc69`; and the `KARIN_KIDNAPED` branch was writing the wrong shared state, using `d2f04` instead of the real shared kidnapped-dialogue bit. Patched [`engines/harvester/npc/johnson_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/johnson_dialogue.cpp), added the missing Johnson-local save bit with versioned serialization in [`engines/harvester/npc/johnson_dialogue.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/johnson_dialogue.h), bumped Harvester save format version to `5` in [`engines/harvester/saveload.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/saveload.cpp), updated the confirmed Johnson notes in [`ARCHITECTURE.md`](/Users/alex/Workspace/scummvm/ARCHITECTURE.md), and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully. Progress counts remain aligned with the current live Ghidra database totals (`912` functions, `120` remaining `FUN_*`).
- On March 22, 2026, reimplemented the ScummVM Loomis dialogue handler directly against the live `HARVEST.LE` Ghidra bodies at `handle_talk_to_loomis` (`0x34f80..0x35826`) and `reset_talk_to_loomis_state` (`0x35870..0x3588d`). The confirmed source drift was concentrated in four native areas the earlier port had flattened: the no-item intro path was still treating `0x1112` as unconditional, substituting a nonexistent `0x111e` for native `0x1118`, and skipping response menu `0x53` plus Loomis `0x1130[3]`; revisit conversations were returning before the native keyword loop and therefore missed the `STEPH_MIDGAME_PLAYED` one-shot `0x1230[3]`, the day-5 bark `0x123c`, and the corrected `0x11b0[3]` follow-up; the keyword loop still used the wrong exit/fallback flow (`0x113a` instead of `0x11a1[1]`, no `0x11fc` unknown-topic bark, and no native aliases for `LOOMIS`, `Phelps`, or `Man's Needs`) along with the wrong portrait variants on `0x114f..0x1153`, `0x1162..0x1163`, `0x1170`, and `0x1196`; and the evidence / dirty-magazine item paths were missing the native response menu tails (`0x50 -> 0x1214/0x1219 -> 0x121f[3]` plus `PC_FRY_DADDY_1`) and the corrected `INV_MAG` response variants and room-state mutations. Patched [`engines/harvester/npc/loomis_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/loomis_dialogue.cpp) and [`engines/harvester/npc/loomis_dialogue.h`](/Users/alex/Workspace/scummvm/engines/harvester/npc/loomis_dialogue.h), and rebuilt [`scummvm`](/Users/alex/Workspace/scummvm/scummvm) successfully. Progress counts now reflect the current live Ghidra database totals (`912` functions, `120` remaining `FUN_*`).
- On March 22, 2026, reran `repair JOHNSON` with the current `InspectCallTermination.java` verifier in the live `HARVEST.LE` session. Johnson showed the same first-save snapback pattern seen on other retry-bucket NPCs: the first `repair -> wait -> save -> verify` cycle rebuilt `handle_talk_to_johnson` cleanly to `0x27dd0..0x28802` (`2539` address-count, `51` dialogue calls) but the fresh verify fell back to the short `0x27dd0..0x287f9` body with `12` stale dialogue gaps and both dialogue entry points marked non-returning again. An immediate second full cycle fixed it: the second `repair -> wait -> save -> verify` left Johnson durably clean again at `0x27dd0..0x28802` with `gapCount=0`, `play_dialogue_line` and `thunk_play_dialogue_line` both returning, and `status=clean`. This confirms Johnson belongs in the same post-save retry class as Stephanie, Mr. Potts, Loomis, Butcher, and similar handlers.
- On March 22, 2026, reran `repair LOOMIS` with the current `InspectCallTermination.java` verifier in the live `HARVEST.LE` session. Loomis showed the same first-save snapback pattern seen on other retry-bucket NPCs: the first `repair -> wait -> save -> verify` cycle rebuilt `handle_talk_to_loomis` cleanly to `0x34f80..0x35826` (`2071` address-count, `33` dialogue calls) but the fresh verify fell back to the short `0x34f80..0x35406` body with `9` stale dialogue gaps and both dialogue entry points marked non-returning again. An immediate second full cycle fixed it: the second `repair -> wait -> save -> verify` left Loomis durably clean again at `0x34f80..0x35826` with `gapCount=0`, `play_dialogue_line` and `thunk_play_dialogue_line` both returning, and `status=clean`. This confirms Loomis belongs in the same post-save retry class as Stephanie, Mr. Potts, Johnson, Butcher, and similar handlers.
- On March 22, 2026, reimplemented the remaining confirmed Mrs. Potts dialogue mismatches in ScummVM directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_mrs_potts` (`0x2ee70..0x2f7be`) plus its overlapping keyword-tail region at `FUN_0002f7fd` (`0x2f7d8..0x2fc78`). The confirmed source drift was concentrated in two native areas the earlier port had flattened: several visible `play_dialogue_line` calls still used the wrong portrait variants (`0x2726`, `0x281b`, `0x2891`, the `0x294d` evidence bark, the `0x293c..0x2945` Whaley photo sequence, and the `0x289e..0x28c1` remains branches), and the `DINER_BURNED` one-shot was missing the rest of the native continuation (`0x2914` behind `KARIN_HUNG`, `0x2918`, response menu `0x236`, `0x2926`, and the response-2 jump back into the shared death-in-chair tail). Patched [`engines/harvester/npc/mrs_potts_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/mrs_potts_dialogue.cpp), updated the confirmed Mrs. Potts notes in [`ARCHITECTURE.md`](/Users/alex/Workspace/scummvm/ARCHITECTURE.md), and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully. Progress counts remain unchanged at `923` total functions and `124` remaining `FUN_*`.
- On March 22, 2026, reran the full durable `repair MRS_POTTS -> wait -> save_program -> verify MRS_POTTS` cycle against the live `HARVEST.LE` session to confirm Mrs. Potts still survives cleanly with the current `InspectCallTermination.java` behavior. `handle_talk_to_mrs_potts` rebuilt to `0x2ee70..0x2fc81` (`2392` address-count during repair; fresh verify reports the saved body as `0x2ee70..0x2f7be` with `54` dialogue calls) and the first post-save verify held without needing a retry: both dialogue entry points still verify as returning and `handle_talk_to_mrs_potts` reports `gapCount=0` / `status=clean` in the saved database. Progress is unchanged because Mrs. Potts was already in the confirmed-clean set, but this revalidation confirms it is not currently regressing under the newer verifier logic.
- On March 22, 2026, reimplemented the ScummVM Mr. Potts dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_mr_potts` (`0x26000..0x27d07`). The confirmed source drift was concentrated in native-only control flow that the earlier port had collapsed: the `HARVEST_BLADE` interrupt was missing the response menu plus `MONSTERFY_POTTS` tail, the `STEPHANIE_DEAD_IN_HALL_JUST_OUTSIDE` interrupt was missing the full five-line exchange, `STEPH_MIDGAME_PLAYED` / `d2eb8` mutation, `C001B.FST`, and `STHLEXIT2B`, the `POTTSDAM_DIGGING` / `POTTSDAM_DIGGING_KARIN` branches were still truncated, every presented-item path was incorrectly falling through into the no-item conversation, the later-day Stephanie-alive revisit had the native day-2 through day-5 `0x2ae5` gate inverted, the `KARIN_KIDNAPED` response-2 path was checking `DAY_6` instead of `STEPH_MIDGAME_PLAYED`, the `KARIN_FOUND_DEAD` follow-up at `0x2b8d..0x2bb6` still used the wrong flag/speakers, and the hidden keyword-loop rewrites and response menus for `AMNESIA`, `MARRIAGE`, `COMMITMENT`, `FATHER`, `SLAUGHTERHOUSE`, `MEAT`, `STEPHANIE`, `LODGE`, `NEWSPAPER`, `MOYNAHAN`, `TOILET`, and `MOLEST` were still over-simplified. Patched [`engines/harvester/npc/mr_potts_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/mr_potts_dialogue.cpp) to match those confirmed native branches and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully. Progress counts remain unchanged at `923` total functions and `124` remaining `FUN_*`.
- On March 22, 2026, reran `repair MR_POTTS` with the current `InspectCallTermination.java` verifier in the live `HARVEST.LE` session. Mr. Potts showed the same first-save snapback pattern seen on other retry-bucket NPCs: the first `repair -> wait -> save -> verify` cycle rebuilt `handle_talk_to_mr_potts` cleanly to `0x26000..0x27d07` (`6853` address-count, `152` dialogue calls) but the fresh verify fell back to the short `0x26000..0x26915` body with `15` stale dialogue gaps and both dialogue entry points marked non-returning again. An immediate second full cycle fixed it: the second `repair -> wait -> save -> verify` left Mr. Potts durably clean again at `0x26000..0x27d07` with `gapCount=0`, `play_dialogue_line` and `thunk_play_dialogue_line` both returning, and `status=clean`. This confirms Mr. Potts belongs in the same post-save retry class as Stephanie, Johnson, Butcher, Dad, and similar handlers.
- On March 22, 2026, reimplemented the ScummVM Jimmy dialogue handler directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_jimmy` (`0x28880..0x28ec1`). The confirmed source drift was all in native control-flow details: the first paper-handoff gate was missing `PC 0x4a52`, later no-item visits with `SNEAKERS` but no `BROOMKEY` were missing the native response menu `0xf8` and shared `0x4a86` / `0x4a8a` / `0x4a8e` branch, direct `SNEAKERS` use failed to return the shoes to `RAH` and award `BROOMKEY`, and the `0x4b00` corpse/ledger plus `0x4b21` blackmail evidence branches had been collapsed to single-line barks instead of the native multi-line exchanges. Patched [`engines/harvester/npc/jimmy_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/jimmy_dialogue.cpp), updated the confirmed Jimmy notes in [`ARCHITECTURE.md`](/Users/alex/Workspace/scummvm/ARCHITECTURE.md), and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully. Progress counts are unchanged at `923` total functions and `124` remaining `FUN_*`.
- On March 22, 2026, reran the full durable `repair JIMMY -> wait -> save_program -> verify JIMMY` cycle against the live `HARVEST.LE` session to confirm Jimmy still survives cleanly with the current `InspectCallTermination.java` behavior. `handle_talk_to_jimmy` rebuilt to `0x28880..0x28ec1` (`1602` address-count, `39` dialogue calls, `27` repair actions) and the first post-save verify held without needing a retry: both dialogue entry points still verify as returning and `handle_talk_to_jimmy` reports `gapCount=0` / `status=clean` in the saved database. Progress is unchanged because Jimmy was already in the confirmed-clean set, but this revalidation confirms it is not currently regressing under the newer verifier logic.
- On March 22, 2026, reimplemented the remaining confirmed Mom dialogue mismatches in ScummVM directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_mom` (`0x31140..0x33523`). Raw instruction windows over the native item and hidden-topic tails confirmed that two evidence branches were still over-ported (`CASKET_PHOTO` is only `PC 0x233b`; `PHOTO_OF_WHALEY_HERRILL` is only `MOM 0x239c`), `JIMMY_ATTACKED` uses `PC 0x265f` with head variant `2`, the `POTTSDAM` second-response branch uses `PC 0x20e3` rather than Mom, the `MEAT PLANT` first response is `MOM 0x2106 -> PC 0x210a -> MOM 0x210e`, the `FATHER` keyword sets the native `momFatherTopicState`, and the fallback `0x26c6` path loops back into the keyword menu instead of forcing an immediate goodbye. Patched [`engines/harvester/npc/mom_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/mom_dialogue.cpp) to match those confirmed native branches and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully. Progress counts now reflect the current live Ghidra database totals (`923` functions, `124` remaining `FUN_*`).
- On March 22, 2026, reran the full durable `repair MOM -> wait -> save_program -> verify MOM` cycle against the live `HARVEST.LE` session to confirm Mom still survives cleanly with the current `InspectCallTermination.java` behavior. `handle_talk_to_mom` rebuilt to `0x31140..0x33523` (`8778` address-count, `251` dialogue calls, `116` repair actions) and the first post-save verify held without needing a retry: both dialogue entry points still verify as returning and `handle_talk_to_mom` reports `gapCount=0` / `status=clean` in the saved database. Progress is unchanged because Mom was already in the confirmed-clean set, but this revalidation confirms it is not currently regressing under the newer verifier logic.
- On March 22, 2026, reran the full durable `repair HANK -> wait -> save_program -> verify HANK` cycle against the live `HARVEST.LE` session to confirm Hank still survives cleanly with the current `InspectCallTermination.java` behavior. `handle_talk_to_hank` rebuilt to `0x33a30..0x34bcb` (`4508` address-count, `131` dialogue calls, `58` repair actions) and the first post-save verify held without needing a retry: both dialogue entry points still verify as returning and `handle_talk_to_hank` reports `gapCount=0` / `status=clean` in the saved database. Progress is unchanged because Hank was already in the confirmed-clean set, but this revalidation confirms it is not currently regressing under the newer verifier logic.
- On March 22, 2026, rechecked `handle_talk_to_sergeant` in the live `HARVEST.LE` Ghidra body at `0x30340..0x30fd2` and found two native continuations still missing from ScummVM despite the earlier broad Sergeant port. The confirmed gaps were the post-`DINER_BURNED` second response menu at `0x2b0` (`0x42de` / `0x42e3` / `0x42e9` / `0x42ef` / `0x42f3`) plus the room-state mutations that re-show `INVITE` and `STEFSKULL`, reactivate `DWAYNE_ST_BEDRM`, and set `STEPHANIE_IS_DEAD` / `STEPHANIE_DEAD_IN_HALL_JUST_OUTSIDE`, and the post-`BARBER_POLE` continuation at `0x4299` with response menu `0x2b2` and the optional `PC_KILLED_ANYONE` tail `0x42a4 -> 0x42a8` / `0x42ad`. Patched `engines/harvester/npc/sergeant_dialogue.cpp` to match those branches, updated `ARCHITECTURE.md`, and rebuilt the Harvester debug target successfully. Progress counts reflect the current Ghidra database totals (`920` functions, `122` remaining `FUN_*`).
- On March 22, 2026, reran the full durable `repair SERGEANT -> wait -> save_program -> verify SERGEANT` cycle against the live `HARVEST.LE` session to confirm Sergeant still survives cleanly with the updated verifier and current save behavior. `handle_talk_to_sergeant` rebuilt to `0x30340..0x30fd2` (`3219` address-count, `70` dialogue calls, `27` repair actions) and the first post-save verify held without needing a retry: both `play_dialogue_line` entry points still verify as returning and `handle_talk_to_sergeant` reports `gapCount=0` / `status=clean` in the saved database. Progress is unchanged because Sergeant was already in the confirmed-clean set, but this revalidation confirms it is not currently regressing under the newer `InspectCallTermination.java` logic.
- On March 22, 2026, reimplemented the remaining Stephanie dialogue mismatches in ScummVM directly against the live `HARVEST.LE` Ghidra body at `handle_talk_to_stephanie` (`0x36710..0x37f5d`). A forced redecompile plus spot disassembly confirmed the still-wrong native details in the current port: intro variants `0x43eb -> STEPHANIE[2]` and `0x43f5 -> STEPHANIE[3]`, item/evidence variants `0x4684 -> STEPHANIE[2]` and `0x469e -> STEPHANIE[2]`, first-followup tail `0x45d8 -> STEPHANIE[2]` / `0x45e2 -> STEPHANIE[3]`, second-followup variants on `0x460e`, `0x4612`, `0x4616`, `0x461b`, `0x4649`, and `0x464e`, the nested `SCRATCHED_TUCKER` reply `0x4709 -> PC[2]`, and the Mrs. Potts alibi tail `0x4989 -> STEPHANIE[0]` rather than PC. The refreshed late-tail listing around `0x377a3..0x37820` also resolved the Karin-alive control flow: after `0x49ba`, Stephanie only reaches `0x49c1` plus the shared `0x49ea/0x49ef` return when the Dwayne Karin-alive follow-up gate is set; otherwise the conversation falls back into the keyword menu immediately. Patched [`engines/harvester/npc/stephanie_dialogue.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/npc/stephanie_dialogue.cpp) to match those native branches and rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully.
- On March 22, 2026, reran `repair STEPHANIE` with the updated `InspectCallTermination.java` verifier in the live `HARVEST.LE` session. Stephanie showed the same first-save snapback pattern seen on other retry-bucket NPCs: the first `repair -> wait -> save -> verify` cycle rebuilt `handle_talk_to_stephanie` cleanly to `0x36710..0x37f5d` (`6166` address-count, `195` dialogue calls) but the fresh verify fell back to the short `0x36710..0x36cd8` body with `9` stale dialogue gaps and both dialogue entry points marked non-returning again. An immediate second full cycle fixed it: the second `repair -> wait -> save -> verify` left Stephanie durably clean again at `0x36710..0x37f5d` with `gapCount=0`, `play_dialogue_line` and `thunk_play_dialogue_line` both returning, and `status=clean`. This confirms Stephanie belongs in the same post-save retry class as Johnson, Butcher, Dad, Loomis, and similar handlers.
- On March 22, 2026, closed out the stubborn `HERRILL` case. The saved `HARVEST.LE` database already held the widened `handle_talk_to_herrill` body (`0x2dc00..0x2e987`, `1755` address-count) from the earlier repair/save pass, but `InspectCallTermination.java` kept falsely flagging `0x2dda1` as a missing-fallthrough dialogue call. Direct inspection showed the live listing at `0x2dda1` is actually `JMP 0x2e97a` with stale `CALL_RETURN` metadata, so the real remaining defect was verifier-side rather than a truncated handler body. Patched `~/ghidra_scripts/InspectCallTermination.java` so tail `JMP` transfers to `play_dialogue_line` / `thunk_play_dialogue_line` are counted as terminal dialogue transfers instead of bogus repair gaps; a fresh `verify HERRILL` now reports `status=clean` with `dialogueCalls=39` and `gapCount=0`. This raises the current durable/confirmed sweep count to `40/48`.
- On March 22, 2026, resumed the one-at-a-time `InspectCallTermination.java` NPC sweep against the live `HARVEST.LE` session using the durable workflow `repair <NPC> -> wait -> save_program -> fresh verify <NPC>`. Including the already-confirmed earlier `STEPHANIE` repair in the same saved database, this pass has now confirmed the following talk handlers as durably clean: `WHALEY`, `MOYNAHAN`, `MR_POTTS`, `JOHNSON`, `JIMMY`, `LIBRARIAN`, `SWELL`, `PRIEST`, `PHELPS`, `BUTCHER`, `MADAM`, `INQUISITOR`, `DAD`, `MOTHER`, `MAINT_MAN`, `AUTHORITY`, `BUSTER`, `GLADIATOR`, `MCKNIGHT`, `BOYLE`, `CHESSMASTER`, `NUDE_MAN`, `CLOAK_ATND`, `KARIN`, `VALET`, `MRS_POTTS`, `VET`, `WASP_WOMAN`, `SERGEANT`, `MOM`, `DARK_WOMAN`, `FIREMAN1`, `FIREMAN2`, `HANK`, `CURATOR`, `BEGGAR`, `PTA_MOM`, `LOOMIS`, and `STEPHANIE` (`39/48`). The sweep also exposed three real outlier patterns beyond the simple retry-after-save snapback: missing immediate fallthrough instructions that require clearing/disassembling the exact continuation address before rerunning repair (seen in Boyle and Wasp Woman), tiny stray tail functions that must be deleted so the main talk handler can absorb the last continuation block (seen in Mom), and a save-time reappearing `CALL_RETURN` override that still leaves `handle_talk_to_herrill` one call short even after direct clearing at `0x2dda1`. The current active retry is `EDNA`, which reverted to its short body after the first save/verify cycle and still needs the standard second repair pass.
- On March 22, 2026, reran `repair STEPHANIE` through the live `HARVEST.LE` Ghidra session with `~/ghidra_scripts/InspectCallTermination.java`, then immediately called `save_program` and re-ran `verify STEPHANIE` in a fresh script invocation. The repair converged in two passes, expanding `handle_talk_to_stephanie` from `0x20e` bytes (`0x36710..0x36cd8`) with `9` fallthrough gaps to a clean `0x1816`-byte body (`0x36710..0x37f5d`) with `195` reachable dialogue calls, `0` remaining gaps, and `27` repair actions. The explicit save succeeded and the fresh verify still reported `status=clean`, so Stephanie's repaired handler is now confirmed durable in the saved `HARVEST.LE` database.
- On March 22, 2026, attempted to run `repair STEPHANIE` again with `~/ghidra_scripts/InspectCallTermination.java`, but the live Ghidra plugin server was still wedged: the bridge layer was gone after cleanup, direct HTTP probes to `http://127.0.0.1:8089/check_connection` timed out even though the Java process still owned the port, and the fallback headless check showed the only on-disk project at `~/.ghidra-projects/harvest_prelim` contains `HARVEST.EXE` rather than the active `HARVEST.LE` program database. No Stephanie repair was applied in this attempt because there is currently no responsive path into the `HARVEST.LE` state without restarting the live Ghidra/plugin session.
- On March 22, 2026, reworked `~/ghidra_scripts/InspectCallTermination.java` again to debug why per-NPC `repair` runs looked successful but reverted after later saves. Confirmed the original nested/manual transaction model only preserved repairs transiently inside the active script invocation. Switched the script to end the runner-owned transaction up front, use an explicit per-handler program transaction inside `repairHandler`, and keep the `repair <NPC>` / `verify <NPC>` argv workflow intact. With that model, `repair JIMMY` followed by a fresh `verify JIMMY` now survives across separate script invocations in the active `HARVEST.LE` session. The remaining blocker is durable save of dirty handlers: once a handler actually changes, both in-script `saveProgram()` and MCP `save_program` still hit Ghidra-side locking/analysis problems (`Unable to lock due to active transaction`, then `AutoAnalysisManager.scheduleWorker may not be invoked from Swing thread`), and the final dirty-save experiment left the MCP/Ghidra session unresponsive. Do not trust `repairrange` for persistence; the only confirmed good path so far is single-NPC repair/verify while the session stays alive.
- On March 22, 2026, rechecked Harvester's native room-combat melee gating in Ghidra after ScummVM still logged `reason='no native attack bank'` for Johnson's `MRJ_M`. Confirmed from `update_actor_runtime_state` (`0x4d750`) that the native class-6 attack picker is state-driven and side-based, then verified from the shipped XFILE archives that `GRAPHIC/MONSTERS/MRJ.ABM` actually contains `296` frames, so ScummVM's failure was local rather than asset-driven. Patched [`engines/harvester/room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) and [`engines/harvester/runtime_entity.h`](/Users/alex/Workspace/scummvm/engines/harvester/runtime_entity.h) so monster attack/death bank checks validate against the full ABM frame count instead of the entity's currently active animation subrange. Rebuilt [`build-vscode-harvester-debug/scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm) successfully.
- On March 22, 2026, re-audited Harvester's native save/load dialogue tail in Ghidra instead of relying on the earlier three-block summary. Confirmed `run_save_game_menu` / `run_load_game_menu` serialize the full post-`TIMER` talk-state inventory, including the standalone `g_serialized_talk_state_flag_d2cd8 @ 0xd2cd8`, the shared dialogue dword range `0xd2e98..0xd2f30`, and the dedicated NPC-local blocks for Boyle, Dwayne, Loomis, Mom, Hank, Dad, Mr. Potts aux/main, Stephanie, Johnson, Swell, Parsons, Mcknight, Edna, Moynahan, Phelps, Herrill, Wasp Woman, Sparky, Fireman 1/2, Nude Man, Butcher, Ryder, Buster, Dark Woman, Memb Dir, Mother, Mrs. Potts, and Inquisitor.
- On March 22, 2026, patched ScummVM's Harvester save schema again so the dialogue blob carries the outer save version, allowing new dialogue-state fields to be appended without breaking version-3 blobs. Added the unresolved native shared-talk slots still present in `0xd2cd8` and `0xd2e98..0xd2f30` to `DialogueSharedState`, bumped the Harvester save version to `4`, and rebuilt `build-vscode-harvester-debug/scummvm` successfully.
- On March 22, 2026, ran `repair AUTHORITY` through the generalized `InspectCallTermination.java` workflow and re-verified the saved `HARVEST.LE` state. The script cleared Authority's `2` stale dialogue-call fallthrough gaps in one pass, expanding `handle_talk_to_authority` from `0x5f` bytes (`0x2b730..0x2b7b2`) to a clean `0x83`-byte body with `3` reachable dialogue calls and `0` remaining gaps while keeping both `play_dialogue_line` entry points marked returning.
- On March 22, 2026, ran `repair JIMMY` through the generalized `InspectCallTermination.java` workflow and re-verified the result in the saved `HARVEST.LE` database. The script cleared Jimmy's `13` stale dialogue-call fallthrough gaps in one pass, expanding `handle_talk_to_jimmy` from `0x327` bytes (`0x28880..0x28eb9`) to a clean `0x642`-byte body (`0x28880..0x28ec1`) with `39` reachable dialogue calls and `0` remaining gaps while keeping both `play_dialogue_line` entry points marked returning.
- On March 22, 2026, batch-verified all `48` named `handle_talk_to_*` entries with the repaired `InspectCallTermination.java` workflow. The current active `HARVEST.LE` state now reports `play_dialogue_line` and `thunk_play_dialogue_line` as returning, `handle_talk_to_stephanie` as the only fully clean talk handler, and `47` remaining handlers still needing iterative fallthrough repair. The heaviest confirmed remaining truncation clusters are `handle_talk_to_mom` (`61` gaps), `handle_talk_to_dwayne` (`59` gaps), and `handle_talk_to_hank` (`28` gaps).
- On March 22, 2026, repaired `~/ghidra_scripts/InspectCallTermination.java` so it now accepts argv-driven NPC commands (`list`, `verify <NPC>`, `repair <NPC>`, plus `verify ALL`) instead of being hardwired to one handler and failing to load under Ghidra's Java script provider. The real loader breakage was a bad `FlowOverride` import; after correcting that and switching the rebuild step back to GhidraScript's auto-body `createFunction(entry, name)` helper, the script now validates the immediate fallthrough byte after every `play_dialogue_line` / `thunk_play_dialogue_line` call, confirms both dialogue callees are marked returning, clears stale call overrides, disassembles newly reachable fallthrough bytes, and iteratively recreates the selected `handle_talk_to_*` body until the interaction graph is clean.
- On March 22, 2026, verified the generalized script live against multiple NPCs in `HARVEST.LE`: `verify HANK` now reports Hank's stale `CALL_RETURN` dialogue exits generically, while `repair STEPHANIE` completed in two passes, expanding `handle_talk_to_stephanie` from `0x20e` bytes (`0x36710..0x36cd8`) to a clean `0x1816`-byte body (`0x36710..0x37f5d`) with `195` reachable dialogue calls and `0` remaining fallthrough gaps. Saved the updated `HARVEST.LE` database after the successful Stephanie repair.
- On March 22, 2026, rechecked Harvester's native room-combat monster attack path in Ghidra after ScummVM monsters could pursue but kept logging `reason='out of range'` at contact. Confirmed from `update_actor_runtime_state` (`0x4d750`) that class-6 monsters arm melee by storing `g_player_combat_avatar` into live actor field `+0x11a4`, pick a random side-appropriate close-range attack family (`0x16..0x18` on the left, `0x19..0x1b` on the right) instead of using a target-Y band mapping, and resolve contact against that linked target after the `+0x113c` contact frame instead of re-running the coarse engage-distance spacing test. Patched [`room.cpp`](/Users/alex/Workspace/scummvm/engines/harvester/room.cpp) to retain the scheduled player target through the swing, resolve monster contact once against that linked target, and randomize native-style left/right attack bank selection from the ABM frame ranges. Verified by rebuilding [`room.o`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/engines/harvester/room.o) and relinking [`scummvm`](/Users/alex/Workspace/scummvm/build-vscode-harvester-debug/scummvm).
- On March 22, 2026, audited native save/load persistence in Ghidra before changing the engine-side dialogue save schema. `run_save_game_menu` / `run_load_game_menu` restore dedicated talk-state blocks in addition to the world/script records, including Sergeant's `0x28`-byte local block at `g_sergeant_talk_state_block @ 0xd2d4c`, Stephanie's `0x4c`-byte block at `g_stephanie_talk_state_block @ 0xd2e4c`, and the shared discussion / follow-up block at `0xd2e98` with size `0x9c`.
- On March 22, 2026, patched ScummVM's Harvester save/load path to preserve and reload those dialogue branches. `HarvesterEngine` now carries a pending dialogue-state blob for launcher/runtime loads, `Flow` serializes and restores the active `DialogueSystem`, `DialogueSystem` syncs shared plus per-handler local talk state, and room-load handoff reapplies the blob immediately after the normal dialogue reset. Bumped the Harvester save version to `3` and rebuilt `build-vscode-harvester-debug/scummvm` successfully.
- On March 22, 2026, re-opened Sergeant's `SCRATCHED_TUCKER` no-item branch against the shipped `DIALOGUE.IDX` and `DIALOG.RSP` assets after ScummVM stopped at Sergeant line `0x420d` without the expected Day 2 response menu.
- Confirmed the missing continuation is asset-backed: Sergeant should continue with `0x421e`, open zero-based `dialog.rsp` line `0x2b4`, and then branch to `0x422a` or `0x422f` / `0x4239`. Patched `engines/harvester/npc/sergeant_dialogue.cpp`, updated the Sergeant dialogue debug dump and `ARCHITECTURE.md`, and rebuilt `build-vscode-harvester-debug/scummvm` successfully.
- On March 22, 2026, cleaned up the native Sergeant room handler in Ghidra by rebuilding `handle_talk_to_sergeant` at `0x30340` from a broken one-byte stub into its real front-half body, setting an explicit `void __stdcall` prototype, and renaming the Sergeant-local `0xd2d50..0xd2d70` state slots to stable labels. The only remaining decompiler noise in that front half is the known register-lift artifact on `ASSIGNED_DNA_TASK` and the odd extra register shown on the `BOLTCLTH` branch.
- On March 22, 2026, recovered the remaining in-line Sergeant continuation heads from native control flow instead of raw asset chasing: labeled and commented `sergeant_completed_application_followup` (`0x30c42`), `sergeant_intro_or_revisit_dispatch` (`0x30d7e`), `sergeant_first_contact_intro_sequence` (`0x30d8b`), `sergeant_revisit_reminder_sequence` (`0x30eff`), and the shared `sergeant_day5_exit_check` / `sergeant_day5_exit_branch` (`0x30f99` / `0x30fa7`). The plate comment on `handle_talk_to_sergeant` now points directly at those continuation blocks.
- On March 22, 2026, reapplied the recovered Sergeant `SCRATCHED_TUCKER` continuation to ScummVM: after the `0x2b4` response menu, both the `0x422a` accept tail and the `0x422f` / `0x4239` pushback tail now fall through a one-shot local `dialogueStateD2d58` gate that dispatches `GO_DAY_3`, matching the native `0x30b84..0x30ba6` action-tag handoff. Updated `engines/harvester/npc/sergeant_dialogue.cpp`, `engines/harvester/dialogue_debug.cpp`, `ARCHITECTURE.md`, and rebuilt `build-vscode-harvester-debug/scummvm` successfully.
- On March 22, 2026, added a batch Ghidra audit script at `~/ghidra_scripts/RecoverTalkHandlerReachability.java` to walk `g_talk_to_handler_entries` in one pass, verify every NPC talk-handler pointer resolves to a real function entry, flag any table target that lands inside an existing function instead of at its entry point, and scan each recovered `handle_talk_to_*` body for off-function direct `CALL` or `JMP` targets. Ran the script against `HARVEST.LE` in report mode and confirmed the current database is clean: `48` table handlers, `0` missing table targets, `0` mid-function table targets, `0` missing call targets, and `0` missing jump-only continuation blocks.
- On March 22, 2026, confirmed the remaining orphan NPC talk-handler code is not a bad `g_talk_to_handler_entries` pointer-table issue. The real failure mode is Ghidra analysis around `play_dialogue_line`: many talk handlers had dialogue call sites incorrectly marked `CALL_RETURN`, and `play_dialogue_line` itself kept being rediscovered as `noreturn`, which truncated handler bodies and left the next code blocks unmapped. Added `~/ghidra_scripts/AuditTalkHandlerDialogueFallthroughs.java` and `~/ghidra_scripts/InspectCallTermination.java` to prove the pattern and verify call-site state.
- On March 22, 2026, re-ran the working manual recovery sequence on live handlers: force `play_dialogue_line` back to returning, clear the bad flow overrides at the affected dialogue call sites, disassemble the fallthrough bytes immediately after those calls, then delete/recreate the handler entry. That sequence cleanly reattached Sergeant's `0x30b28` continuation back under `handle_talk_to_sergeant`, and also expanded `handle_talk_to_stephanie` from its truncated `0x526`-byte body to a wider `0x668`-byte body. The problem is iterative rather than one-shot: once additional continuation blocks are absorbed, newly reachable `play_dialogue_line` sites inside those blocks need the same treatment on the next pass.
- On March 22, 2026, pushed the iterative Sergeant recovery through two more dialogue-call waves. `play_dialogue_line` and `thunk_play_dialogue_line` now remain marked returning in the active database, `handle_talk_to_sergeant` rebuilt cleanly to a `0x8a1`-byte body (`0x30340..0x30fd2`), and the decompile now reaches the native `SCRATCHED_TUCKER` Day 3 handoff (`dispatch_action_tag_if_set("GO_DAY_3")`) plus the shared Day 5 exit gate at `0x30f99`. The remaining `CALL_RETURN` sites visible in the Sergeant handler are now narrow tail cases rather than the missing mid-function continuations that were previously orphaning the core logic.
- On March 22, 2026, checked the unmapped bytes immediately after Sergeant's terminal `RET` at `0x30fd2`. That region is recoverable, but it is not additional `handle_talk_to_sergeant` continuation logic: the bytes starting at `0x30fe0`, `0x31000`, `0x31020`, `0x31040`, `0x31060`, `0x31080`, `0x310a0`, and `0x310c0` form a compact cluster of standalone Sergeant-local getter/setter helpers over globals `0xd2d4c..0xd2d6c`, using the same `if (param_1 == 0) global = param_2; return global;` pattern as the already-defined `get_set_sergeant_*` wrappers elsewhere in the binary. The hard `RET` at `0x30fd2` is therefore a real function end for the talk handler.
- On March 22, 2026, defined the post-`0x30fd2` Sergeant accessor cluster as standalone functions instead of leaving it as unmapped code: `get_set_sergeant_talk_state_block` (`0x30fe0`), `get_set_sergeant_dialogue_state_d2d50` (`0x31000`), `get_set_sergeant_post_application_briefing_completed` (`0x31020`), `get_set_sergeant_dialogue_state_d2d58` (`0x31040`), `get_set_sergeant_dialogue_state_d2d5c` (`0x31060`), `get_set_sergeant_dialogue_state_d2d60` (`0x31080`), `get_set_sergeant_dialogue_state_d2d64` (`0x310a0`), `get_set_sergeant_dialogue_state_d2d68` (`0x310c0`), and `get_set_sergeant_dialogue_state_d2d6c` (`0x310e0`). Applied matching two-parameter `get/set` prototypes and removed the stale temporary labels that overlapped the new helper bodies.
- On March 22, 2026, reimplemented `engines/harvester/npc/sergeant_dialogue.cpp` against the current recovered native Sergeant state instead of the earlier partial stub logic. The engine now mirrors the helper-backed local state block (`d2d4c`, `d2d50`, `d2d54`, `d2d58`, `d2d5c`, `d2d60`, `d2d64`, `d2d68`, `d2d6c`, `d2d70`), uses the recovered menu/FST tails for `REMAINS`, `INVITE`, `DINER_BURNED`, `BARBER_POLE`, `BOLTCLTH`, `SCRATCHED_TUCKER`, and the intro/revisit/application branches, and carries over the native Day 5 exit gate and `BOLTCLTH` slash-transition side effects. Verified by building `engines/harvester/npc/sergeant_dialogue.o` and `engines/harvester/dialogue.o` with `make`.
- On March 22, 2026, re-ran the Stephanie talk-handler recovery using the stricter `InspectCallTermination` approach that checks the exact fallthrough byte after each `play_dialogue_line` call instead of relying on `inst.getNext()`. Clearing those stale `CALL_RETURN` overrides and disassembling the immediate fallthrough bytes recovered the Stephanie handler far enough to confirm the day-five, `BURNED_TV_STATION`, `SCRATCHED_TUCKER`, `BOLT_OF_CLOTH_TAKEN`, `BARBER_POLE_STOLEN`, `DINER_BURNED`, jail, and keyword-menu branch layouts plus the small orphan Stephanie continuations at `0x37650` and `0x37e78`.
- On March 22, 2026, patched `engines/harvester/npc/stephanie_dialogue.cpp` against those confirmed native Stephanie paths: corrected the recovered Stephanie/PC head variants that were still hardcoded as `0`, removed the bogus unconditional `0x4717` tail after the nested `SCRATCHED_TUCKER` reply, and restored the native early-return behavior on the `BOLT_OF_CLOTH_TAKEN`, `BARBER_POLE_STOLEN`, `DINER_BURNED`, and `0x2ed` keyword-response exits. Rebuilt `build-vscode-harvester-debug/scummvm` successfully.

## Next Suggested Action

- Immediate: run an in-engine WASP_WOMAN validation pass that explicitly covers the restored native-only branches: first no-item talk before and after Mom's topic `0x121` sets the shared follow-up gate so the optional `PC 0x4bf2[0]` insert is confirmed; topic `0x302` to verify `C027A.FST` and the rewrite to `dialog.rsp[0x304]`; topic `0x305` with the Mom follow-up gate both clear and set to verify the unconditional `C027B.FST` plus the gated `dialog.rsp[0x307]` menu; topics `0x308`, `0x30a`, and `0x30c` to confirm the rewrites to `dialog.rsp[0x309]`, `dialog.rsp[0x30b]`, and `dialog.rsp[0x30d]`; the initial exit topic to confirm the conversation ends cleanly; and the late `0x30e` plus an unrelated typed topic to verify `0x30e` loops silently while the unmatched late-topic path now plays `0x4c9a[0]` and stays in the conversation. Also present `PHOTO_OF_WHALEY_HERRILL`, `CASKET_PHOTO`, and note/checkbook evidence, then immediately visit one downstream consumer of the shared evidence ints to confirm the restored `discussed*` writes propagate beyond Wasp Woman's local startup-script flags.
- Immediate: run an in-engine VET validation pass that explicitly triggers `VET_INTERRUPT`, confirms the restored `0x6d9[0] -> PC 0x447[0] -> 0x6e1[2]` opener, both `dialog.rsp[0x2ff]` answers (`0x6ec[2]` and `0x6f0[0]`), menu dismissal falling straight through to the shared `0x6f4[2]` exit, and a second talk proving the startup flag was cleared so the interrupt exchange no longer replays.
- Immediate: run an in-engine VALET validation pass that explicitly covers the now-confirmed native-only sequence: enter the conversation empty-handed to confirm the four-line opener `0xf2c[1] -> PC 0xf31[0] -> 0xf35[0] -> PC 0xf39[0]`, the `MIDGET02.FST` playback, response menu `0x2fe`, response `1` producing `0xf4a[0]`, response `2` producing `0xf50[2]`, and menu dismissal falling straight through to the shared exit line `0xf56[0]` with no extra bark or item-specific branch.
- Immediate: rerun a focused in-engine Swell validation pass that checks the two follow-up corrections from this pass: after presenting `PHOTO_OF_WHALEY_HERRILL`, `CASKET_PHOTO` / `CASKET_PHOTOCOPY`, both ledgers with `HAVE_BOTH_LEDGERS`, and note/checkbook evidence to Swell, immediately visit at least one downstream consumer that reads the shared evidence-state ints (for example Dwayne, Moynahan, or Karin/Herrill paths already known to key off those discussion markers) to confirm the restored shared-state writes now propagate beyond the local startup-script flags; and in the keyword menu verify that only native topic `dialog.rsp[0x21e]` exits with `0xff1[1]` while `0x229` remains a silent continue and an unrelated typed topic still falls back to `0xfba[2]`.
- Immediate: run an in-engine PTA_MOM validation pass that explicitly covers the corrected response-`1` portrait tail and the existing native branches: trigger `PTA_RESPOND_TO_TV` once to confirm the early `0x3233[2]` bark clears the flag and skips `dialog.rsp[0x297]`; then talk repeatedly without the flag until each opener `0x31ee[1]`, `0x31f2[1]`, and `0x31f6[1]` has appeared at least once, taking response `1` to confirm `0x3204[2] -> 0x320b[0]`, response `2` to confirm `0x320f[0] -> 0x3213[0]`, response `3` to confirm `0x3217[0] -> 0x321c[0] -> PC 0x3221[0] -> 0x3226[0]`, and dismissing the menu once to verify the native handler returns silently with no fallback bark.

- Immediate: run an in-engine Priest validation pass that explicitly hits the recovered native-only branches: first talk with response `2` at menu `0x27a`; then separate fresh runs taking `1 -> 2` at `0x27b`, `1 -> 1` at `0x27c`, and `1 -> 2` at `0x27d` to confirm all four early `0xcc4[2] -> FOLOWR_MNSTFY_A` exits; plus a full `1 -> 1 -> 2 -> 1` descent to menu `0x27e`, taking response `4` once to verify `0xd0e[1]`, `PRIEST_FLAG` clear, and `RELIG_EXITS_A`, and taking any non-`4` response once to verify `0xd13[3]`, `PRIEST_FLAG` set, and `FOLOWR_MNSTFY_A`. Present one arbitrary inventory item as well to confirm the native handler ignores used objects.

- Immediate: run an in-engine MEMB_DIR validation pass that explicitly covers the recovered native-only branches: talk before `PC_KILLED_KEWPIE`, take both `dialog.rsp[0xfe]` answers to confirm `0x2c2[0]` versus `0x2cb[0]`, and dismiss the menu once to verify the handler returns silently without a fallback bark; then talk after `PC_KILLED_KEWPIE` with fresh local state to confirm the one-shot `0x2d5[1]` prelude plus `TOOK_THE_DOLL -> 0x2e3[0]` versus no-doll `0x2ea[0]`, and repeat once more to confirm the local post-kill gate suppresses the branch on revisit.
- Immediate: run an in-engine MCKNIGHT validation pass that explicitly covers the restored native-only branches: `LOOK_SAFE_1ST` twice to confirm `0x15c9[2]` is now one-shot, `LOOK_SAFE_2ND` to confirm `0x15d0[2]`, a first empty-hand talk to confirm the full `0x1587 -> 0x159d` six-line exchange plus the shared `d2f04` write, a repeat empty-hand talk to confirm the fallback `0x15e4`, `CASKET_PHOTO` / `CASKET_PHOTOCOPY` with shared state `d2ebc` both clear and set to verify the `0x1605 -> 0x1609` versus `0x15f8 -> 0x15fd` follow-ups, note/checkbook evidence to confirm the restored five-line `0x161b -> 0x1631` sequence, and one arbitrary wrong item to confirm the handler still falls back to `0x15de`.
- Immediate: run an in-engine Swell validation pass that explicitly covers the recovered native-only flow: first empty-hand talk to confirm the full `0xf61[1] -> PC 0xf65 -> 0xf69[1] -> PC 0x3d50 -> 0xf72[1] -> 0xf7f[0]` intro and the seeded `dialog.rsp[0x21d]` topic buffer; a repeat empty-hand talk to confirm the default `0xfb3[0]` bark plus one-shot follow-ups for `STEPH_MIDGAME_PLAYED`, `BARBER_POLE_STOLEN`, `BOLT_OF_CLOTH_TAKEN`, `BURNED_TV_STATION`, and `KARIN_KIDNAPED`; `PHOTO_OF_WHALEY_HERRILL`, `CASKET_PHOTO` / `CASKET_PHOTOCOPY`, both ledgers with `HAVE_BOTH_LEDGERS`, and note/checkbook evidence to verify the restored `0xfc6[1]`, `0xfcd[0]`, `0xfd4 -> PC 0xfd9 -> 0xfde`, and `0xfe6[1]` branches; and the keyword loop through `0x21e`, `0x21f/0x220`, `0x222`, `0x224/0x225/0x226`, `0x228`, `0x229`, and one unknown typed topic to confirm the topic-buffer rewrites and the `0xff1[1]`, `0xf88[3]`, `0xf95[0]`, `0xfa0[0]`, `0xfaa[1]`, and `0xfba[2]` responses.
- Immediate: run the same durable single-handler cycle on `DWAYNE`. A fresh `verify DWAYNE` against the saved `HARVEST.LE` database still reports `handle_talk_to_dwayne @ 0x3a310` as truncated to `0x3a310..0x3c385` (`3375` address-count) with `59` reachable dialogue calls, `59` stale `CALL_RETURN` gaps, and `status=needs_repair`.
- Follow-up: if `ARCHITECTURE.md` needs another compression pass, move the longest branch-by-branch NPC dialogue traces into a companion note or appendix and keep the main file focused on subsystem structure, stable data types, and engine relationships.
- Immediate: run an in-engine Madam validation pass that explicitly covers the recovered native branches: trigger `MADAM_INTERRUPT_CONVERSATION`, take the non-`1` `dialog.rsp[0xfb]` path once to confirm `0x1577[0] -> 0x157b[2]` and the queued live `HOOKER` death-or-monsterfy transition; then take response `1` without `BARCASH200` in inventory to confirm `PC 0x156e[0] -> MADAM 0x1572[0] -> 0x157b[2]` and the same death transition; finally repeat with visible `BARCASH200` in inventory to confirm both inner `dialog.rsp[0xfc]` exits, including response `1` to `0x154e[0] -> 0x157b[2]` and response `2` to `0x1555[1] -> HOARLOVE.FST -> 0x155a[0] -> 0x1563[0] -> C117.FST`, `BARCASH200` removal from inventory, and the `HURT_PC_0` plus `ACTV_LUST_EXIT` action-tag side effects. Also dismiss `dialog.rsp[0xfc]` once to verify the native handler simply returns without forcing a fallback bark.
- Immediate: run an in-engine Librarian validation pass that explicitly covers the recovered native branches: enter the conversation empty-handed and take both `dialog.rsp[0xfa]` responses plus one dismissed menu to confirm `0x1d85` versus `0x1d8b[4]` and the always-present shared tail `0x1d8f -> 0x1d94 -> 0x1d98 -> 0x1da1 -> 0x1da5`; present one arbitrary wrong item to confirm the handler now only plays `0x1d5b`; then present or visibly carry `CAINBOOK` to confirm `CLUE` is awarded, the full `0x1db1[1] -> 0x1dc6` trade sequence plays, and `CAINBOOK` disappears from visible inventory afterward.
- Immediate: run an in-engine Inquisitor validation pass that explicitly covers the recovered native sequence: talk to `INQUISITOR` with `PAIN_SOLVED` clear and the local talk-state bit freshly reset to confirm `0x18e7 -> 0x18e8 -> 0x18e9 -> TORCH2.FST -> 0x18f8 -> START_INQ_TIM`; talk again in the same eligible state to confirm the cleared local bit suppresses the sequence on repeat visits; then set `PAIN_SOLVED` and confirm the handler returns silently even after a room/reset cycle re-arms the local state.
- Immediate: run an in-engine Gladiator validation pass that explicitly covers the recovered native tree: trigger `GLADIATOR_INTERRUPT_CONVERSATION`, confirm the opening `0x69c[1] -> 0x6a3[0]`, take response `1` on `dialog.rsp[0xc3]` to verify the `0x6af[2] -> 0x6cd[2]` death tail and queued live Gladiator death-or-monsterfy transition, then take response `2` to verify `0x6b3[0] -> C110.FST -> dialog.rsp[0xc4]` and both second-menu exits (`0x6c3[2] -> 0x6cd[2]` death tail versus `0x6c7[1] -> 0x6cd[0] -> START_MERCY_TIMR`). Also dismiss each response menu once to confirm the native handler simply exits without playing an extra fallback bark.
- Immediate: run an in-engine Dark Woman validation pass that explicitly covers the recovered native tree: direct `HANDMIRROR` use to confirm `0x4d03[2] -> C109.FST -> 0x4d0d[3]`, `HANDMIRROR` removal from inventory, and `SET_D_W_FLAG`; a fresh no-item talk through response lines `0x4b`, `0x4c`, and `0x4d` to confirm response `1` on any menu plays `0x4ce2[2]` and queues the live Dark Woman death-or-monsterfy transition; response `2` through all four menus without the mirror in inventory to confirm `dialog.rsp[0x4f]` and the `0x4cfc[2]` fallback; then the same path with `HANDMIRROR` already owned to confirm `dialog.rsp[0x4e]` and the shared reveal branch even without directly presenting the item. Also present one wrong item on the first visit to confirm the native handler falls through to the intro instead of playing a rejection bark.
- Immediate: run an in-engine Curator validation pass that explicitly covers the recovered native tree: take both `dialog.rsp[0x4a]` choices to confirm `0x430[3]` versus `0x435[0]`, confirm the shared tail `0x43a[0] -> 0x43b[0] -> PC 0x447[0] -> 0x44b[3]` always follows either valid choice, cancel or dismiss the menu once to verify the branch-specific line is skipped while the shared tail still plays, and present one arbitrary item to confirm the native handler ignores used objects.
- Immediate: run an in-engine Cloak Attendant validation pass that covers the full recovered native space: empty-hand talk before `CLEANED_CLOTHES` to confirm only `0x1066`; empty-hand talk after the startup flag is set to confirm silent return; `BARCASHFIVE` presentation to confirm `CLEAN_CLOTHES` still dispatches and applies its room/inventory effects; and one arbitrary non-payment item to confirm the native handler ignores it without falling back to the no-item bark.
- Immediate: run an in-engine Ryder validation pass that explicitly hits the recovered native branches: first talk through both `0x2a1` responses to confirm the optional `AUTOGRAPH` award versus `0x4a9 -> 0x4ae`; `PHOTO_OF_WHALEY_HERRILL`, `CASKET_PHOTO` / `CASKET_PHOTOCOPY`, and note/checkbook evidence to confirm the restored shared evidence-state writes now land alongside the existing startup-script flags; the `KARIN_KIDNAPED` tail through both `0x2a3` responses to verify `C063D.FST`, `C063E.FST`, and the autograph alternative; the keyword loop through both `0x2a7` / `0x2a8` interview topics and both `0x2a9` responses to confirm `C101.FST`, `0x511`, `0x521`, and `0x527`; and the `BURNED_TV_STATION` override with and without `BOYLE_BURNED_TV` set to confirm presented items are ignored and the `RYDER_TV_OUT` line chain still short-circuits correctly.
- Immediate: run an in-engine Chessmaster validation pass that explicitly hits both menu responses and all three puzzle outcomes: response `1` with the native correct hotspot on each of `CHOOSE1`, `CHOOSE2`, and `CHOOSE3` to confirm `CHESS_SOLVED`, `C125.FST`, and the queued Chessmaster slash-death transition; response `1` with a wrong hotspot on each stage to confirm the immediate `0x4126[2] -> ADD_CHESS_KING_MONSTER -> KINGGROW.FST` fail path; and response `2` or a canceled menu selection to confirm the puzzle is skipped and the same monster branch fires directly.
- Immediate: decode one shipped `HARVEST.SCR` sample and lift a few real `COMMAND` clusters into [`OPCODES.md`](/Users/alex/Workspace/scummvm/OPCODES.md) so the new `triggerTag` / `currentTag` section includes native examples instead of illustrative ones. Keep the examples tied to records and continuations that are actually present in data.
- Immediate: run an in-engine Beggar validation pass that explicitly hits the recovered native-only dialogue tree: trigger the `BEGGAR_INTERRUPT_CONVERSATION` path, take both `dialog.rsp[0]` answers to confirm `0x128d[1]` versus `0x1291[3]`, then both `dialog.rsp[1]` answers to confirm `0x12ab[2]` versus `0x12a5[2]`, and verify that `BEGGAR_DIALOG_3` still produces the intended post-dialogue room mutation / monsterfy result once the conversation exits.
- Immediate: run an in-engine Authority validation pass that explicitly covers all recovered native branches: `STOP_AUTHOR_TALK` set to confirm silent early return; `IF_DON_T_EAT_THE_FOOD` alone to confirm only `0x3992`; both food flags set to confirm `0x3992` followed by `0x399b`; `IF_DON_T_EAT_THE_FOOD` clear with only `IF_YOU_EAT_THE_FOOD_AND_DEFEAT_THE_ENEMIES` set to confirm the direct `0x399b` bark; and both food flags clear to confirm the default `0x398a` path.
- Immediate: run an in-engine Fireman1 validation pass that explicitly covers the recovered native-only follow-ups: first talk with neither flag set to confirm only `0x453`; a revisit with neither flag set to confirm only `0x463`; a talk after `STEPH_MIDGAME_PLAYED` to confirm the appended `0x45a[3]` bark; a separate talk after `BOLT_OF_CLOTH_TAKEN` to confirm the appended `0x46a[2]` bark; and a save/load cycle after each follow-up has fired once to verify both Fireman1-local gates persist and do not replay on the next revisit.
- Immediate: run an in-engine Sparky validation pass that explicitly hits the corrected native-only paths: `PC_TRIES_TO_TURN_ON_LIGHT`; any non-empty presented item for the `0x3c7[2]` rejection bark; the intro through all three `0x2be` responses to confirm `0x2f7[1]`, `0x2fe[2]`, `0x30d[1]`, the `SPARKY_SPOTS_MEAT` action tag, and the `C135/C098/C096` FST inserts; same-day and later-day revisits to confirm `0x3bb[1]` versus `0x3ac[1] -> 0x3b0 -> 0x3b4`; and the keyword loop starting from the native empty menu so typed topics `0x2c4/0x2c5`, `0x2c7`, `0x2c8`, `0x2ca/0x2cb`, and an unknown topic verify the restored `0x34c[1]`, `0x35f[0]`, `0x36c[0]`, `0x36d[0]`, `0x37a[3]`, `0x38f[4]`, `0x39e[1]`, and `0x3c1[1]` portrait variants plus the non-persistent `0x2c9` topic-list rewrite.
- Immediate: run an in-engine Phelps validation pass that explicitly hits the corrected native-only portrait flow: first no-item talk to confirm the `0x1b44[1] -> 0x1b48 -> 0x1b4c -> 0x1b52 -> 0x1b56[1] -> 0x1b5a` intro and the delayed `d2f04` write; a later day-1 to day-4 revisit through both `0x22a` responses to confirm `0x1bf4[1]`, `0x1b7b[1]`, `0x1b85[0]`, and `0x1bbc[1] -> 0x1c44[1]`; `PHOTO_OF_WHALEY_HERRILL`, `CASKET_PHOTO` / `CASKET_PHOTOCOPY`, note/checkbook evidence, and `QUARTER` to verify the recovered portrait variants plus `GET_PRN_MAG` / `DEL_DRT_MAG`; and separate revisits for `STEPH_MIDGAME_PLAYED`, day 5, `SCRATCHED_TUCKER`, `BARBER_POLE_STOLEN`, `PC_ESCAPED_JAIL`, Jimmy death response menu `0x22c`, `KARIN_FOUND_DEAD`, and `KARIN_KIDNAPED` to confirm the restored one-shot bark variants and shared kidnapped-dialogue write.
- Immediate: run an in-engine Sergeant and Nude Man validation pass against both a fresh playthrough state and an older ScummVM save created before this fix. Confirm Nude Man now stops after `0x628[1]` until the Sergeant `SCRATCHED_TUCKER` branch has actually set the native first-task completion state, then verify the second menu `0x25e` unlocks correctly after that branch and still unlocks after loading a pre-fix save where only Sergeant's legacy local completion bit was persisted.
- Immediate: run an in-engine Parsons validation pass that explicitly hits the restored native-only flow: first no-item talk to confirm the full `0xdcc[1] -> 0xdd2[0] -> 0xdd6[0] -> 0xde3[1]` intro plus the initial `Pole/Alarm System/Pastorelli` topic buffer; `STEPH_MIDGAME_PLAYED`, `SCRATCHED_TUCKER`, `BARBER_POLE_STOLEN`, `DINER_BURNED`, `BURNED_TV_STATION`, and `KARIN_KIDNAPED` on separate revisits to verify each Parsons-local one-shot gate now persists across save/load and that the Swell exchanges reset the active portrait back to Parsons; `CASKET_PHOTO` / `CASKET_PHOTOCOPY` with and without shared state `d2ebc` to confirm the optional `PC 0xe84 -> PARSONS 0xe88` continuation; note/checkbook and TV-deed evidence to verify the restored multi-line evidence replies and portrait variants; and the keyword loop through `Pole`, `Alarm System`, `Aliens`, `Pastorelli`, `barber`, and `Mr. Pastorelli` plus both visible `BYE` exits to confirm the `C093.FST` / `C094.FST` inserts, topic-buffer rewrites, and common `0xe5f` exit line.
- Immediate: run an in-engine Mother validation pass that explicitly hits the recovered native-only flow: first talk with response `1` to confirm `0x26d6[3] -> C072.FST -> 0x26e9[0] -> menu 0x173 -> 0x26f5[3] -> C073.FST`; restart or reload and take response `2` to confirm `C073.FST -> 0x2709[3] -> C072.FST -> 0x2714[0] -> 0x2719[3]`; then cancel the menu once to verify the native silent fallthrough and confirm a second talk in the same room returns immediately because the local talk-state bit is cleared after the first entry.
- Immediate: run an in-engine Boyle validation pass that explicitly hits the corrected native-only paths: intro response menu `0x5 -> 0x7 -> 2` to confirm the new Boyle-local evidence-reply bit persists and later changes the `BOYLES_BUTTON` and note/checkbook continuations; `BOYLES_BUTTON` through both `0x2` responses to verify `PC 0x179`, the optional `PC 0x186 -> BOYLE 0x18b` tail, the `d2e98` / `d2eb0` writes, and simple inventory hiding instead of object reset; note/checkbook evidence through response menus `0x3` and `0x4` with and without `HAVE_LODGE_APP` to confirm the restored `PC 0x141` / `0x14a`, `PC 0x1a9`, optional `PC 0x186 -> BOYLE 0x1b8`, and the no-item follow-up staying gated off shared note/checkbook state rather than the local Boyle branch; `GASCAN` after Boyle has accepted the application deal to confirm only `GASCAN` hides and `LODGE_APPLICATION` is awarded; `CASKET_PHOTO` / `CASKET_PHOTOCOPY` with and without shared `d2ebc` to verify the optional `PC 0x253`; and keyword topics `0xe`, `0x17`, `0x18..0x1b`, typed `Arsonist`, and both visible `BYE` entries to confirm the restored PC/Boyle speaker split and native exit flow.
- Immediate: run an in-engine Moynahan validation pass that explicitly hits the corrected native-only branches: `IF_TRY_TO_TAKE_THE_GLUE` before and after the slashes-on-body topic unlock to confirm the short `0x3e76` / `0x3e7a` versus extended `0x3e5b..0x3e71` interrupt; presented `CASKET_PHOTO` / `CASKET_PHOTOCOPY`, both ledgers, note/checkbook evidence, `PHOTO_OF_WHALEY_HERRILL`, and `WED_MATCHES` to verify the restored response menus, shared-state writes, portrait variants, and both glue handoff paths; a no-item revisit after shared corpse-photo evidence to confirm `0x3f5b` re-arms the intro; day 5, `BURNED_TV_STATION`, `KARIN_KIDNAPED`, Butcher death, Jimmy death, and `KARIN_FOUND_DEAD` to verify the one-shot bark chain plus the shared kidnapped-discussion writes; and the keyword loop through `0x19a/0x19b`, `0x19d..0x1a0`, `0x1a2/0x1a3`, `0x1a9..0x1ab`, `0x1b2/0x1b3`, `0x1b4/0x1b5` in `MBLM`, `0x1b8..0x1be`, `0x1bf..0x1c2`, typed unknown text, and both visible `BYE` entries to confirm the restored topic-buffer rewrites and fallback flow.
- Immediate: run an in-engine Buster validation pass that explicitly hits the restored native-only paths: `PHOTO_OF_WHALEY_HERRILL`, `CASKET_PHOTO` / `CASKET_PHOTOCOPY`, and note/checkbook evidence through response menu `0x23` to confirm the recovered shared-state writes and both blackmail continuations; the first three no-item visits to verify the full `0x163c..0x168e`, `0x1828..0x185e`, and `0x18c5..0x18cd` sequences plus the saved `secondIntroResponseTwoSeen` gate; the keyword loop through topic groups `0x28/0x29`, `0x2c/0x2d`, `0x32`, `0x34`, `0x36`, `0x38`, `0x3b`, `0x3e`, `0x40/0x41`, `0x44/0x45/0x46`, `0x48`, and `0x49` to confirm the restored topic-buffer rewrites and `C058.FST` / `C085.FST` inserts; and at least one fatal branch (`0x183a`, `0x1850`, `0x16d0`, `0x16ef`, `0x1702`, `0x1804`, or `0x18df`) to confirm `C086.FST` plus the recovered game-over screen now hand back to the main menu correctly.
- Immediate: run an in-engine Herrill validation pass that explicitly hits the corrected native-only paths: `HERRILL_IN_LODGE` before `PC_TALKED_TO_HERRILL` to confirm the full `0x31ca` / `0x31ce[4]` / `0x31d2` / `0x31da` / `0x31de[2]` sequence, the optional `0x31e0[2]` / `0x31e2[2]` insert when the shared Whaley/Herrill-photo flag is already set, the `GRAND_MUCK` monsterfy transition, and the one-shot `PC_TALKED_TO_HERRILL` gate; `PHOTO_OF_WHALEY_HERRILL` followed by a no-item revisit to confirm Herrill no longer self-arms the shared photo followup; `KARIN_FOUND_ALIVE` and `KARIN_FOUND_DEAD` to verify the non-native `0x3050` / `0x3057` and `PC 0x461b` lines are gone; `Lodge`, `The Lodge`, and an unrelated typed topic through the keyword loop to confirm `0x2fdd` on both lodge aliases versus the `0x3099 -> 0x2ff4` unknown-topic fallback; and both visible `BYE` entries to confirm they now share the direct `0x2ff4` exit.
- Immediate: run an in-engine Dad validation pass that explicitly hits the newly restored native-only paths: `TAKING_BONDAGE` to confirm the `0x3a61 -> C011.FST -> 0x3a6b` tail; first no-item talk to confirm the full `0x39a5` / `0x39a9` / `0x39ad` / `0x39b1[PC2]` / `0x39b5` intro and the shared `d2ed0` / `d2f04` writes; no-item revisits with and without shared state `dadMeatPermissionState` to confirm the automatic `C112S4.FST` permission-conversion branch and the empty visible keyword menu; typed `Lodge`, `The Lodge`, and an unrelated free-text topic through Dad's Other input path to verify the restored `0x3a7d` / `0x3a83` / `0x3a88` special case vs the generic `0x3a2c` fallback; and presented `PHOTO_OF_WHALEY_HERRILL`, note/checkbook evidence, and `MEAT_PERMISSION0` to confirm the corrected shared dialogue-state writes and inventory side effects.
- Immediate: run an in-engine Butcher and Dad validation pass that explicitly hits the recovered native-only paths: first Butcher talk before and after talking to Dad once to confirm `0x190f` vs `0x1913`; the revisit bark on day 1 with and without `MEAT_PERMISSION0` / `MEAT_PERMISSION` to confirm the `0x261` topic buffer disappears once permission is owned or after day 4; topics `0x263`, `0x267`, `0x26b`, `0x26e`, `0x270`, `0x273/0x274`, `0x277`, and the `0x279` no-op path to verify the restored menu rewrites plus `C119/C060/C119A/C119B/C123/C123A` FST tails; presented `MEAT_PERMISSION` to confirm `MEAT` is awarded and `GOT_MEAT` is set; and then Dad with `MEAT_PERMISSION0` both before and after Butcher grants it to confirm Dad now reads the shared permission state and the first Dad intro seeds `dialogueStateD2ed0`.
- Immediate: run an in-engine Edna validation pass that explicitly hits the restored native-only branches: `KILLED_KARIN1` to confirm the flag now clears; `DNA_S_SUICIDE_NOTE` with and without `KARIN_FOUND_ALIVE` to confirm `C043.FST` / `C043K.FST`, shared states `d2ea4` / `d2ea8`, and `DNAEXT:SIGNOUT`; direct `K_PURSE` use plus the `KARIN_FOUND_DEAD -> 0xa2 -> 0xa3/0xa4` nested purse menu to verify `REWARD_MONEY` and the `RAH`-hidden purse side effect; the day-5 alive and dead follow-ups; and the keyword loop through the recovered `0xa6`, `0xac/0xad`, `0xb0`, `0xb2`, `0xb6/0xb7`, `0xba`, `0xbc/0xbd/0xbe`, `0xc0`, `0xc2`, typed unknown text, and goodbye branches to confirm the restored menu buffer rewrites and both `C091.FST` / `C070.FST` exits.
- Immediate: run an in-engine Karin validation pass that explicitly hits the restored native-only branches: the first `KARIN_FOUND_ALIVE` + `IN_CEM10` visit to confirm the full `0x10a5`..`0x10c3` early-return sequence; first and repeat no-item visits before Karin is found alive to confirm the recovered `0x1076`..`0x108b` prelude plus the shared `0x10dc` bark; first and repeat uses of `CASKET_PHOTO` or `PHOTO_OF_WHALEY_HERRILL` to verify the `0x10e7` Edna follow-up, portrait reset, shared evidence-state write, one-shot override, and repeat-photo `CALL_POLICE` escalation; and `INV_MAG` to confirm `0x10ee` / `0x10f2[2]` plus `DNALFT_PERVERT`.
- Immediate: run an in-engine Dwayne validation pass that explicitly hits the restored native-only branches: `CHECK_EVIDENCE_DOOR` and the first/second `ST_BEDRM` visits to confirm the new flag clears; the sheriff-in-diner intro and revisit to verify Edna `0x3490/0x3496`, the optional `0x34a0/0x34a4` tail, and the later keyword loop; Boyle's button, Karin's purse, note/checkbook photocopy -> original note/checkbook, both `TV_DEED` and `TV_DEED_PHOTOCOPY` response trees plus the override revisit on the real deed, corpse photo with and without shared state `d2ec8`, and both-ledger handoff with and without `SHERIFF_IN_DINER`; `KARIN_KIDNAPED` through response menus `0x7b/0x7c`, `KARIN_FOUND_DEAD` without the purse through `0x7f/0x80`, and the `PC_HAS_GOOJF_CARD` arrest path with and without `PC_KILLED_ANYONE` to confirm the confiscation and bust follow-up state now matches native.
- Immediate: resume the sequential talk-handler sweep at `MEMB_DIR`, using the proven single-handler workflow `verify <NPC> -> repair <NPC> -> wait ~10s -> save_program -> verify <NPC>` and immediately rerunning the same NPC once when the first saved verify snaps back. After the durable Lodge Chef pass, the durable Sparky retry, the earlier Pastorelli/Parsons work, and the Ryder re-audit, the remaining not-yet-confirmed handler in that queue is `MEMB_DIR`.
- Immediate: run an in-engine Johnson validation pass that explicitly hits the restored native-only branches: `OPENING_MANHOLE_DAYTIME` twice to confirm `0xbe0` now clears the flag; `PHOTO_OF_WHALEY_HERRILL`, `CASKET_PHOTO` / `CASKET_PHOTOCOPY` with and without shared state `d2ebc`, and `LEDGER` / `LEDGER2` with `HAVE_BOTH_LEDGERS` to verify the restored `0xb7b`, `0xb80`, `0xb90`, `0xb94`, `0xb99`, `0xba4`, and `0xba8` follow-ups; the first `SCRATCHED_TUCKER` visit with `BOLT_OF_CLOTH_TAKEN` already set to confirm `0xc25` / `0xc2b` / `0xc30` plus `0xc5f` / `0xc69` and that the Johnson intro only replays on the later revisit; and a `KARIN_KIDNAPED` follow-up that later reaches a consumer such as Stephanie to confirm the shared kidnapped-dialogue state now propagates correctly.
- Immediate: run an in-engine Loomis validation pass that explicitly hits the restored native-only branches: first talk with and without `SHERIFF_IN_DINER` to confirm the `0x1112 -> 0x1118 -> 0x1121 -> response 0x53 -> 0x1130[3]` intro; the second and third no-item visits to confirm the one-shot `0x11a8/0x11ac/0x11b0[3]` follow-up, the later `0x11f6` bark, and the `STEPH_MIDGAME_PLAYED` / day-5 `0x1230[3]` and `0x123c` gates; the keyword loop through `LOOMIS`, `Phelps`, `Man's Needs`, sheriff topics, `0x65`, `0x66`, typed unknown text, and goodbye to verify the restored alias matches, portrait variants, `0x11fc` fallback, and `0x11a1[1]` exit; note/checkbook or TV-deed evidence through both `0x50` responses to confirm `0x1214` / `0x1219` / `0x121f[3]` plus `PC_FRY_DADDY_1`; and `INV_MAG` in the diner branch through both `0x51` responses to confirm `0x11c6[1]`, `0x11cc[3]`, drawer visibility, `GAVE_MAG_TO_LOOMIS_TODAY`, and the `C048.FST` tail.
- Immediate: run an in-engine Mrs. Potts validation pass that explicitly hits the restored native-only paths: `PHOTO_OF_WHALEY_HERRILL`, `CASKET_PHOTO` / `LEDGER` with `HAVE_BOTH_LEDGERS`, and the `GOT_REMAINS_FOR_LODGE` response-1 death branch to confirm the corrected portrait variants; the non-dead revisit gate around `0x281b` / `0x2820` / `0x2825`; and `DINER_BURNED` with and without `KARIN_HUNG`, taking both `0x236` responses to verify the new `0x2914` / `0x2918` / `0x2926` continuation and the response-2 jump into the shared death-in-chair sequence.
- Immediate: run an in-engine Mr. Potts validation pass that explicitly hits the restored native-only branches: the `HARVEST_BLADE` response menu and `MONSTERFY_POTTS` tail; `STEPHANIE_DEAD_IN_HALL_JUST_OUTSIDE` to confirm the full `C001B.FST` / `STHLEXIT2B` exit; first and repeat `POTTSDAM_DIGGING`; `POTTSDAM_DIGGING_KARIN`; `MEAT` through both `0x1d4` responses to confirm the reset path; `CASKET_PHOTO`, `PHOTO_OF_WHALEY_HERRILL`, note/checkbook, and TV-deed evidence to verify the restored multi-line exchanges; the later-day Stephanie-alive revisit on day 2 through day 5 to confirm `0x2ae5`; the `KARIN_KIDNAPED` response-2 branch to verify the `STEPH_MIDGAME_PLAYED` gate; the `KARIN_FOUND_DEAD` `0x2b8d..0x2bb6` follow-up; and the topic loop branches for `AMNESIA`, `FATHER`, `SLAUGHTERHOUSE`, `TOILET`, and `MOLEST` to confirm the recovered buffer rewrites and nested menus.
- Immediate: run an in-engine Jimmy validation pass that explicitly hits the corrected native-only paths: the first no-item visit through response menus `0xf5`, `0xf6`, and `0xf7`; the later no-item `SNEAKERS` revisit through response menu `0xf8`; first and repeat `NEWSPAPER` handoffs to confirm `0x4a4c` / `0x4a52` and the `0x4acc` / `0x4a4b` random bark; direct `SNEAKERS` use to confirm `BROOMKEY` is awarded; and the `LEDGER` / corpse-photo plus note/checkbook evidence exchanges to verify the restored `0x4b19` and `0x4b31` tails.
- Immediate: run an in-engine Mom validation pass that explicitly hits the now-corrected native-only paths: `CASKET_PHOTO`, `PHOTO_OF_WHALEY_HERRILL`, the `JIMMY_ATTACKED` branch, the `POTTSDAM` second response that reaches `0x20e3`, the `MEAT PLANT` first response (`0x2106 -> 0x210a -> 0x210e`), the `FATHER` topic to confirm `momFatherTopicState` persists through save/load, and the fallback `0x26c6` bark to verify it returns to the keyword loop instead of ending the conversation.
- Immediate: run an in-engine Sergeant validation pass that explicitly covers the newly restored native tails: `DINER_BURNED` through both `0x2af` and `0x2b0` menus to confirm the `INVITE` / `STEFSKULL` / `DWAYNE_ST_BEDRM` world-state mutations, and `BARBER_POLE` through response menu `0x2b2` with and without `PC_KILLED_ANYONE` set to verify the `0x42a4` / `0x42a8` / `0x42ad` control flow and the day-5 exit fallthrough.
- Immediate: run an in-engine Stephanie validation pass that hits the corrected native-only branches: first intro/revisit, the `0x45d8/0x45e2` first-followup tail, `SCRATCHED_TUCKER` response `1 -> 1` to confirm `0x4709` keeps the PC portrait on variant `2`, the Mrs. Potts alibi tail around `0x4984/0x4989/0x498f`, and both Karin-alive outcomes with and without the Dwayne follow-up gate to verify that only the gated path plays `0x49c1` plus `0x49ea/0x49ef`.
- Follow-up: after the sweep finishes, fold the reusable gap-fix logic back into `InspectCallTermination.java` itself so it can automatically clear/disassemble exact fallthrough ranges and optionally absorb tiny tail functions instead of requiring ad hoc helper-script intervention for cases like Boyle, Wasp Woman, and Mom.
- Follow-up: keep an eye out for any additional tail `JMP` dialogue transfers when finishing the remaining NPCs. `HERRILL` proved that some apparent one-gap leftovers are verifier false positives caused by stale `CALL_RETURN` metadata on jump-based dialogue tails rather than missing handler continuations.
- Immediate: continue the NPC talk-handler sweep one handler at a time with top-level `verify <NPC>` followed by `repair <NPC>` only when needed, and after each successful repair call `save_program` plus a fresh `verify <NPC>` to confirm the saved `HARVEST.LE` state actually held. Keep using single-handler passes; do not resume `repairrange`.
- Immediate: restart the live Ghidra GUI/plugin session that owns `HARVEST.LE`, then verify the plugin server is responding on `127.0.0.1:8089` before retrying `repair STEPHANIE`. The current server process is listening but not answering, so no further MCP/script work against `HARVEST.LE` is possible until that restart happens.
- Immediate: restart the Ghidra GUI / MCP bridge cleanly before attempting any more NPC repairs, because the last dirty-handler save experiment left `run_ghidra_script`, `save_program`, and even `check_connection` timing out. After restart, reopen `HARVEST.LE`, rerun `verify JIMMY` first to confirm whether the saved single-handler repair survived restart, then continue with top-level `repair <NPC>` one handler at a time from `BUSTER` onward.
- Follow-up: once the fresh session is up, finish isolating the dirty-save blocker inside `InspectCallTermination.java`. The most likely next experiments are: wait/poll for auto-analysis completion without calling `analyzeChanges()` from the Swing thread, or temporarily disable/suppress auto-analysis around handler recreation so `saveProgram()` can complete after a dirty repair. Do not resume `repairrange` until a dirty single-handler save has been proven durable on disk.
- Immediate: run an in-engine Harvester combat pass in `MR_JOHNS` to confirm Johnson's monster now enters melee instead of looping `reason='no native attack bank'`, and verify at least one lethal monster hit still plays a death bank now that full-frame-range checks are used.
- Follow-up: if melee now triggers but the chase still feels too aggressive, capture one short native-vs-ScummVM comparison clip and re-audit the class-6 locomotion step sizing around `update_actor_runtime_state` before changing movement cadence; the current Ghidra evidence does not justify slowing monsters yet.
