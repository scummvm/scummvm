cmake_minimum_required(VERSION 3.23)

project(ScummVM)

add_library(engines)

target_link_libraries(engines PRIVATE common image gui)

target_sources(
  engines
  PRIVATE achievements.cpp
          advancedDetector.cpp
          dialogs.cpp
          engine.cpp
          game.cpp
          metaengine.cpp
          obsolete.cpp
          savestate.cpp)

# Generate "engines/plugins_table.h"
file(REMOVE "plugins_table.h")
file(APPEND "plugins_table.h"
     "/* This file is automatically generated by CMake */\n")
