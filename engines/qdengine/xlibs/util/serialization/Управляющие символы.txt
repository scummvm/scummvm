ѕри использовании PropertyI/OArchive-ов (например, через kdw::edit или
kdw::PropertyTree::attach) доступен набор управл€ющих символов.
¬писываютс€ как первые символы в nameAlt, в любом пор€дке:

 "&" - вынести значение в дайджест родительского элемента. ƒайджест - серенький текст после имени.
 ќтражает краткое содержание структуры. ”добно выносить именна и другие важные данные
 (чаще всего используетс€, дл€ поиска нужного элемента в списке).
 
 "!" - только чтение. «апрещает изменение значени€ элемента (не рекурсивно, только текущий уровень). 
 ƒл€ контейнеров запрещает измен€ть количество элементов в нем.
 
 "^" - выносит значение в корень структуры. ћожет быть сделано не более двух раз внутри одной
 структуры: один раз дл€ контейнера (тогда элементы отображаютс€ сразу после остальных значений) и
 один раз дл€ пол€ или галочки. ≈сли в nameAlt после управл€ющих символов имени нет (напр. просто
 "^"), то элемент пр€четс€ идоступен только через корень стурктуры.
 
 "<" - распахивает поле со значением на максимальную ширину. ”добно дл€ длинных полей.
 nameAlt может быть и просто "<", тогда вс€ строчка будет зан€та значением.
